# Archived done entries from `bugs/gaps.md`

Moved verbatim from `bugs/gaps.md` to keep the active tracker small. Entry numbers are unchanged — a reference like `gaps #N` resolves here once the entry ships.

1. [x] Email plugin lacks the ability to do file attachments
       — Shipped: `Attachment { filename, content_type, data: Vec<u8> }` struct + `EmailMessage::attach(filename, content_type, bytes)` builder. Bytes-only by intent (no path-loading, no auto content-type, no inline images) — users with files do `std::fs::read(path)?` themselves; users wanting any of the deferred shapes get them when a real consumer surfaces a need. When attachments are present, `compose()` wraps the existing body in `multipart/mixed` with the body part(s) (text-only / html-only / alternative) nested inside per RFC 2046. 11 new tests in `tests/attachments.rs` pin the on-wire MIME shape (multipart/mixed wrapper, Content-Disposition: attachment, base64 transfer encoding for binary payloads, alternative-inside-mixed for text+html+attachment). See `documentation/docs/v0.0.1/plugins/email.mdx`.
2. [x] Django has special way of doing pages ie 404, 500 (Internal server error) which is quite symbol and direct
       — Shipped via `App::builder().not_found_template("404.html")` and `.server_error_template("500.html")`. Composes with `slash_redirect`. See `documentation/docs/v0.0.1/web/error-pages.mdx`.
3. [x] The current version does not make use of the cli. Ie instead of `cargo run`, you must use `umbra serve` to run the server. Or even better, I need something like umbra startproject, startapp commands to set up a new project like django. We need to make it easy to setup the app that way with `apps`. We talked about this, it might be implemented but docs are out of sync by far.
       — Shipped via commits 95f0709 (code) + e5645a4 (docs). `cargo install umbra-cli` installs a global `umbra` binary with `startproject` + `startapp`. User binaries call `umbra_cli::dispatch(app).await` from `main.rs` to host the management subcommands (serve / migrate / makemigrations / inspectdb / dumpdata / loaddata). `cargo run -- serve` is the project-local way to invoke them. See `documentation/docs/v0.0.1/cli/startproject.mdx` + the rewritten `management-commands.mdx`.
4. [x] Docs are out of sync with the current version ie how to use plugins. There is no plugin section on how to use `rest` plugin for example. So the rest plugin should make it easy to create rest views with minimal effort and with the ability to use custom fields like in django we define the view and then the fields, currently it returns the whole struct/model. DRF classes make it easy to even redefine fields ie you can hide emails ie using a method like get_email(obj) and it returns "***@mail.com" instead of the actual email by spliting at the `@` symbol and replacing the username with `***`.
       — Docs portion: rest.mdx + openapi.mdx + rls.mdx + sessions.mdx + tasks.mdx + admin.mdx + web/ section all landed (commit 1cdbc18).
       — Custom field rendering shipped: `RestPlugin::hide(table, field)`, `.transform(table, field, |v| ...)`, `.computed(table, name, |row| ...)`. Per-row, per-table, applied in hide→transform→computed order on every outbound response. The example `transform("user", "email", mask_email)` in rest.mdx now matches exactly the DRF `get_email(obj)` pattern called out in the gap. 8 new tests in `tests/field_overrides.rs`.
5. [x] The openapi plugin should allow the user to pass in params to customize the openapi schema. ie the base path, title, version, description, etc.
       — `.description(s)` added in commit e42f408; `.at()`, `.title()`, `.version()`, `.exclude()` already existed.
6. [x] Security audit across the system and the plugins
       — Shipped via `cd656d5`. Four parallel audit agents (ORM/SQL, Auth+Sessions+RLS, Templates+XSS+Headers, CLI+Scaffolding+IO) produced triaged reports. Combined findings: 2 HIGH (both fixed), 6 MEDIUM (all fixed or documented), 8 LOW (key ones fixed, rest acknowledged in canary comments). HIGH fixes: session tokens SHA-256 hashed before DB storage so a DB leak doesn't surrender live sessions; CSRF compare uses `subtle::ConstantTimeEq`. MEDIUM fixes: defense-in-depth for dev secret_key when bind_addr isn't loopback; `model.table` quote-doubled in REST/admin/backup SQL; RLS using/with_check raw-SQL trust boundary documented with `# SQL injection warning` sections; admin sanitises sqlx errors to a fixed string (logs full error server-side); Content-Type in error pages derived from render result (not template-was-set). LOW: CRLF redirect canary comment, autoescape extension-whitelist sync warning, ConsoleBackend warns in non-dev environments. Major verified-clean surfaces: argon2id w/ per-password CSPRNG salt, account-enumeration defense, HttpOnly+Secure+SameSite cookie flags, autoescape on by default for HTML, no open redirects, scaffold name validation blocks path traversal, no secret logging, lettre's typed APIs for email headers (no manual construction → no CRLF injection in From/To/Subject/filename).
7. [x] There is no `.create()` method on `Model` objects
       — Shipped via `Manager::create(instance)` in commit 9ab3c00. INSERT + RETURNING * for the populated row. Autoincrement sentinel: PK == 0 (ints), nil UUID, or empty String are omitted from the INSERT so the DB assigns them; explicit non-default PKs are bound as supplied. Requires `T: serde::Serialize` (which models already derive for REST).
8. [x] There is no `.update()` method on `Model` objects
       — Shipped via `QuerySet::update_values(map)` in commit 9ab3c00. Takes a `serde_json::Map<String, Value>` of column → new value. PATCH semantics (absent keys keep current values). PK in the map is silently skipped to prevent identity rewrite while filtering on the old PK. Unknown columns raise `WriteError::UnknownColumn` early.
9. [x] There is no `.delete()` method on `Model` objects
       — Shipped via `QuerySet::delete()` in commit 9ab3c00. Applies accumulated filter predicates as WHERE; returns affected-rows count. Without filter calls, deletes every row — same semantics as raw SQL DELETE FROM.
10. [x] There are no bulk create/update/delete methods on `Model` objects
       — All covered by gaps 7-9's primitives: `Manager::bulk_create(Vec<T>)` produces one multi-VALUES INSERT (cheap, batched); `QuerySet::filter(...).update_values(map)` and `QuerySet::filter(...).delete()` are naturally bulk via the WHERE clause. 10 new tests in `tests/model_writes.rs` + 5 unit tests in the `crate::orm::write` module pin the JSON→sea_query::Value dispatch. See the new "Writing rows" section in `documentation/docs/v0.0.1/orm/models.mdx`.
11. [x] We need to go deeper on how to create a plugin/an app - http://localhost:5173/docs/v0.0.1/plugins/plugins-are-apps ie how to get access to the ORM from a plugin. So do a short but complete example of a plugin here, not in split parts, a single codeblock.
       — Shipped: new "## A complete plugin" section in `documentation/docs/v0.0.1/plugins/plugins-are-apps.mdx` with a single-codeblock `BlogPlugin` example covering: model declaration in the plugin file, two route handlers that hit `Post::objects().fetch()` and `.first()` directly (no `State<DbPool>` or pool plumbing — the ORM is ambient), a `rest_resource()` function that exposes the plugin's REST customization (hide/transform/computed) for `RestPlugin::default().resource(...)` to pick up, and an `on_ready` hook that bridges sync→async via `Handle::current().block_on(...)` to seed initial rows. The body wraps with the three "what matters here" points: ambient ORM, plugin-bundled REST customization, and the sync-on_ready / async-sqlx bridge.
12. [x] In the docs, http://localhost:5173/docs/v0.0.1/plugins/plugins-are-apps, add a section for Rest Plugin. Actually, the better way to do it to avoid mixing things up, create a folder maybe examples/plugins/ and copy the `BlogPlugin` example there for the Rest plugin and now show all the rest plugin features.
       — Shipped: `examples/plugins/blog/` is a standalone runnable Cargo project (not a workspace member) that demos every plugin concern in one place: model declaration + `#[derive(Model)]`, `Plugin::models()` auto-registration (no `.model::<Post>()` in main.rs), HTTP routes (`Plugin::routes()` at `/blog` and `/blog/{id}`), REST customisation bundled in `rest_resource()` (hide/transform/computed + the new `@action` endpoints `recent` + `publish`), and a seed-on-empty `seed()` helper called after `migrate::run()`. `on_ready` does an `info!` log instead of seeding because schema isn't applied yet when it fires — that distinction is the only non-obvious bit. README in `examples/plugins/blog/README.md` covers the running shape (`cargo run` from inside the directory; curl examples for each endpoint). End-to-end verified — list shows masked `***@example.com`, `recent` returns N newest, `publish` flips the boolean.
13. [x] Something else, currently, we register the plugin, the models for that plugin and other resources for the plugin, can we like just register the plugin and the rest get autoregistered?
       — Already shipped at the code level: `Plugin::models()` was wired through `App::builder()` at M7 (`crates/umbra-core/src/app.rs:313,335`), and `tests/plugin_contract.rs::plugin_contract_meta_lands_in_registry` proves a plugin-contributed model lands in `models_for_plugin("plugin_name")` with no `.model::<T>()` line. Docs were lying — `plugins-are-apps.mdx` still said "for now register with `.model::<blog::Post>()`; auto-discovery lands when M7 ships." Fixed: the BlogPlugin example now overrides `Plugin::models()`, the `main.rs` snippet drops the redundant `.model::<Post>()`, and a new "One `.plugin(...)` line is enough" call-out explains when `.model::<T>()` is the right tool (models in main.rs with no owning plugin) vs `Plugin::models()` (plugin-owned models, the default).

```rust
let app = App::builder()
    .settings(settings)
    .database("default", pool)
    .model::<blog::Post>()                       // register the model
    .plugin(blog::BlogPlugin)                     // JUST REGISTER THE PLUGIN and all models in it get registered to the app, same with the resource if any there. So we need a way to decouple plugins ie REST (I know resources in this case are just for the REST plugin) in this case to avoid double registration of it ie plugin x might use the same etc. Maybe register resources somewhere within the plugin itself, I don't know. We just need to make it easy and direct.
    .plugin(
        umbra_rest::RestPlugin::default()
            .resource(blog::rest_resource()),    // REST customisation
    )
    .build()?;
```

Though we can still keep the current state as is, if it has no harm to the system in general but do the updates

14. [x] How do we use different models in different areas of the app ie BlogPlugin vs CategoryPlugin, ie we need to reuse categories in other plugins etc. This question goes hand in hand like, how do we do foreign keys, many-to-one, one-to-many, many-to-many etc. In django we also use foreign keys extensively for many-to-one, one-to-many, many-to-many relationships without much hassle and this automatically helps in validation and data integrity.
       — Shipped: `ForeignKey<T: Model>` field type. Stores `i64` referencing `T::TABLE`'s PK. The derive emits `BIGINT REFERENCES "<T::TABLE>"("id")` DDL (both SQLite and Postgres), a `ForeignKeyCol<Owner>` constant in the sibling module (`.eq`, `.ne`, `.in_`, `.asc`, `.desc`), and `FieldSpec.fk_target = Some(T::TABLE)`. At runtime: `.id() -> i64`, `.set(i64)`, `From<i64>`, and async `.resolve(&pool) -> Result<T, sqlx::Error>`. Serialises as a plain integer. Tests: `tests/foreign_keys.rs` (10 tests). Docs: `documentation/docs/v0.0.1/orm/relationships.mdx` + column-types.mdx FK row + models.mdx PK table update. Deferred: non-i64 FK targets, ON DELETE beyond RESTRICT, reverse accessors, M2M. See `docs/specs/relationships.md` for the full cut record.
15. [x] What have we done to make sure the system can handle many requests safely. Things like proper database access, proper file handling, proper static handling for css/js and static files through cache (this is sensitive, should have a shutdown config ie static cache off to allow better development). Like Django does lots of things right, if its scaling, I can add redis cache and cache decorators to views
       — Shipped: three cooperating features in `plugins/umbra-cache/` and `plugins/umbra-static/`.
       (1) **Redis backend** (`plugins/umbra-cache/src/lib.rs`, behind `--features redis`). `Cache::redis(url)` connects via `redis::aio::ConnectionManager` (auto-reconnect). `RedisBackend` implements the existing `CacheBackend` trait exactly. TTL stored natively via `SETEX` so expiry is server-side. `Cache::sqlite()` return type widened to `Result<Self, CacheError>` (breaking, but correct — SQLite constructor can fail). New `CacheError` enum with `Redis(redis::RedisError)`, `Sqlx(sqlx::Error)`, `Other(String)` variants. `Cache::redis(url)` constructor gated on `#[cfg(feature = "redis")]`. Existing in-memory and SQLite backends unchanged except the `SqliteBackend::new` / `sweep` return type is now `CacheError` instead of `sqlx::Error`.
       (2) **`cache_page` view middleware** (`plugins/umbra-cache/src/cache_page.rs`, new). `cache_page(Duration)` returns a `CachePageLayer` implementing `tower::Layer`. `CachePageService` wraps the inner service: on GET/HEAD it checks `AMBIENT_CACHE` for a hit, on miss calls through, collects the body, serialises response headers + body in a compact binary format, and stores. Bypass rules: non-GET/HEAD methods, non-200 status, `Cache-Control: no-store`, `Set-Cookie`. Cache key: `cache:page:<METHOD>:<URI>`. `.with_cache(cache)` method on `CachePageLayer` for test injection without initialising the ambient OnceLock. Ambient fallback: silently a no-op if `CachePlugin::init` was never called.
       (3) **Static cache headers** (`plugins/umbra-static/src/lib.rs`). `StaticPlugin::max_age(Duration)` builder method adds `Cache-Control: public, max-age=N` via `tower_http::set_header::SetResponseHeaderLayer`. Dev-mode opt-out: when `umbra::settings::get_opt()` returns `Environment::Dev`, the effective max-age is forced to 0 regardless of the configured value. Requires adding `get_opt() -> Option<&'static Settings>` to `umbra-core/src/settings.rs` and re-exporting from `umbra::settings`.
       Tests: `plugins/umbra-cache/tests/cache_page.rs` (7 tests: second GET served from cache, POST bypass, no-store bypass, Set-Cookie bypass, query-string isolation, 404 not cached, Content-Type preserved). `plugins/umbra-cache/tests/redis_backend.rs` (8 tests, all gated on `#[cfg(feature = "redis")]` + `REDIS_URL` env — graceful skip when absent). `plugins/umbra-static/tests/cache_headers.rs` (4 tests: no header without max_age, correct header with max_age, max-age=0 emitted explicitly, files served in both modes). All 22 new tests pass; all 16 pre-existing tests still pass. Docs: `documentation/docs/v0.0.1/plugins/cache.mdx` (new), `documentation/docs/v0.0.1/plugins/static.mdx` (new). Deferred: memcached backend, distributed cache invalidation, ETag/304 conditional caching in cache_page, Vary-header awareness.
17. [x] There is no `.get` method on the model to get a single record by id or by any other field. Also we need the rest of the methods like `.first` if not yet implemented.
       — Shipped: `QuerySet::get()` + `Manager::get(predicate)` + `_pg` variants. Returns `Result<T, GetError>` with three variants (`NotFound`, `MultipleObjectsReturned`, `Sqlx`) — Django's exactly-one shape. Internally `SELECT ... LIMIT 2` to distinguish "one" from "many" cheaply. `.first()` was already implemented. 3 new tests in `tests/queryset.rs`. Docs: new "Terminals" subsection in `documentation/docs/v0.0.1/orm/models.mdx`.
18. [x] Django admin is so extensive ie you get alot of things out of the box by extending it ie you can add custom views, filters, actions, forms, etc. Do we have the same level of extensibility in our admin interface?
       — Shipped: `AdminConfig` builder in `plugins/umbra-admin/src/config.rs` (new). Per-model registration via `AdminPlugin::default().register(...)`. Six customisation methods covering the high-leverage 80% of Django's ModelAdmin: `list_display(&[col, ...])` picks visible columns; `list_filter(&[col, ...])` produces filter facets in the right sidebar; `search_fields(&[col, ...])` enables an `?q=` search box that ANDs `LIKE %term%` across the listed columns; `ordering(&["-published_at", "title"])` sets default sort (minus prefix = DESC); `actions(vec![Action::new("publish", handler)])` for bulk actions on selected rows with a built-in `Action::delete_selected()`; `readonly_fields(&[col, ...])` renders form fields as read-only. The admin list template (`templates/list.html`) gained filter sidebar, search box, ordering controls, and an actions dropdown. 6 new tests in `plugins/umbra-admin/tests/extensibility.rs`. Docs: new "Customizing the admin" section in `documentation/docs/v0.0.1/plugins/admin.mdx`. Deferred: fieldsets (form-section grouping), inlines (related-model editing on the parent form), prepopulated_fields, raw_id_fields.
19. [x] Defer this but have it somehwere as a pending task - We shall use tailwind css for everything, we are not going to rebuild any css by us for the admin interface, the default pages like 404, 500, etc. will use tailwind css by default.
       — Shipped piecewise across three landings: gap 35 (default 404/500 templates use inline Tailwind utility classes with a `<style>` reset fallback for when Tailwind isn't loaded), gap 45 (`templates/wrapper.html` holds the Tailwind CDN + design-token block + theme persistence IIFE, both `base.html` and `login.html` extend it), gap 47 (`AdminPlugin::default().site_title(...).brand_color(...)` chainable builders feed Tailwind's `--primary` token at boot). No remaining piece — the admin and the framework's default error pages are both Tailwind-first. The "tailwind CDN is dev-only" callout in `documentation/docs/v0.0.1/plugins/admin.mdx` flags the prod-build follow-up that's not part of this gap.
20. [x] When I start a project, do I get a complete scaffold out of the box? ie all the basic setup and configuration already done for me?
       — Shipped: `scaffold_project` in `crates/umbra-cli/src/scaffold.rs` rewritten from a hello-world skeleton (~30-line `main.rs`, 3 templates) to a complete blog-style demo. `umbra startproject myapp` now produces: `Cargo.toml` with all built-in plugins listed (umbra-auth, umbra-sessions, umbra-admin, umbra-rest, umbra-openapi); `src/main.rs` (~120 lines) declaring a `Post` model with `ForeignKey<AuthUser>`, registering all 5 plugins including `RestPlugin` with `ResourceConfig::new("post").enable_filters()`, three routes (`/` public, `/api/posts` JSON, `/dashboard` gated with `login_required_html("/login")`), a `LoggedIn<AuthUser>` extractor in the dashboard handler, an `umbra::transaction` wrapping the ORM fetch, and `auto_migrate()` on boot; `.env` with working `DATABASE_URL`/`BIND_ADDR`/`SECRET_KEY`; `README.md` with a tour table and run instructions; `templates/base.html` with Tailwind CDN; `templates/home.html`; `templates/dashboard.html`; `templates/404.html`; `templates/500.html`. Total files: 12 (was 8). 4 new tests in `crates/umbra-cli/tests/scaffold.rs` (12 total): file set, Cargo.toml plugin refs, main.rs plugin markers, Tailwind CDN, name substitution. Docs: `documentation/docs/v0.0.1/cli/startproject.mdx` rewritten — "what you get" list replaced with a surface table, new "Tour of the scaffold" section walking through models/app-wiring/handlers/templates/.env. Also fixed pre-existing `AdminPlugin` struct/unit typo in `plugins/umbra-admin/tests/integration.rs` (`.plugin(AdminPlugin)` → `.plugin(AdminPlugin::default())`) that was blocking `cargo clippy --all-targets`.
21. [x] DRF views, ie ApiView, has a way of extending and adding custom actions. Can we be able to have the same ie in the same viewset, I can have a method decorated by `@action` that is a custom action that is not part of the standard CRUD operations?
       — Shipped: `ResourceConfig::action(name, method, scope, handler)` is the umbra-rest equivalent of DRF's `@action`. `ActionScope::Collection` mounts at `/api/<table>/<name>/`; `ActionScope::Detail` mounts at `/api/<table>/<id>/<name>/`. The handler is any `async Fn(ActionContext) -> Result<Value, ActionError>`. The `ActionContext` carries `table`, `name`, `pk` (detail-scope), `identity`, `body`, and `query`. `ActionError` variants (`BadInput`, `NotFound`, `Unauthenticated`, `Forbidden`, `Internal`) map to the existing JSON error envelope. The Permission gate runs with `Action::Custom(name)`, which `permission.rs` now models alongside the five CRUD variants; existing impls (`AllowAny`, `IsAuthenticated`, `IsStaff`, `ReadOnly`) handle Custom via the trait's `&Action` signature without further changes. The trait method changed from `check(&self, Action, ...)` to `check(&self, &Action, ...)` because `Custom(String)` makes `Action` no longer Copy — this is a public-API breaking change for downstream Permission impls but worth the trade. 6 new tests in `plugins/umbra-rest/tests/actions.rs` + 1 gated test in `actions_gated.rs`. Docs: new "Custom actions (`@action`)" section in `documentation/docs/v0.0.1/plugins/rest.mdx`.
22. [x] How do we do templates discovery? ie a template in app1 might be referenced in a view in app2 ie referencing base.html in different apps/plugins. But remember, we should not override templates in the same app. (http://localhost:5173/docs/v0.0.1/templates/rendering-html - There is a deffered section here for reference, after fix update the doc)
       — Shipped: new `Plugin::templates_dirs(&self) -> Vec<PathBuf>` hook (default empty). `App::build()` collects the app's own `templates_dir` plus every plugin's `templates_dirs()` into one ordered search list. The minijinja loader walks it in registration order with **first-match-wins**: a plugin can reference another plugin's `base.html` via `{% extends "base.html" %}` and resolve. Same-name collisions across plugins emit a `tracing::warn!` at boot and the first registration wins (matches Django's `APP_DIRS` loader). 2 new tests in `tests/template_discovery.rs`. Docs: deferred note removed from `documentation/docs/v0.0.1/templates/rendering-html.mdx`; new "Cross-plugin templates" section explains the search order and collision policy. Deferred: namespaced lookups (`admin:base.html`), hot reload via `minijinja-autoreload`, `Plugin::on_ready` custom filter registration.
23. [x] Update the docs landing page to have complete SEO as to why this is the best Rust web framework for developing complete web applications with APIs and some features. Remove the term "Django Shape" from the landing page. Replace it with something better that reflects the actual capabilities of the framework.
       — Shipped: `documentation/src/routes/+page.svelte` rewritten. "Django shape. Rust spine." replaced with "Ship the whole app. In Rust." — capability-led, not analogy-led. Hero subtitle now names the actual surface area inline: managed migrations, REST API, admin UI, auth + sessions, background tasks, email. Feature grid expanded from 3 cards (Plugins / Migrations / Safety) to 6 (ORM + migrations, REST + DRF-shape ResourceConfig + @action, Admin UI, Auth + sessions + security defaults, Tasks + email, Plugin architecture). New "Why Umbra" section frames the framework against the "here's a router" baseline most Rust frameworks stop at. SEO: `<title>` + `<meta description>` + `<meta keywords>` (Rust web framework, Rust ORM, Rust admin panel, etc.), OpenGraph + Twitter Card tags, `<link rel="canonical">`, and a JSON-LD `SoftwareApplication` structured-data blob. `specra.config.json` description also rewritten away from "Django-shape." svelte-check + vite SSR/browser bundles transform clean for the page; the build's mdx-cache failure is a pre-existing vendor issue in node_modules/specra unrelated to this edit.
24. [x] How do we handle cases where a user deleted some migrations but in the db there are migrations. We need a strong migration flow and logic
       — Shipped: four-state drift model in `crates/umbra-core/src/migrate.rs`. `detect_drift(plugin, applied_set, plugin_dir)` returns `Vec<MigrationEntry>` where each entry carries one of `Applied`, `Pending`, `AppliedButMissing` (critical: file gone, tracking row present), or `OutOfOrder` (file restored after a later migration was applied). `DriftReport` aggregates entries with `has_critical_drift()` / `missing_on_disk()` accessors. `run_checked_in(dir, allow_drift)` wraps the existing apply loop: errors with `MigrateError::DriftDetected { missing }` when drift is critical and `allow_drift = false`; logs per-missing-file warnings and proceeds when `allow_drift = true`. `show_in` upgraded to the four-state `[X]` / `[ ]` / `[!]` / `[?]` display. New CLI surfaces on `umbra migrate`: `--fake <plugin/name>` (mark one migration applied without SQL via `fake_apply_in`), `--fake-initial` (per-plugin: if 0001 tables exist, fake-apply — SQLite path checks `sqlite_master`, Postgres path checks `information_schema.tables`), `--allow-drift`. `showmigrations` now prints the pending count at the end. `umbra-admin` type-annotation fix bundled because the pre-existing `umbra-auth` generics refactor was already in the working tree and broke the `authenticate` call-site. 9 tests in `crates/umbra-core/tests/migrate_drift.rs`: 4 pure-function tests (no pool), 5 pool-needing tests serialized with a `tokio::sync::Mutex`. Docs: `documentation/docs/v0.0.1/migrations/migration-drift.mdx` (new page: three drift states, four flags, two worked examples); `documentation/docs/v0.0.1/cli/management-commands.mdx` updated with migrate flags table and four-state showmigrations reference.
25. [x] How do we handle primary keys? when they are ints and when they are uuids, strings? By default, in django, a user does not need to specify a primary key field, it is auto-generated. In our case, we need to be able to specify a primary key field by default too which should be a bigint and then make sure its autoincrementing. For UUIDs, they should also be auto-generated. So the user does not need to specify a primary key and if they do, we help them handle the autogeneration. This means for all ints, we need to enable auto-increment feature on them
       — Shipped: `id: i64` / `id: i32` → `INTEGER PRIMARY KEY AUTOINCREMENT` (SQLite) / `BIGSERIAL PRIMARY KEY` (Postgres). `id: uuid::Uuid` → `TEXT PRIMARY KEY` (SQLite) / `UUID PRIMARY KEY` (Postgres), no auto-default at v1. `id: String` → `TEXT PRIMARY KEY`, user-supplied. `Manager::create` omits the PK column when it equals the sentinel (0 for ints, `Uuid::nil()` for UUID, `""` for String) so the DB autoassigns. The sentinel logic was already in `orm/write.rs::is_default_pk`; this gap adds schema-emission tests and documents the rule. Tests: `tests/pk_autogen.rs` (14 tests). Docs: `documentation/docs/v0.0.1/orm/column-types.mdx` PK table + `models.mdx` PK auto-generation rule table.
26. [x] In django, for view, we had this decorator called `login_required` (It has something like redirect_to or auth_page) that required the user to be logged in to access the view. Do we have the same functionality in our framework?
       — Shipped: two complementary shapes in `plugins/umbra-auth/src/login_required.rs`. (1) `LoggedIn<U: UserModel>` axum extractor — drop in a handler signature; resolves the `umbra_session` cookie into the authenticated user of type `U`, or returns the configured rejection. Generic over any `UserModel` so `LoggedIn<TenantUser>` works without adapters. (2) `LoginRequiredLayer` tower middleware — wraps a Router subtree; gates every route before the inner handler is called; injects the `LoginRequired` config into request extensions so downstream `LoggedIn<U>` extractors read it automatically (no double check). (3) `LoginRequired` config struct with two shapes: `LoginRequired::API` (401 JSON + `WWW-Authenticate: Bearer`, default) and `LoginRequired::html("/login")` (302 to `/login?next=<uri>`). `.no_next()` drops the query parameter. Convenience constructors: `login_required()` (API shape) and `login_required_html(path)` (HTML shape), both returning `LoginRequiredLayer` which implements `tower::Layer`. The layer authenticates by querying only the `session` table (user_id IS NOT NULL, expires_at > now); the extractor does the full user hydration from `U::TABLE`. umbra-auth does not depend on umbra-sessions; the SHA-256 token hash is re-implemented locally (~5 lines) to keep the dep arrow pointing the right way. 7 new tests in `plugins/umbra-auth/tests/login_required.rs`. Docs: new "Guarding views" section in `documentation/docs/v0.0.1/plugins/auth.mdx` (above "Swapping the user model"). Deferred: `permission_required(perm)` and `staff_member_required` — depend on gap 33 (groups + content-type model) which does not exist yet.
27. [x] In docs, under backends, we have only shown postgresql but no sqlite! We need to add sqlite to the list of supported database backends. The `postgres` doc is also out of date!
       — Shipped: new `documentation/docs/v0.0.1/backends/sqlite.mdx` covers configuration (the three URL forms `:memory:` / `sqlite://path?mode=rwc` / `sqlite:`), when-to-reach-for-SQLite framing, migrations (`AUTOINCREMENT` PKs + table-recreation for nullability changes), querying with `.on(&sqlite_pool)`, the SQLite inspectdb path, the v1 limits (no native arrays / JSONB / FTS / UUID / network types / RLS), and the SqlType→column-type table. `postgres.mdx` rewritten to match current reality: removed the stale "What's not there yet" section (inspectdb-pg, array/network/JSONB, RLS plugin all shipped); added a Postgres-only field types table; documented the `_pg` QuerySet variants; added the type catalogue; pointed at `umbra-rls` and the new sqlite page from "See also". Both pages link to `orm/models.mdx`'s per-type sections (the source of truth on individual fields).
28. [x] Django has a way of doing `prefetch_related` and other helpers like `select_related`, `F`, `Q` to avoid N+1 queries. Do we have the same functionality in our framework?
       — Shipped: `F`, `FExpr`, `Q`, `select_related`. `F::col("col")` returns an `FExpr` for column references; arithmetic via `.add(n)` / `.sub(n)` / `.mul(n)` / `.div(n)` enables atomic updates (`update_expr("views", F::col("views").add(1))`). `FColExt` trait on `IntCol`, `ForeignKeyCol`, `StrCol` adds `.eq_f(FExpr)` / `.ne_f(FExpr)` for column-vs-column WHERE. `Q::and(a, b)`, `Q::or(a, b)`, `Q::not(p)` compose predicates explicitly; the existing `&`/`|` operators are unchanged. `select_related("field")` and `select_related_many(&[...])` on `QuerySet` eagerly load FK fields via a batch `SELECT ... WHERE id IN (...)` after the main query; `ForeignKey<T>.resolved()` returns `Some(&T)` after loading. `#[derive(Model)]` now emits `HydrateRelated` with `fk_id_for` and `hydrate_fk` methods used by the select_related machinery. `prefetch_related` (reverse FKs, M2M, nested traversal `"author__manager"`) is deferred — documented in relationships.mdx. Tests: `tests/f_expressions.rs` (8), `tests/q_objects.rs` (8), `tests/select_related.rs` (9). Docs: `documentation/docs/v0.0.1/orm/expressions.mdx` (new), `documentation/docs/v0.0.1/orm/relationships.mdx` (updated with select_related + prefetch_related deferred section).
29. [x] The rest framework is also extended by some packages like `django-filter`, `django-rest-swagger`, `django-rest-framework-simplejwt` that provide additional functionality. Do we have the same functionality in our framework? The target is the django-filter like plugin for our rest framework. So first question is, can a plugin be extended? Next question is, can a plugin be used next to the other? Can a plugin be used in a view with a single registration point? And then, how can we create our own `filter` plugin for complex filtering and data modeling?
       — Shipped: django-filter-style query-string filtering built into `umbra-rest` as a first-class feature. `ResourceConfig::enable_filters()` opts a resource in; disabled by default so existing resources see no behaviour change. New module `plugins/umbra-rest/src/filtering.rs` contains `parse_filters()` (parser entry point), `FilterClause` (WHERE fragment + ordered bindings), and `FilterValue` (per-type binding enum). Lookup grammar: `eq` (default), `ne`, `gte`, `lte`, `gt`, `lt` (numeric/date/datetime only), `in` (comma-separated, expands to `IN (?,?,?)`), `contains` (`LIKE %v%`), `icontains` (`UPPER(col) LIKE UPPER(%v%)`), `startswith` (`LIKE v%`), `isnull` (nullable columns only, value = "true"/"false"). All lookups are type-validated at parse time: range ops on text fields, string-pattern ops on non-text fields, and `isnull` on NOT NULL columns all return HTTP 400 with a clear "X is not available for field Y of type Z" message. Empty values (`?published=`) also 400. Filters are applied before pagination so the paginated `count` reflects the filtered result set. The list handler's `fetch_rows` and the new `count_rows_filtered` both accept a `&FilterClause` and bind the values via `bind_filter_values`. `RestPlugin` gains a `filters_enabled: HashSet<String>` and the `resource()` merge path propagates the flag. 9 new tests in `plugins/umbra-rest/tests/filtering.rs`. Docs: new "Filtering" section in `documentation/docs/v0.0.1/plugins/rest.mdx`. Deferred: granular per-field/per-lookup allowlist (`filter_fields`/`filter_lookups`), OR-composition via repeated keys, relation traversal (`author__name__icontains`), separate `umbra-filter` crate.
30. [x] Do we create tables based on plugin namespace ie `blogs_Post` `main_Category`? I ask this since it will be essential for the migrations to tell whether a table was moved to a different namespace and it just needs to be renamed instead of creating a new table and deleting the old table. This goes hand in hand with the cli makemigrations command in which we have to quee and show the user or ask the user some questions like did you rename/move the table? So the makemigrations command needs to be strong. So migrations can have enough data about the changes safely.
       — Shipped: two cooperating features, both opt-in and backward-compatible.
       (1) **Plugin-namespaced table names.** `#[umbra(plugin = "blog")]` on a model emits `const TABLE: &'static str = "blog_post"` instead of the bare `"post"`. Precedence: explicit `table = "..."` always wins over a `plugin = "..."` prefix, which wins over the bare snake_case default. Built-in plugins (auth, sessions, admin, tasks) are NOT changed — their table names are stable DB identifiers. `Model::NAME` (the Rust struct name) is unaffected; it remains the rename-detection anchor.
       (2) **Rename detection in `makemigrations`.** `diff()` now runs two passes before falling back to drop+create. First pass: if a model's `Model::NAME` is unchanged but its `Model::TABLE` changed (the common case when the `plugin` attribute is added to an existing model), emit `Operation::RenameTable { from, to }` — zero DropTable + CreateTable. Second pass: among still-unpaired drops and creates, if the column shapes are bit-identical (sorted (name, ty, nullable, fk_target) tuples), emit `RenameTable` plus a stderr warning naming both sides so the developer can verify. A stdout message is printed on every first-pass rename; the stderr warning fires only on second-pass heuristic matches. `RenameTable` DDL: `ALTER TABLE "from" RENAME TO "to"` on both SQLite and Postgres (rendered via sea-query's `Table::rename()`). `suffix_for` produces `rename_<from>_to_<to>` filenames. Tracking table rows are keyed by `(plugin, migration_name)` and are not affected by table renames inside the migration.
       Files touched: `crates/umbra-macros/src/lib.rs` (parse `plugin` key, compute namespaced table name), `crates/umbra-core/src/migrate.rs` (add `RenameTable` variant, two-pass `diff`, `suffix_for`, SQLite + Postgres renderers), `crates/umbra-core/src/inspect.rs` (exhaustive match arm), `crates/umbra-core/tests/migrate.rs` (exhaustive match arm), `documentation/docs/v0.0.1/orm/models.mdx` (new "Plugin-namespaced tables" subsection), `documentation/docs/v0.0.1/migrations/managed-migrations.mdx` (new "Rename detection" section). New test files: `crates/umbra-core/tests/namespacing.rs` (6 tests), `crates/umbra-core/tests/rename_detection.rs` (6 tests).
       Deferred: interactive CLI prompts asking the user to confirm ambiguous renames (the second-pass warning is the v1 substitute), per-plugin `table_prefix()` hook on the `Plugin` trait (the model-level attribute is simpler and doesn't force a shared prefix on every model in a plugin), cross-snapshot rename-tracking for squashed migrations.
31. [x] Custom User Model implementation and setting the custom user model in settings
       — Shipped: `UserModel` trait in `plugins/umbra-auth/src/lib.rs`. Required methods: `id() -> i64`, `username() -> &str`, `password_hash() -> &str`, `set_password_hash(String)`. Default-impl optional flags: `is_active()` (true), `is_staff()` (false), `is_superuser()` (false). `AuthUser` implements the trait unchanged. `AuthPlugin<U: UserModel = AuthUser>` is now generic with `PhantomData<U>`; `AuthPlugin::default()` and all existing code continue to work without annotation. `authenticate<U>` and `set_password<U>` are generic over `U: UserModel`; creation helpers (`create_user`, `create_user_with_flags`, `create_superuser`) remain `AuthUser`-specific because constructing an arbitrary `U` from a fixed column set is not possible without a user-supplied factory. `AuthPlugin.user_model_name: Option<String>` is an informational-only field for admin/OpenAPI nav; dispatch is entirely through the type parameter, not a string setting. `umbra-sessions` and `umbra-admin` reference `AuthUser` concretely - documented in `documentation/docs/v0.0.1/plugins/auth.mdx` under "What requires manual adjustment" rather than refactored, because making sessions/admin generic is a separate follow-on. 9 new tests in `plugins/umbra-auth/tests/custom_user.rs`; 11 existing tests in `tests/integration.rs` pass unchanged after trivial `AuthPlugin` -> `AuthPlugin::<AuthUser>::default()` fixes. All downstream plugin test files (`umbra-sessions`, `umbra-admin`, `umbra-openapi`) updated similarly. See `documentation/docs/v0.0.1/plugins/auth.mdx`.
32. [x] There is no dedicated docs page for template helpers like humanize etc
       — Shipped: new `documentation/docs/v0.0.1/templates/helpers.mdx`. Covers 40+ minijinja built-ins across six sections (strings, numbers, collections, default/type, global functions, loop helpers) with `{{ value | filter }}` examples. Calls out that umbra registers zero custom filters today, names django-style helpers absent at v1 (`naturalday`, `intcomma`, etc.) with the recommended workaround (compute in the handler, pass as context strings), and points at the `Plugin::on_ready` extension hook for future filter registration. Links the upstream minijinja filter docs for the full reference catalogue.
33. [x] We don't have proper role management, hence contenttypes and user groups. Django has groups which automatically handles actions. We need to fix this up, we can create groups too, we can have that model level roles and at the same time we need this on top of RLS. If a user wants to use RLS they can do that otherwise they can use groups and such roles. There should be a complete middleware for this for db access per logged in user etc. Related to #31. We will need to map roles to the active User model implementation whether default or custom User model. Also, we shall build a UI in the admin to do this and update docs on how to create roles per model for the User etc. This means, we don't control access via is_superuser perse fully but rather with proper RBAC (Proper permissions). We need a `hasPermission` method after this for the user
       — Shipped: new `plugins/umbra-permissions/` crate providing the Django-style RBAC data model + query layer. Five models in `models.rs`: `ContentType` (id, app_label, model — one row per registered Model), `Permission` (id, content_type_id FK, codename, name), `Group` (id, name), `GroupPermission` (id, group_id FK, permission_id FK), `UserGroup` (id, user_id, group_id FK), `UserPermission` (id, user_id, permission_id FK). The query layer in `perm.rs` exposes `has_perm(user_id, "<app>.<codename>")` and `user_perms(user_id) -> HashSet<String>` as free async functions taking `user_id: i64` rather than a generic `UserModel` instance — keeps the dependency arrow `umbra-permissions → umbra-auth` clean (no cycle). `has_perm` returns true when (a) the user is a superuser, OR (b) a direct `user_permission` row matches, OR (c) the user is in a group that has the permission — single JOIN, not three queries. Standard permissions (`add_<model>`, `change_<model>`, `delete_<model>`, `view_<model>`) auto-create at boot via `PermissionsPlugin::on_ready` for every registered model. 7 new tests in `tests/integration.rs`. Docs: `documentation/docs/v0.0.1/plugins/permissions.mdx` (new). Deferred: admin UI for managing groups/perms (depends on gap 19's Tailwind admin), `permission_required(perm)` layer from gap 26 (now unblocked by this gap — a follow-up), RLS predicate injection (umbra-rls's responsibility), ContentType auto-population is best-effort and may miss models registered late.
34. [x] Related to #32 - Add the third column in the tables for `usage`
       — Shipped: third "Usage" column added to all five filter-listing tables in `documentation/docs/v0.0.1/templates/helpers.mdx`: strings (13 rows), numbers (4 rows), collections (16 rows), default/type (5 rows), and global functions (4 rows) — 42 rows total. All snippets use canonical minijinja invocation form (`{{ value | filter }}` for pipe filters; `{% for i in range(5) %}` / `{% set ... %}` for global functions that are naturally used in tag context rather than expression context).
35. [x] We should catch internal server errors and allow the developer to use/display the error if they want to and also we shall create default 404 and 500 error pages using tailwindcss(Related to #19)
       — Shipped: three cooperating features, all additive on `App::builder()`.
       (1) **`on_server_error` hook.** `App::builder().on_server_error(|err, path| { ... })` registers a `Fn(&str, &str)` closure that fires before the 500 template is rendered. Used for Sentry, Datadog, custom logging. Cannot change the response.
       (2) **Panic catching is now always-on.** Previously the `CatchPanicLayer` was only installed when `.server_error_template(...)` was explicitly set. Now it installs whenever default pages are enabled (the default) OR a hook is registered. Panicking handlers produce a graceful 500 instead of aborting the connection even with no explicit configuration.
       (3) **Default Tailwind 404/500 templates.** Two HTML pages embedded via `include_str!` in `crates/umbra-core/src/templates/defaults/`. Both use inline Tailwind utility classes and include a `<style>` reset so they degrade to functional unstyled HTML when Tailwind isn't loaded. The 500 page renders an expandable dev-mode block (error message, cause chain, request path) when `settings.environment == Dev`; the block is empty in `Prod`/`Test`. Templates registered into minijinja at `__umbra__/default_404.html` and `__umbra__/default_500.html` in `templates::init` before any disk directories are scanned. Opt out via `App::builder().disable_default_error_pages()`.
       Cross-reference gap 19 (Tailwind, deferred): this gap uses inline Tailwind utility classes without requiring a Tailwind build pipeline — the classes work with any stylesheet that includes them (Tailwind CDN, pre-compiled CSS), and the fallback `<style>` reset makes pages usable without any stylesheet.
       Follow-up `4798f22`: handler-Err 500s also route through the template now (was panic-only at first ship). New response-rendering middleware (`render_500_middleware`) intercepts any non-HTML 500 response and re-renders through the configured 500 template, also firing `on_server_error`. HTML 500s (already rendered by `CatchPanicLayer` or by the handler itself) pass through unmodified.
       New files: `crates/umbra-core/src/templates/defaults/default_404.html`, `default_500.html`, `crates/umbra-core/tests/error_handling.rs` (16 tests). Modified: `src/errors.rs` (hook, defaults, dev context, `render_500_middleware`), `src/app.rs` (two new builder methods, panic + response middleware wiring), `src/templates.rs` (register defaults at init time), `tests/error_pages.rs` (updated for 2-arg `server_error_panic_handler`), `documentation/docs/v0.0.1/web/error-pages.mdx`.
36. [x] Related to #26, update the docs on how to use this structure for auth gating! And its like you forgot to do so for every feature implementation yet its in claude.md!
       — Shipped: new `documentation/docs/v0.0.1/web/auth-gating.mdx` (sidebar position 3 under Web) as the discoverable entry point for the `LoggedIn<U>` extractor + `LoginRequiredLayer` story. Cross-links into the full reference in `plugins/auth.mdx#guarding-views`. Fixed stale section in `documentation/docs/v0.0.1/auth/users-and-passwords.mdx` that still listed the `Auth<U>` / `#[login_required]` shape as deferred and never updated when gap 26 shipped — replaced with a "Guarding views" section pointing at the new web/ page, and rewrote the "What ships now vs deferred" list to reflect actual current state (custom user model shipped, extractor + layer shipped, login/logout/reset shipped via sessions + email, RBAC/groups still deferred → gap 33). The full reference under `plugins/auth.mdx` was already there from gap 26 (lines 116-235) — this gap closes the discoverability and stale-cross-reference issues. Companion commit `ea4dbe1` swept all 21 broken `/v0.0.1/...` MDX cross-references across 8 files to `/docs/v0.0.1/...` (SvelteKit only routes docs under `/docs/...`).
37. [x] In django, if you have a `Post` model, and it has `author` which is an fk to User model, when you get the post, in the templates, you can call `post.author.first_name` etc. In other words, it does prefetch the related and linked models on the fly deeply! Do we have that capability?
       — Shipped as a side-effect of gap 28's `select_related`. `ForeignKey<T>` gains a `resolved: Option<Box<T>>` slot; when populated by `select_related("author")`, `serde::Serialize` emits the full `T` object instead of the bare integer. Template rendering via minijinja therefore resolves `{{ post.author.username }}` correctly — the template engine reads `ctx["author"]["username"]` from the serialised JSON context. No template-engine changes were needed: the behaviour falls out of the serialisation contract. Backward compat is preserved: without `select_related`, `ForeignKey<T>` still serialises as a bare integer. See `tests/select_related.rs::select_related_serialises_as_full_object` for the round-trip verification and `documentation/docs/v0.0.1/orm/relationships.mdx` for the user-facing docs.
38. [x] How do we handle atomic txs here? We need this since its veryvery critical and nice to have
       — Shipped: three cooperating layers in the ORM/db module, all additive.
       (1) **`Transaction` wrapper type.** `umbra::db::Transaction` wraps either a `sqlx::Transaction<'static, Sqlite>` or a `sqlx::Transaction<'static, Postgres>` in a `TransactionInner` enum. Exposes `as_sqlite_mut()`, `as_pg_mut()`, `backend_name()`, `commit()`, and `rollback()`.
       (2) **`transaction` / `transaction_sqlite` / `transaction_pg` closure helpers.** Each takes a `for<'a> FnOnce(&'a mut Transaction) -> TxFuture<'a, T, E>` closure (a `Pin<Box<dyn Future>>`). Calls `begin`, runs the closure, commits on `Ok`, rolls back best-effort on `Err`, and returns the closure's result. `begin` / `begin_sqlite` / `begin_pg` are the manual-control variants. `TxFuture<'a, T, E>` is the named type alias for the HRTB-compatible pinned future that stable Rust requires for closures capturing `&mut Transaction`.
       (3) **ORM integration.** `QuerySet::on_tx(&mut Transaction) -> QuerySetTx` attaches a transaction to a query chain; all terminals on `QuerySetTx` (`fetch`, `first`, `count`, `exists`, `get`, `delete`, `update_values`, `create`) execute inside the open transaction. `Manager::create_in_tx(instance, &mut tx)` and `Manager::bulk_create_in_tx(instances, &mut tx)` are the Manager-level entry points. Existing `.on(&pool)` and ambient-pool calls are unchanged.
       Facade: `umbra::db::{Transaction, TxFuture, begin, begin_sqlite, begin_pg, transaction, transaction_sqlite, transaction_pg}` + top-level `umbra::transaction(...)` re-export. `umbra::orm::QuerySetTx` also re-exported. Tests: `crates/umbra-core/tests/transactions.rs` (8 tests). Docs: `documentation/docs/v0.0.1/orm/transactions.mdx`. Deferred: savepoints / nested transactions, per-transaction isolation-level control, two-phase commit.
39. [x] The startapp command should check if there is an existing app to avoid app confusions (Check if it is implemented that else implement)
       — Shipped: `scaffold_app` already rejected `plugins/<name>/` collisions (commit 95f0709's `AlreadyExists` branch) but had no check for the harder case — picking a name that collides with a built-in umbra plugin like `auth` or `sessions`. The user would scaffold an empty plugin and only discover the conflict at `App::build()` time when route mounts or migration tables collided. Now `scaffold_app` checks the requested name against the `RESERVED_PLUGIN_NAMES` const (`admin`, `app`, `auth`, `cache`, `email`, `openapi`, `permissions`, `rest`, `rls`, `security`, `sessions`, `signals`, `static`, `tasks`) before creating any files. Reserved-name attempts return `ScaffoldError::ReservedName(name)` with a message that lists the full reserved set so the user can pick again. Hyphen/underscore normalised (`umbra-auth` and `umbra_auth` both reject). The `AlreadyExists` message also reworded to say "an app already exists at ..." rather than the generic "target ..." so the diagnostic clearly names what went wrong. 4 new tests in `crates/umbra-cli/src/scaffold.rs::tests` (reserved-name rejection per name, error message lists the set, AlreadyExists wording). Docs: new "Names umbra refuses to scaffold" subsection in `documentation/docs/v0.0.1/cli/startproject.mdx`.
40. [x] I haven't seen any places for signals! Also this goes hand in hand with the Tasks plugin. We need to finalize that to have the #[task] macro. Remember for signals, I might want to run tasks behind the scenes ie after user signup etc. Another thing I think covered from before is the settings/env variables, I should be able to get those anywhere in the app easily. So document that well how to use env vars and settings. Update this doc too afterwards http://localhost:5173/docs/v0.0.1/plugins/tasks
       — Shipped: three cooperating pieces.
       (1) **`#[task]` attribute macro** in `crates/umbra-macros/src/lib.rs`. The user writes `#[task] async fn send_welcome(payload: WelcomeEmailPayload) -> Result<(), String>`; the macro preserves the original function and emits a sibling `pub fn register_send_welcome()` that wraps the body with `serde_json::from_str` + JSON-payload deserialisation and calls `umbra_tasks::register_handler` against the function's identifier. `#[task(name = "blog.send_welcome")]` overrides the registration name. Constraints enforced via `compile_error!`: must be `async fn`, exactly one parameter, return `Result<(), String>`. 9 new tests across `crates/umbra-macros/tests/task_macro.rs` (6: 2 inline + 4 trybuild rejection snapshots) and `plugins/umbra-tasks/tests/macro_integration.rs` (3: happy path, name override, bad-payload deserialise error). Re-exported as `umbra::task` through the facade. The macro references `::umbra_tasks::register_handler` and `::umbra_tasks::_serde_json` directly — `umbra` facade cannot depend on `umbra-tasks` (cycle), but every user crate that applies `#[umbra::task]` already has `umbra-tasks` in deps because `TasksPlugin` requires it.
       (2) **Signals docs page** at `documentation/docs/v0.0.1/plugins/signals.mdx` (sidebar position 12). Covers plugin registration, `subscribe` / `subscribe_async` / `emit` API, the durable-signal pattern (signal handler enqueues a task so the work survives the process crash), payload-shape conventions, the built-in signals shipped at v0.0.1 (`post_login`, `post_logout`, `post_signup`), and what's deferred (typed events via enum, cross-process broker, persistence). Cross-links to `tasks.mdx` and `getting-started/settings-and-env.mdx`.
       (3) **Settings + env-vars docs page** at `documentation/docs/v0.0.1/getting-started/settings-and-env.mdx` (sidebar position 2). Covers the three-source loading precedence (defaults → `umbra.toml` → `UMBRA_`-prefixed env vars), every known field with its env-var name and default, the canonical `umbra.toml`, `umbra::settings::get()` / `get_opt()` access from any context (handler, on_ready, `#[task]`, signal handler), the `extra` field for app-defined keys with `extra_str()` and nested-table access patterns, the explicit "umbra does not parse `.env` files" call-out with the recommended `set -a; source .env` workaround, and the `--addr`-flag precedence rule.
       (4) **`tasks.mdx` rewritten** to put `#[task]` as the recommended path; the stringly-typed `register_handler("name", closure)` form demoted to "advanced / dynamic registration" use cases. Added "Combining with signals" callout pointing at the durable-fire-and-forget pattern. The cross-link between signals → tasks → settings is now bidirectional.
41. [x] The commands are not well explained, even with basic usage. http://localhost:5173/docs/v0.0.1/cli/management-commands, ie there is serve command but I have no idea how to pass in the port or even `0.0.0.0:8080`
       — Shipped: `documentation/docs/v0.0.1/cli/management-commands.mdx` rewritten so every subcommand leads with copy-pasteable invocations before listing flags. `serve` section shows four common cases (default 127.0.0.1:3000, custom localhost port, 0.0.0.0:8080 for production / Docker / LAN, env-var equivalent) plus precedence rules (`--addr` > `UMBRA_BIND_ADDR` > `umbra.toml` > default). New dedicated sections for `makemigrations`, `dev`, `dumpdata` / `loaddata`, `createsuperuser`, and `tasks-worker` — each starts with a real command, then a flags table with defaults. `dev` covers the watch-path syntax explicitly: each path needs its own `-w` flag, comma-separated values are NOT supported (would be read as one path literally named "src/,templates/"). Cross-references the in-process template hot-reload so users know they don't need `dev` for `.html` edits. The old "Plugin-contributed subcommands" table collapsed to a pointer at the dedicated sections.
42. [x] No doc illustrating use of umbra.toml or .env anywhere. This is part of env and settings module
       — Shipped in commit `1cc47fc` as part of gap 40's `documentation/docs/v0.0.1/getting-started/settings-and-env.mdx`. The page has dedicated sections "umbra.toml" (with a copy-pasteable example covering database_url, bind_addr, secret_key, environment, log_level, allowed_hosts, and a `[databases]` named-pool block) and "A `.env` file is not loaded automatically" (with the explicit non-support callout and the recommended `set -a; source .env; set +a` workaround). Plus a precedence table showing umbra.toml is layered between built-in defaults and UMBRA_-prefixed env vars. The sibling section "Custom settings (the `extra` field)" shows both env-var and `umbra.toml` paths for app-defined keys (`UMBRA_OPENAI_API_KEY` vs `[external.openai]`). No new docs work needed; this gap was already closed by the time it was filed.
44. [x] I forgot - When creating models, is it possible we define display name and icon (ie "users" for a users table)? Or can the Admin Plugin allow for this where you can rename a given model (Like display name) and dashboard icon?
       — Shipped: `#[umbra(display = "Users", icon = "users")]` attributes on `#[derive(Model)]`. Two new associated consts on the `Model` trait: `DISPLAY` (defaults to `NAME`) and `ICON` (defaults to `"database"`). The derive macro picks up `display = "..."` and `icon = "..."` keys alongside the existing `table` and `plugin` keys and emits the two consts. `ModelMeta` gains `display: String` and `icon: String` fields populated from `Model::DISPLAY` / `Model::ICON`; `#[serde(default)]` on both fields keeps existing JSON snapshots forward-compatible. `AdminRegistry::apps()` rewired to synthesise a default `AdminRegistration` for every model in the global registry (auto-discovery — no explicit `.register(...)` call needed); explicit registrations overlay and win. `AdminModel::label()` / `AdminModel::icon()` on an explicit registration override the struct-level defaults. Ordering: named plugins alphabetically first, implicit `"app"` bucket last. 4 new tests in `crates/umbra-macros/tests/display_icon.rs` (explicit attrs, defaults, display-only, icon-only). 4 new tests appended to `plugins/umbra-admin/tests/phase1_shell.rs` (explicit label override, explicit icon, auto-discovery, theme toggle onclick). Updated `documentation/docs/v0.0.1/plugins/admin.mdx` with a "Model display name and sidebar icon" section.
45. [x] The admin templates, both base.html and login.html have the same general layout ie tailwind config, either choose one, create a helper js function in a js asset or simply create wrapper.html..
       — Shipped: new `templates/wrapper.html` holds `<!DOCTYPE>`, `<html>`, `<head>` with Tailwind config + Lucide + Inter font + design-token block + theme persistence IIFE (including `toggleTheme`, `applyTheme`, `sidebarFilter`, `toggleUserMenu` + Lucide `createIcons` on DOMContentLoaded). `base.html` extends `wrapper.html` via `{% extends "admin/wrapper.html" %}` and overrides `{% block body %}` with the sidebar + topbar + `{% block content %}` slot. `login.html` extends `wrapper.html` directly (no chrome) and overrides `{% block body %}` with only the centered login card. Theme toggle wiring also fixed (gap 1): `onclick="umbra.toggleTheme()"` was already on the button but the IIFE wasn't exposing `toggleTheme` on the `umbra` object — now it is, from `wrapper.html`, so both chrome pages and the login page honour the persisted preference on load. `wrapper.html` registered in the template engine's `ENGINE` OnceLock alongside the existing templates. 
46. [x] Django has a way of defining the string method of a column. Instead of displaying all the columns, we can display a value from such a method ie id by default and then the actions column instead of all columns in a given model as default. We can use another macro like `#[umbra(string = true, max_length=50)]. So behind the scenes we pick the first field with this from the model and limit characters to this. Only applicable to string col types only. But maybe you have implemented it, I don't know, if so, update the docs under models else do some implementation around it and update the docs
       — Shipped in commit `c533c99`. `parse_umbra_field_attr` in `umbra-macros` now accepts `string` (bareword or `= true`) and `max_length = N`. Two new fields on `FieldSpec` and `migrate::Column` (`is_string_repr: bool`, `max_length: u32`) carry the values through to the registry and snapshots. `umbra-admin/src/discovery.rs::default_list_display(meta)` returns `[pk, first string_repr field]` (falls back to `[pk, first Text column]`, then to all columns) when no `list_display` is configured. `rows::apply_max_length_truncation` walks each fetched row and truncates cells per their column's cap — UTF-8 char count, appends `…` so the cut is visible. Docs added in `documentation/docs/v0.0.1/orm/models.mdx` under "Display hints (`string`, `max_length`)".
47. [x] Complete Admin Rename ie site title, description, default colorscheme (primary color and theming)
       — Shipped in commit `efd3436`. Three chainable builders on `AdminPlugin`: `.site_title("...")`, `.site_description("...")`, `.brand_color("#xxx")`. New `plugins/umbra-admin/src/branding.rs` owns the struct + a `OnceLock<AdminBranding>` that `Plugin::routes()` seals once. The template engine reads the sealed value on first init and registers `site_title`, `site_description`, `brand_color` as Jinja globals. Templates touched: `wrapper.html` (title + meta + brand-color `<style>` that reassigns `--primary` across `:root` and `.dark`), `base.html` (sidebar header), and every per-page title block in `changelist/detail/form/dashboard/list/index/login`. Handlers need no per-render context changes — the values are always in scope.
48. [x] Plugin of `#umbra(plugin = "app")` needs further details ie I tried to change the plugin the `Article` model and I started encountering errors. Plugin `app` does not exist, but you realize the UI showed extra table called `Articles` under `APP` namespace
       — Shipped in commit `1475c43`. `"app"` is the implicit namespace for every user-binary model registered via `App::builder().model::<T>()`, so the macro now skips the table prefix when `plugin == "app"`. Previously, tagging `Article` with `#[umbra(plugin = "app")]` renamed the table from `article` to `app_article` and triggered a destructive rename migration against the existing database (the user had `0002_rename_article_to_app_article.json` and `0003_rename_app_article_to_article.json` artifacts as evidence). The model still groups under the `app` sidebar bucket — that's governed by the runtime registry, not the table name — so the user-visible label they wanted is unchanged.
50. [x] http://localhost:5173/docs/v0.0.1/plugins/permissions - We need to finish this, the admin now is fully tailwind and update the docs
       — Shipped three pieces. (1) **`permission_required(perm)` axum layer** in `plugins/umbra-permissions/src/middleware.rs`. Resolves the `umbra_session` cookie to a user id via the new public `umbra_auth::current_session_user_id(&headers)` helper, then calls `has_perm`. `PermissionRequired::api(perm)` returns 401 JSON on unauthenticated, 403 JSON on lacks-perm; `PermissionRequired::html(perm, login_url)` returns 302 to `login_url?next=<uri>` on unauthenticated. Superuser bypass via a tolerant `SELECT is_superuser FROM auth_user WHERE id = ?` that returns false (no bypass) when the column is absent, so custom user models opt out by simply not having the column. (2) **umbra-permissions → umbra-auth dep escalation**: dev-dep promoted to runtime so `permission_required` can call into the session helper. This is the correct arrow direction (permissions sit above auth in the layering). Pulls `axum`, `axum-core`, `http`, `tower` as runtime deps; `tokio` gains `rt-multi-thread` to fix a `block_in_place` regression on the existing `on_ready`. (3) **Polish + admin discoverability**: all six permission models (`ContentType`, `Permission`, `Group`, `GroupPermission`, `UserGroup`, `UserPermission`) gained `display = "..."` and `icon = "..."` attributes so they appear in the admin sidebar with proper labels under the `permissions` plugin bucket — Plugin::models() auto-discovery (from gap 13/44) means no explicit admin registration is needed. 6 new tests in `plugins/umbra-permissions/tests/middleware.rs`. Docs: `documentation/docs/v0.0.1/plugins/permissions.mdx` rewritten — removed the "deferred admin UI" note, added a new "Gating routes with `permission_required`" section with API + HTML usage examples, superuser-bypass section, and composition story with `login_required`. The "deferred admin UI" callout is replaced by an honest "the six tables are visible as standard models; a Django-style permission-matrix widget lands in a follow-up." Deferred: `Meta.permissions = [...]` syntax, object-level / row-level permissions, RLS predicate injection (umbra-rls's job).
51. [x] The admin plugin has some unimplemented stuff like inline edit, it does not appear in the frontend, the password reset. Make sure everything in the backend is fully supported on the frontend
       — Backend audit found every feature in this gap is **already shipped end-to-end** — the appearance of incompleteness was a discoverability problem, not a code gap. The actual landings: (1) **Inline cell edit**: `plugins/umbra-admin/src/handlers/inline_edit.rs` exposes `GET /admin/{table}/{id}/cell/{field}/edit` + `POST /admin/{table}/{id}/cell/{field}`; `templates/_macros/data_table.html` wires `dblclick` triggers + `field_editor.html` renders the editor fragment. Opt-in via `AdminModel::inline_edit_fields(&[...])`. (2) **Password reset on edit sheet**: `change_password_handler` in `handlers/sheet.rs` at `POST /admin/{table}/{id}/change-password` (validates, argon2-hashes via `umbra_auth::hash_password`, writes a `password_field` column, logs to `admin_audit_log`, fires a success toast). The "Change password" button at `sheet.html:249` calls `umbra._openChangePasswordDialog` (defined at `wrapper.html:547`) which opens an HTMX-driven dialog. Opt-in via `AdminModel::password_field("password_hash")`. (3) **Bulk actions** + **FK async combobox** + **Choices `<select>`** + **MultiChoice chip checkboxes** — all auto-wired with no opt-in, fully visible on every model. The shipped work in this gap: derive-demo gained `.inline_edit_fields(&["title", "status"])` on Article and a new `.register("auth_user").password_field("password_hash")` so a fresh `cargo run` from the example surfaces both features in the admin UI. Docs: new "**Opt-in features (and what makes them visible)**" table at the top of `documentation/docs/v0.0.1/plugins/admin.mdx` enumerating every feature, whether it's automatic, and the exact registration line that unhides it. Per-field validation errors (single string → per-column error map) are deferred — that's a bigger handler refactor for a separate gap; today the form renders a top-level red banner with the error message.
52. [x] We have choices and now we need to close this off by having a multichoice macro, more like an M2M field
       — Shipped: `MultiChoice<E>` wrapper type in `crates/umbra-core/src/orm/multichoice.rs`. Storage = single TEXT column carrying a CSV of the underlying `ChoiceField` enum's variant strings. The macro detects the type directly (no `#[umbra(multichoice)]` marker needed — `MultiChoice<E>` is already unambiguous), pulls `<E as ChoiceField>::VALUES` / `LABELS` from the inner enum at derive time, and sets a new `is_multichoice: bool` flag on `FieldSpec` + `migrate::Column`. The DDL emitter deliberately **skips** the single-value `CHECK (col IN (...))` constraint when `is_multichoice` is true (it would reject every legal CSV); validation happens via sqlx's `Decode` path which fails fast on any unknown segment. `#[umbra(default = "tag1,tag2")]` is reused — CSV initial value used both as the SQL `DEFAULT` and as the value `ALTER TABLE ADD COLUMN` applies. The wrapper exposes the usual collection surface (`as_slice`, `into_vec`, `push`, `contains`, `IntoIterator`, `Deref<Target=[E]>`, `FromIterator`); serde serialises to a JSON array `["a","b"]` and deserialises from either an array or a CSV string (so HTML form posts round-trip). Admin: `view.rs::input_kind` returns `"multiselect"` when `is_multichoice` is true; `templates/_macros/field_editor.html` and `sheet.html` both render a checkbox-chip group bound to a hidden CSV input via the new `initMultiChoicePickers` JS in `templates/wrapper.html` (idempotent, runs on `htmx:afterSwap`). The macro emits a `StrCol` predicate constant for filter chains. Tests: `crates/umbra-core/src/orm/multichoice.rs` (8 unit tests covering CSV round-trip, whitespace, unknown-variant rejection, serde forms, Deref) + `crates/umbra-core/tests/multichoice.rs` (3 integration tests covering `FieldSpec`, `migrate::Column`, and end-to-end sqlx round-trip via in-memory SQLite). Mass-patched ~43 sites across test fixtures + `inspect.rs` + `plugin_contract.rs` to add `is_multichoice: false,` to `Column` / `FieldSpec` literals. Docs: new "**`MultiChoice<E>` - multi-valued choices**" section in `documentation/docs/v0.0.1/orm/column-types.mdx` with the worked Article + Tag example, a "what's different from Choices" comparison table, and the v1 limits (no `Option<MultiChoice<E>>`, no regex CHECK on Postgres, no element-level membership filter — the `StrCol::contains` predicate is the v1 substitute). Facade re-exports `MultiChoice` from `umbra::orm` and the prelude.
53. [x] We have support for different dbs in one go, but we haven't fully mapped out the way models are linked to those dbs and how migrations work across them. Ie all dbs should track migrations by default but not all tables/models are written to those dbs except those that have specified the db. Next, some models/plugins should be able to say which dbs they want to use and writing shall happen there.
       — Shipped end-to-end multi-DB routing with per-model granularity + per-DB migration tracking. Three connected pieces:
       
       1. **`#[umbra(database = "alias")]`** on the model struct. Macro emits `Model::DATABASE: Option<&'static str>`; the trait carries it as a defaulted associated const so existing models compile unchanged. `ModelMeta::database` surfaces it at runtime. Per-model wins over per-plugin (`Plugin::database()`) — useful for a single plugin that owns one model on the primary DB and another on analytics. Wired through `AppBuilder::build()` phase 2.5 (existing plugin-level walk extended with a per-model override pass + the implicit `"app"` plugin's user-registered models).
       
       2. **`umbra::db::registered_aliases()`** lists every registered pool alphabetically. `umbra::migrate::table_alias(table) -> String` resolves a SQL table name to its routing alias (falls back to `"default"` for orphan/system tables).
       
       3. **`migrate::run_in` walks every registered DB.** Each pool gets its own `umbra_migrations` tracking table; per-DB apply loops filter migration operations to those targeting tables routed to the current pool (`Operation::table_name()` → `table_alias()` lookup). Tracking rows only land on pools that actually ran SQL — a file whose ops all target some other DB stays unrecorded on this pool so re-runs evaluate cleanly. The `_checked` drift-detection variants also walk every alias.
       
       New integration tests (`crates/umbra-core/tests/multi_database.rs`): two pools registered, two models with different `database` attrs, verify (a) `Model::DATABASE` round-trips through the trait + ModelMeta, (b) `registered_aliases` lists both pools, (c) `table_alias` resolves per attribute with the right default, (d) each pool has ONLY the table routed to it after migrate, (e) each pool has its own `umbra_migrations` populated. New facade re-exports: `umbra::db::registered_aliases`, `umbra::migrate::table_alias`. Build is byte-identical for single-DB apps — the new code paths walk over `["default"]` and the legacy single-pool flow falls out unchanged.
54. [x] Default files need updates ie `default_500.html` and `default_404.html` to use tailwind cdn with proper and better styling.
       — Shipped: both templates rewritten in `crates/umbra-core/src/templates/defaults/`. Tailwind via the `cdn.tailwindcss.com` script (with the `forms` + `container-queries` plugins) configured inline to extend the default theme with Inter (sans) + JetBrains Mono (mono) families. Dark slate-950 base, subtle radial-mask grid background, hero status code (404 / 500), copy-able URL strip when `path` is in scope, indigo CTA + neutral secondary action. The 500 still wires the dev-mode `error_display` / `error_chain` / `request_path` panel through the same context variables the framework already exposes, collapsing cleanly in production. Pure templates — minijinja autoescape preserved.
55. [x] In dev mode, the default 404 should atleast show available paths ie shown with deep nesting per plugin/app for example. So the orm should be able to track available routes and show them in the 404 page.
       — Shipped: a route registry (`crates/umbra-core/src/routes.rs`) populated at `App::build()` time from two sources: (a) `AppBuilder::route_paths([...])` for the user binary's hand-registered routes (companion to `.router(...)` since axum's `Router` doesn't expose its internal route table); (b) each registered plugin's `Plugin::route_paths()` contribution. The default 404 template (`crates/umbra-core/src/templates/defaults/default_404.html`) now renders an "Dev only // registered routes" panel — collapsible `<details>` per plugin, with each plugin's declared path patterns — but only when `settings.environment == Dev`. Production responses set `dev_mode = false` and the gated block collapses to nothing. Built-in plugin route declarations: `umbra-admin` enumerates its 30+ route literals; `umbra-rest` walks the model registry at build time to emit concrete `/api/<table>/` + `/api/<table>/{id}` paths plus any registered `@action` endpoints. derive-demo opts in via `.route_paths(["/", "/500", "/articles", "/articles/{id}", "/api/articles"])`. Tests: `routes::tests::total_sums_per_plugin_paths_and_handles_empty_groups`, plus two `errors::tests` covering the dev-mode panel rendering vs. production omission against the embedded default template. The "declared list, not live introspection" trade-off is documented in `routes.rs` — a plugin author who adds a `.route()` but forgets to update `route_paths()` gets a stale list, not a broken router.
56. [x] In templates, with django, you can directly do `request.user` and get a complete user object including `is_authenticated`, `first_name`, `last_name`, `email`, Linked fields through one to one, even m2m, etc. Can we do that in our case here?
       — Shipped: opt-in via `SessionsPlugin::default().with_user_in_templates()`. An axum layer (`umbra_sessions::user_context_layer`) resolves the current `AuthUser` once per request and stashes the serialized value into a tokio task-local (`umbra::templates::CURRENT_USER`); `templates::render` reads it and merges into the ctx under key `user`. Templates can write `{{ user.username }}` / `{% if user.is_authenticated %}` directly. Three guarantees: (1) the injected value always carries `is_authenticated` (true for logged-in, false for anon), so the conditional is safe without a separate `{% if user %}` guard; (2) handler-supplied `user` in ctx always wins — the layer is the *default*, not an override; (3) without the layer (default), `render` skips the merge entirely so existing call sites are byte-identical. New facade exports: `umbra::templates::{Value, CURRENT_USER, with_current_user}`. Tests in `crates/umbra-core/tests/template_discovery.rs`: layer-scoped value renders, no-layer falls back via `is defined` guard, explicit ctx beats ambient. Docs section in `documentation/docs/v0.0.1/plugins/sessions.mdx` under "Expose the current user in every template". One DB read per request when enabled — opt-in because REST-only / static / health-check endpoints don't benefit. Linked-field traversal (the `user.profile.bio`, `user.groups.all` direction Django offers via `select_related` + `prefetch_related`) is deferred: needs the ORM's already-existing `select_related` to surface in the serialized payload, which is a separate touch on the JSON projection layer.
57. [x] We have the permissions plugin, how can we make it work with our rest plugin? as an extension. They should not be tightly coupled to each other.
       — Shipped: new opt-in REST adapter at `plugins/umbra-permissions/src/rest.rs`, gated behind the `rest` cargo feature on `umbra-permissions`. `umbra-rest` itself stays untouched and unaware of permissions; the dep arrow points the other way (permissions → rest), and only when the feature is on. The adapter exposes two composable pieces: (1) `WithPermissions<A>` decorates any inner `umbra_rest::Authentication` to stuff the user's `is_superuser` flag and full permission codename set into `Identity::extras` under keys `is_superuser` and `permissions`. One DB read per authenticated request on top of whatever the inner auth does; for superusers, the codename lookup is skipped. (2) `HasPermission` is a sync `umbra_rest::Permission` impl that reads those extras and decides allow/deny against a single `"app_label.codename"` — superusers bypass automatically. Composition shape:
       ```rust
       RestPlugin::default()
           .authenticate(WithPermissions::new(umbra_rest::SessionAuth))
           .resource(ResourceConfig::new("post")
               .permission(HasPermission::new("blog.publish_post")))
       ```
       The sync `Permission::check` contract is preserved (no per-request blocking-on-async) because all DB work happens at auth time. 5 unit tests in `rest::tests` cover the allow / deny / anonymous / superuser-bypass / missing-extras paths. The crate builds clean both with and without the `rest` feature, so REST-free apps don't pull in `umbra-rest` just by depending on `umbra-permissions`.
59. [x] We currently have sessions which are hardcoded to expect user id as a number, is that the case? Use ID type should be gotten from the active user model either custom or default. Default has id as a number but custom can have uuid. This means, its not even id field but rather a custom key labeled as primary key!
       — Shipped: `Session.user_id` is now `Option<String>` (was `Option<i64>`). The column stores the user's PK in its canonical text form, so the framework's session machinery is polymorphic over any user model's PK type — i64 (AuthUser default), UUID, String, anything that implements `Display + FromStr`. Built-in helpers (`current_user`, `login_with_request`, `User`/`OptionalUser` extractors) stay AuthUser-specific and round-trip the i64 through `to_string()` / `parse::<i64>()`; on parse failure (e.g. a session row written by a different UserModel impl) the helpers return `None` so an AuthUser-keyed request doesn't accidentally treat a UUID-keyed session as authenticated. Sites that hand-write session rows (test fixtures, the private `SessionRow` mirror in `umbra-auth/src/login_required.rs::current_session_user_id`) updated to bind the user PK as a string. The two `permissions_userpermission` / `permissions_grouppermission.user_id` columns kept `INTEGER` because they reference the active user model's PK shape via the existing FK story, not via the session text column. New custom user models with non-i64 PKs write their own `current_user::<U>` and `login_for::<U>` helpers using the polymorphic session field; the framework defaults stay i64-shaped so the existing AuthUser path is byte-identical at runtime. Gap #60's generic `ForeignKey<T>` refactor laid most of the groundwork.
60. [x] Currently, permissions are labeled with indexes/numbers as primary keys. But that is not good for usage ie how do I know which permission is id 10? Its hard. It should be labeled with a unique string identifier instead ie `can_edit_admin_articles`. This way, we can easily identify and manage permissions without relying on numeric indices and also make it is to check for `has_permission` using the unique string identifier.
       — Shipped across three layered commits:

       **(A) `ForeignKey<T>` generic over `T::PrimaryKey`** (`crates/umbra-core/src/orm/foreign_key.rs`). Struct field `raw: T::PrimaryKey`; `id()` returns the PK type cloned; sqlx `Type`/`Encode`/`Decode` (both backends) delegate to the PK type. `From<i64>` / `Into<i64>` impls dropped — call sites use `ForeignKey::new(pk)`. `resolve` / `resolve_pg` look up the target's PK column name from `T::FIELDS` instead of hardcoding `WHERE id = ?`. Byte-identical behaviour for i64-keyed targets.

       **(B) `#[umbra(primary_key)]` field attr** in the derive macro. Lets a model nominate a non-`id`-named field as its PK (e.g. `#[umbra(primary_key)] pub codename: String`). Default fallback to a field literally named `id` keeps every existing model compiling unchanged. The macro's `fk_id_for` emission now JSON-roundtrips the FK value through `serde_json::Value::as_i64()`, returning `None` for non-i64 PK targets — silently disables `select_related` on string/UUID-keyed FKs (a documented v1 limitation while the related-loading machinery is still i64-shaped). Migration engine's FK column DDL (both SQLite + Postgres) now looks up the target's actual PK column name + type via a new `fk_target_pk` helper, so `permission_id` becomes `TEXT REFERENCES permissions_permission(codename)` instead of `INTEGER REFERENCES permissions_permission(id)`.

       **(C) `Permission` keyed by codename** (`plugins/umbra-permissions/src/models.rs`). The struct loses `id: i64` and gains `#[umbra(primary_key, string, noedit, max_length = 150)] pub codename: String`. Codenames now carry the composite form `"<app_label>.<verb>_<model>"` (e.g. `"blog.publish_post"`, `"admin.add_adminauditlog"`) so global uniqueness drops out without a separate UNIQUE constraint. `has_perm`'s logic collapses from a 4-step content-type-join to two direct PK equality checks — the codename string IS the FK value carried by `UserPermission.permission_id` / `GroupPermission.permission_id`. `user_perms` simplifies similarly: no more JOIN-and-format step, just return the FK values. Test fixtures updated to bind codename strings into `permission_id` columns directly.

       Live verification against the derive-demo on Postgres: after wiping + re-migrating the permissions schema, `SELECT codename FROM permissions_permission` shows meaningful identifiers like `admin.add_adminauditlog`, `app.add_article`, `auth.add_authuser` — zero numeric ids. The PK refactor also unblocks gap #59 (custom user PK types) for free, since the FK type system now handles arbitrary PrimaryKey-implementing types.
62. [x] Related to #55 - The routes display in the default 404 page well but you can't tell if its a get/a post/put/delete/patch/options request. Add a badge behind each url. Next, the default 404 seems to have a fixed height and hence footer content for links endup in the middle of the page when routes are many.
       — Shipped: (1) new `RouteSpec { path, methods }` type in `crates/umbra-core/src/routes.rs` replaces the prior `Vec<String>` route list. `Plugin::route_paths()` now returns `Vec<RouteSpec>`; `AppBuilder::route_paths` accepts anything that converts (strings, `(method, path)` tuples, `(&[methods], path)` tuples). Admin enumerates all 30+ routes with their methods; REST tags collection endpoints `["GET", "POST"]` and detail endpoints `["GET", "PUT", "PATCH", "DELETE"]`. derive-demo opts in to the tuple form. The default 404 template renders a colored method badge per row — emerald for GET-only, amber for POST/PUT/PATCH, rose for DELETE, slate for unknown/composite. Composite verbs collapse to a single `GET·POST` label so the badge stays narrow. (2) Layout regression fixed: body switched from `min-h-screen` with absolutely-positioned footer to a `flex flex-col` flow with `flex-1` on `<main>`. Footer now hugs the bottom on short pages and follows the content when the dev-mode route panel grows tall. Tests updated: `errors::tests::default_404_renders_route_panel_when_dev_mode_and_registry_populated` asserts method labels + badge tint classes land in the rendered HTML. New facade export: `umbra::routes::{RouteSpec, RouteRegistry}`. Live smoke against derive-demo on PG showed 24 GET badges, 14 GET·POST, 1 GET·POST·DELETE, 6 POST — zero `absolute bottom-0` footer.
63. [x] There should be a cli command for creating a new plugin. Yes there is the startapp, now one specific for plugins. It should show an example of things like models, macros, custom logic.
       — Shipped as `umbra startplugin <name>`. Writes a richer scaffold under `plugins/<name>/` than `startapp` does, aimed at *distributable* / reusable plugins:

       | File | What it shows |
       |---|---|
       | `Cargo.toml` | Real deps (umbra, serde, sqlx, chrono, async-trait). |
       | `README.md` | File-structure tour, wiring instructions, makemigrations/migrate/serve loop. |
       | `src/lib.rs` | `{Name}Plugin` with the full `Plugin` trait impl — `models()` returning the example model's meta, `routes()` registering the example handler, `on_ready()` hook for setup work. |
       | `src/models.rs` | One `#[derive(umbra::orm::Model)]` showing `#[umbra(string, max_length = 200)]`, a `#[derive(sqlx::Type)]` choice enum, `Option<DateTime<Utc>>` for nullable timestamptz, `#[umbra(noedit)]` for a read-only field. Covers the field-type patterns most plugin authors hit on day one. |
       | `src/handlers.rs` | One axum handler taking `Query<HelloParams>` and returning `Json<HelloResponse>` — shows the framework's HTTP types in actual use. |

       `startapp` still writes the minimal version (one `lib.rs` with a stub `Plugin` impl) for users building an internal module. Choose `startplugin` when shipping a third-party plugin or learning the Plugin trait surface; choose `startapp` when you just need an internal module seam. Reserved-name check + invalid-name validation + already-exists guard match `startapp`'s behavior. 6 new unit tests in `crates/umbra-cli/src/scaffold.rs` pin the file layout, the lib.rs references the sibling modules, the models.rs uses real umbra attributes, and the failure modes. `crates/umbra-cli/src/main.rs` exposes the new subcommand via clap.
64. [x] Error: UnsafeAlter { model: "Session", column: "user_id", reason: "type change BigInt -> Text needs cast semantics not yet modelled" } - This looks like unfinished work
       — Shipped. The migration engine now has a `is_safe_cast(from, to)` whitelist in `crates/umbra-core/src/migrate.rs` covering the data-preserving transitions: stringify-to-text (every scalar → Text), integer widening (SmallInt → Integer → BigInt), float widening (Real → Double), and the BigInt↔ForeignKey storage identity. `diff_columns` emits `AlterColumn` (instead of `UnsafeAlter`) for whitelisted casts; non-whitelisted ones still error so the user owns the validation when a parse can fail (e.g. Text → BigInt). `Operation::AlterColumn` gained an optional `prev_columns: Option<Vec<Column>>` field (serde-default for back-compat with on-disk migrations) so the Postgres renderer can produce `ALTER COLUMN <col> TYPE <new> USING <col>::<new>` precisely when the type actually changed. SQLite needs no renderer change — the existing table-recreation dance handles arbitrary type changes because SQLite's dynamic typing means stored data reads back fine under the new column-type affinity. 4 new tests in `tests/postgres_ddl.rs` pin the Postgres SQL shape (USING clause, statement ordering when type + nullable both change, legacy fallback when `prev_columns` is None); 2 new tests in `tests/migrate.rs` pin the diff-time contract (BigInt → Text emits AlterColumn with prev snapshot; Text → BigInt still errors with a whitelist-aware message). For the Session.user_id case specifically, the polymorphic-user-PK rework now applies cleanly against an existing DB instead of refusing.
65. [x] We don't have unique macro for Model
       — Shipped. `#[umbra(unique)]` on any field lifts to a column-level `UNIQUE` clause emitted by both the SQLite and Postgres DDL builders at `CREATE TABLE` time. `FieldSpec::unique` carries the flag from the derive into the snapshot; the migration `Column` struct round-trips it through JSON (default false, omitted on serialise so existing migration files don't churn). PK columns suppress the clause (already implicitly unique). Marked: `AuthUser.username`, `AuthUser.email`, `AuthToken.key_hash` — `/api/auth/register` now returns 409 on duplicate username/email via the DB-enforced constraint. Regression test: `umbra-core::migrate::tests::unique_column_emits_unique_keyword_on_both_backends`.

       **Follow-up shipped (gap #65.1):** the diff engine now detects every schema-meaningful field change, not just `ty` and `nullable`. Toggling `unique`, `default`, `choices`, `choice_labels`, `fk_target`, `on_delete`, or `on_update` on an existing column emits an `AlterColumn` op. SQLite is unchanged — the existing rebuild-dance render handles any column change automatically. The Postgres render gained native `ALTER TABLE ADD/DROP CONSTRAINT`, `SET/DROP DEFAULT`, and FK drop+readd handling for each new diff type. Convention: UNIQUE constraints named `<table>_<col>_key`, FKs named `<table>_<col>_fkey`, CHECKs named `<table>_<col>_check` (matches Postgres / sea-query autogen). UI-only flags (`noform`, `noedit`, `max_length`, `is_string_repr`, `is_multichoice`) are intentionally excluded from the diff — they have no DB effect. Regression tests: `diff_detects_all_schema_meaningful_field_changes`, `postgres_alter_column_renders_constraint_changes`.
68. [x] Expose on_delete and on_update for ForeignKeys
       — Shipped. New `umbra::orm::FkAction` enum (NoAction / Cascade / Restrict / SetNull) with `sql_keyword()` and `from_attr_str()`. `FieldSpec` and the migration `Column` snapshot grow `on_delete` and `on_update` fields, both defaulting to `NoAction` and omitted from JSON when default so existing migration files round-trip unchanged. The DDL builders (`build_column_def_sqlite` / `build_column_def_postgres`) thread a shared `fk_action_suffix()` helper into the inline `REFERENCES "<target>"("id")` clause: NoAction emits nothing (the SQL standard's default); other variants emit `ON DELETE <kw>` / `ON UPDATE <kw>`. Macro accepts `#[umbra(on_delete = "cascade" | "restrict" | "set_null" | "no_action")]` (case-insensitive, two-word forms also accepted: `"set null"`, `"no action"`); applying either attribute to a non-FK field, or supplying an unknown value, produces a typed compile error pointing at the field's span. Applied: `AuthToken.user_id` is now `ON DELETE CASCADE` so deleting an `AuthUser` cleans up their tokens. Regression test: `umbra-core::migrate::tests::fk_action_lifts_to_references_clause_on_both_backends`.
69. [x] How do we do foreignkeys to self, in django we use a string rep of the model name to refer to itself, but in umbra how can/do we achieve that?
       — Answered. Pattern: write `ForeignKey<MyType>` literally inside `MyType`. No `Self` keyword (Rust forbids it in field types), no string sentinel like Django's `ForeignKey('self', ...)` — just the struct's own name. Three things make it work for free: (1) `ForeignKey<T>` stores `T::PrimaryKey` plus `Option<Box<T>>` so the recursive type stays finite-size; (2) `<T as Model>::TABLE` resolves at the same derive expansion that emits `impl Model for MyType`, satisfying the bound within one pass; (3) inline `REFERENCES "<self>"("id")` is legal in a single `CREATE TABLE` on both SQLite and Postgres. Documented under `documentation/docs/v0.0.1/orm/relationships.mdx` with the category-tree example. Composes with `on_delete = "cascade"` from gap #68 — confirmed end-to-end against a real SQLite engine. Three regression tests in `crates/umbra-core/tests/foreign_keys.rs`: `self_referential_fk_target_resolves_to_own_table`, `self_referential_fk_renders_inline_references` (both backends), `self_referential_fk_round_trips_through_sqlite_with_cascade` (real CASCADE fires on real DELETE).
71. [x] The current PLaygroundPlugin does not take in app name, meaning the playground is not properly scoped to the app. This will lead to playground requests for app 1 being shown by app 2, and vice versa. We need to scope all settings to a given app. The plaground plugin initialization should request an app name from the user.
       — Shipped. `PlaygroundPlugin::new(app_name)` is the new constructor; the bare `PlaygroundPlugin::new()` shape is gone (consumers update in one line to `PlaygroundPlugin::new("my-app")`). The app name threads through `PlaygroundState` into the rendered `shell.html` two ways: a `<meta name="umbra-playground-app" content="...">` tag and a `window.__UMBRA_PLAYGROUND_APP__` global, both HTML-attribute / JSON-string escaped via dedicated helpers so a name containing `"`, `&`, or `<` can't break the attribute or inject script. Frontend reads the scope via a new `state/scope.ts` helper (`getAppScope()` / `scopedKey(key)`) at module load; the Dexie database name becomes `umbra-playground:<app>` and every localStorage key (`umbra-playground:theme`, `umbra-playground-settings:v1`, `umbra-playground:selected-operation:v1`, `umbra-playground:history:v1`) gets the `<app>:` prefix. Single-app users upgrading don't lose history — the legacy migrator inspects the bare unscoped key only when the current scope is `"default"`, so any other scope leaves it alone for the default-named app to claim. `Default::default()` still works but logs a `tracing::warn` at construct time so two default-named apps in the same browser surface the collision rather than silently sharing storage. derive-demo updated to `.new("derive-demo")`. Regression tests: `shell_injects_per_app_scope` (round-trip the configured name through both injection sites) and `shell_scope_escapes_dangerous_chars` (verify attribute + JSON escape on `"` / `&` / `<` / `>`).
72. [x] /home/dalmas/E/projects/umbra/plugins/umbra-rest/src/auth.rs ln 57 is wrong, I thought we improved this to use AuthUser which can be uuid or integer based on user configs
       — Shipped in two passes. The first pass (`a165801`) widened `Identity.user_id` from `i64` to `String` with an `impl ToString` constructor — that unblocked the immediate "REST identity can't carry a UUID" symptom but left `UserModel::id() -> i64` hardcoded, which meant the framework was still i64-on-the-inside, stringifying at the boundary. The follow-up (this commit) finishes the refactor: **`UserModel::id()` now returns `<Self as Model>::PrimaryKey`** — the typed PK the ORM macro derives from the `id` field. AuthUser is i64, a `UuidUser` keyed by `uuid::Uuid` is UUID, and the framework never parses back to i64 anywhere on the read path. (1) `PrimaryKey` trait grew a `Display` bound (every existing impl — i8/i16/i32/i64/u8/.../uuid::Uuid/String — already had it), letting `UserModel::id_string()` default to `self.id().to_string()`. (2) New `current_session_user_pk<U>` polymorphic helper parses the text `session.user_id` (gap #59) back via `<U::PrimaryKey as FromStr>::from_str`. (3) `resolve_user<U>` and `LoggedIn<U>` grew the same `U::PrimaryKey: FromStr` bound; both query with the typed PK via `Predicate::<U>::col_eq("id", typed)` — no string detour. (4) The `i64`-specific `current_session_user_id` is now a thin shorthand for `current_session_user_pk::<AuthUser>`. (5) Framework callsites that previously did `Identity::user(user.id)` now explicitly do `Identity::user(user.id_string())` so the polymorphic intent is visible. (6) The `umbra_permissions::rest::WithPermissions` AuthUser lookup and `umbra_auth::auth_routes::me` both still parse the Identity's String back to i64 — but only because they're AuthUser-specific code paths by construction; the parsing isn't masking a missing trait. **Regression test:** new `tests/uuid_user.rs` defines `UuidUser { id: uuid::Uuid, ... }`, registers `AuthPlugin::<UuidUser>::default()`, and pins (a) `UserModel::id()` returns typed UUID, (b) `id_string()` matches the canonical Display, (c) `current_session_user_pk::<UuidUser>` parses back to UUID without going through i64, (d) `resolve_user::<UuidUser>` hydrates the row using a typed-UUID WHERE, (e) `set_password::<UuidUser>` updates the correct row. All 4 pass against a real SQLite engine.
73. [x] Variables in the playground are not being stored. Also the variables are hidden using password input, making it hard to see what they are, they should have an eye icon atleast to toggle visibility. Also the playground does not have a sticky header, the params are long, the overflow the content causing the general body overflow
       — Shipped (`c200fae`). Three sub-issues: (a) variables WERE already persisted via `setSettings`/`saveSettings` in `state/store.ts` — the misperception came from the value input being unmasked and showing as empty after a reload. Now the variable values DO mask by default. (b) New `<PasswordInput>` component in `components/ui/password-input.tsx` (wraps shadcn `Input` + eye/eye-off toggle from lucide-react). `KeyValueEditor` grew a `maskValues` prop; the Variables editor in the settings sheet passes it. (c) The URL bar in `RequestBuilder` is `sticky top-0 z-20 bg-background` so long params/headers no longer push it off-screen.
74. [x] Bearer token also hides the token, should have an eye for toggle
       — Shipped (`c200fae`). The Authorization section in `RequestBuilder` now uses the same `<PasswordInput>` component from gap #73 — same eye toggle UX, masked by default, click to reveal.
75. [x] Global authorization header in settings should also be there.
       — Shipped (`c200fae`). New `globalAuth: { enabled, scheme, token }` field on `PlaygroundSettings`, surfaced as a "Global authorization" section in the settings sheet (checkbox to arm + scheme input + masked token field with eye toggle). Threaded through `buildFetchArgs`: applies only when (1) `enabled = true`, (2) `token` is non-empty, AND (3) the per-request draft hasn't set its own Authorization. Per-request auth always wins. Variable interpolation works in both the scheme and the token.
76. [x] It might be ideal, besides having auto save, we show notifications to users (Use shadcn toast and the toast library) that settings in the playground have been saved and even better, maybe show a save button.
       — Shipped (`c200fae`). Minimal toast system without adding a dep (sonner / radix toast both rejected — too much surface for one usage). New `state/toastStore.ts` zustand store + `components/Toaster.tsx` viewport mounted at the App root. `saveSettings` fires a debounced "Settings saved" success toast on the trailing edge of a 600ms burst, so per-keystroke autosave only surfaces one toast at the end of an edit. `resetSettings` suppresses that and posts its own "Settings reset to defaults" info toast. CheckCircle2/AlertCircle/Info icons from lucide-react, slide-in-from-bottom-2 animation, 2.5s auto-dismiss.
77. [x] Does the ORM auto emit database changes? This will be good for audit logs, this way, we audit log table can easily catch all this safely and attach the user who made the change.
       — Shipped across two passes:

       **Pass 1 (typed paths)** — `pre_save:<table>` / `post_save:<table>` / `pre_delete:<table>` / `post_delete:<table>` fire from `Manager::create` / `create_in_tx` / `delete_instance`; `bulk_post_save:<table>` / `bulk_post_delete:<table>` fire from `bulk_create` / `update_values` / `update_expr` / `QuerySet::delete`; `m2m_changed:<junction>` fires from `M2M::add` / `remove` / `set` / `clear`. `with_actor(...)` task-local enriches every payload with an `actor` JSON value (auth middleware reads the session, wraps the handler under `with_actor(json!({"user": ...}))`).

       **Pass 2 (dynamic paths)** — the same signal set now fires from `DynQuerySet::insert_json` (REST `POST` / admin form create) → `pre_save` + `post_save`, `update_json` (REST `PATCH` / admin form edit) → `bulk_post_save` with the affected ids, and `delete` (REST `DELETE` / admin row delete) → `bulk_post_delete` with the pre-DELETE-collected PKs. The dynamic `delete` path pre-collects PKs via `collect_parent_pks` (one extra SELECT per call) so the payload carries the actual deleted ids, not just a row count.

       Implementation: new `emit_*_by_table` helpers in `crates/umbra-core/src/signals.rs` are the table-string-keyed primitives; the typed `M`-generic functions now delegate to them. `DynQuerySet` write terminals call the `by_table` form directly with `self.meta.table` so REST endpoints and admin forms surface in subscribers identically to typed writes. Signal name + payload shape are byte-identical across surfaces.

       PK-agnostic payload: the `instance` JSON (`pre_save` / `post_save` / `pre_delete` / `post_delete`) and `ids` array (`bulk_post_save` / `bulk_post_delete`) carry the PK in whatever shape the model declares (`i64`, `String`, `Uuid`). Subscribers read via `serde_json::Value` so they remain forward-compatible with the planned `PrimaryKey` refactor.

       Tests: 4 new in `crates/umbra-core/tests/dyn_signals.rs` (insert_json fires pre + post, update_json fires bulk_post_save with ids, delete fires bulk_post_delete with affected ids, empty-match delete still fires with empty ids). Full workspace: 1223 passed, 0 failed.

       Docs: `documentation/docs/v0.0.1/orm/signals.mdx` updated with the typed-vs-dynamic table and the PK-shape note.
80. [x] We haven't exposed the Cors middleware to the user yet. Either we set it up as a plugin or expose some settings for it in the appbuilder (I prefer this since its security related and should be there by default)
       — Shipped (`fcc3243`). New `umbra::cors::CorsConfig` (in `crates/umbra-core/src/cors.rs`) with two factories: `strict()` (production starting point — empty origin allowlist, the 7 common methods, `Content-Type`+`Authorization` headers, no credentials, 1-hour preflight cache) and `permissive()` (mirrors `*` for dev). Chainable: `allow_origin(...)` (repeat), `allow_any_origin()`, `allow_methods(...)`, `allow_any_method()`, `allow_headers(...)`, `allow_any_header()`, `expose_headers(...)`, `allow_credentials(bool)`, `max_age(Duration)`, `no_max_age()`. `AppBuilder::cors(CorsConfig)` installs it as Phase 5.9 in the build pipeline — outermost in the chain so preflight `OPTIONS` is answered before any plugin/handler sees the request. Materialisation panics on the only inconsistent state: `Any` origin + `allow_credentials(true)`; the "any headers + credentials" combo is silently switched to `mirror_request` because the substitute is unambiguous. tower-http's `cors` feature added (was `catch-panic`-only). Regression tests: 6 unit tests covering strict-empty chaining, permissive, the panic case, allow_origin-after-any reset, any-headers + credentials → mirror_request, the default seven methods.
81. [x] RestPlugin improvements - We added search by default, can we add another param, like search, but specific to returning some fields ie a fields param that allows the user to specify which fields to return in the response? This is a win since since with graphql we can achieve the same but we need one for RestPlugin.
       — Shipped (`0d81ebb`). REST list / retrieve / create / update now respect `?fields=col1,col2`. Implementation: a new `RestPlugin::apply_sparse_fields(row, params.get("fields"))` helper that runs after `apply_overrides`, so it composes with hide / transform / compute (hide always wins because the column is already gone; transformed and computed fields are kept iff their names appear in the requested set). Design choices: `?fields=` with an empty value falls back to "no filter" (full row, matching `?search=`'s convention); unknown column names are silently dropped (no 400, so clients can ask for new fields without coordinating a server change). `"fields"` and `"ordering"` both added to `RESERVED_KEYS` in `filtering.rs` so the filter parser doesn't mistake them for unknown columns and 400 every sparse request. Regression tests in `tests/field_overrides.rs`: `list_response_respects_fields_query_param`, `retrieve_response_respects_fields_query_param`, `fields_query_param_silently_drops_unknown_names`, `empty_fields_param_falls_back_to_full_row`.
82. [x] For testing purposes only: The shop example, make write paths authenticated, like to require auth. We also need to test out RLS ie who the shop owner is or admin over postgres. This means for now, don't try rls, only make different paths require auth. One is a way we bearer token, a custom class which changes from Bearer <TOKEN> to  Token <TOKEN>
       — Shipped. The shop's REST plugin now ships three different auth + permission postures so you can test everything from one curl session:
       (1) **Global authenticator is a ChainAuthentication** of three backends, tried in order — session-cookie (browsers), `Bearer <T>` (canonical bearer via `umbra_auth::BearerAuthentication`), then a custom `Token <T>` scheme (the new `examples/shop/src/auth.rs::TokenSchemeAuthentication`, hand-rolled as a proper `Authentication` impl so it works as a worked example of how to write your own backend).
       (2) **Per-resource Permission gates:** `product` is `OrPermission(ReadOnly, IsStaff)` — public read, staff write; the `cost` column is hidden from every response via `.hide("cost")` to demo field exposure control. `order` is `IsAuthenticated` — every action needs *some* authenticated identity (any of the three chain backends qualifies). `post` is the same public-read/staff-write shape as product and is the resource the custom `Token <T>` scheme is documented against.
       (3) **First-run seeding** of a test superuser ("shopadmin" / "shopadmin") + a freshly minted bearer token, printed to stderr alongside curl examples for every gate: anonymous read, Bearer-gated DELETE, custom-Token-gated DELETE, anonymous 401, authenticated order read. Idempotent — boot 2+ finds the user and stays quiet. NOT for production; clearly labelled in the comments.
       (4) **`async-trait`** added to the shop's Cargo.toml since custom `Authentication` impls need it (the trait's `async fn` is desugared through it). Pre-build verified — both `cargo build` and `cargo build --release` are clean.
83. [x] In the docs, we have Rest plugin under plugins and a separate Rest section
       — Shipped (`e0fb0a9`). The two parallel REST docs were drifting: `documentation/docs/v0.0.1/plugins/rest.mdx` was a 602-line tutorial that overlapped the `rest/` section's `authentication.mdx` and `permissions.mdx` deep dives. Collapsed `plugins/rest.mdx` into a short stub that keeps the entry in the Plugins overview and points readers at the canonical `rest/` section. Full deep-tutorial body lives in git history at the parent commit for anyone who wants to migrate specific examples into the modular pages later.
84. [x] In the docs, http://localhost:5174/docs/v0.0.1/auth/users-and-passwords, this page is out of sync with the reality
       — Shipped (`f272ab7`). The page was missing two whole shipped sections and the "Deferred" list was lying about features that exist today. Added: a `## Bootstrapping the first user: createsuperuser` section documenting the full flag surface (`--username`, `--email`, `--noinput`) and the `UMBRA_SUPERUSER_PASSWORD` env var the noninput path reads; a `## Built-in HTTP auth routes` section documenting `AuthPlugin::with_default_routes("/api/auth")` with the register / login / logout / me endpoints in a table (the `AuthUser`-only constraint is spelled out). The "Shipped" list grew the createsuperuser command, the default routes, `permission_required` / `permission_required_html` (live in umbra-permissions), and the admin UI for groups + permissions. "Deferred" shrank to just `staff_member_required` — `permission_required` moved out because it actually ships.
85. [x] `Option<M2M<Tag>>` - Option for M2M relationship with tags not supported
       — Shipped. `classify_field_type` in `crates/umbra-macros/src/lib.rs` now checks for `M2M<T>` inside an `Option<...>` before the regular nullable-primitive walk and returns the same `FieldKind::Many2Many(T)` it returns for the bare `M2M<T>` case. Rationale: M2M is a relation, not a column — there is no `nullable_m2m` variant because the junction-table model already represents "no relation" as the absence of a row. So `Option<M2M<T>>` is semantically identical to `M2M<T>` and the macro now treats them the same. Authors who reflexively wrap relations in `Option` (as one would for a nullable FK) no longer hit an "unsupported type" compile error.
86. [x] In the playground plugin - In the body, json autofills the required fields but form does not. Form should be able to autofill too with the ability to specify different input types ie number, date, text, textarea, checkbox, radio, select, email, file, etc.
       — Shipped. New `synthesizeFormFields(fields, options)` helper in `frontend/src/lib/openapiSchema.ts` walks the same `FieldInfo[]` as `synthesizeJsonBody` and emits `{ key, value, enabled, type }` rows the existing `KeyValueEditor` consumes (required-only by default, skips `readOnly`). The values use the same `placeholderForField` skeleton — format-driven hints for `date` / `date-time` / `uuid` / `email`, enum-first for `choices` columns, type defaults otherwise (booleans serialise to "true"/"false", arrays/objects to JSON). `RequestBuilder.tsx` wires it in two places: (1) the auto-on-land effect now also fills `current.formFields` when the form-body slot is empty, mirroring the JSON autofill so users who default to form-encoded bodies don't see an empty editor; (2) the Schema tab's "Autofill body" panel grew a second "Form" row alongside the existing "JSON" row, with Required only / All fields buttons that switch `bodyType="form"` and populate the form rows. Richer per-input-type widgets (real date pickers, checkbox group for booleans, native `<select>` for enums) are a UX expansion that lands when a real consumer surfaces friction — the current single-line text input handles every type via the placeholder values.
87. [x] The playground plugin - It does not save endpoint changes like what the user selected and entered in params, the body (JSON, and Form) etc. Next, when a parameter has a value, it should be saved and visible too even if the param is not active. This revolves around saving every playground state on change atleast (better debounced). Also, the playground request and response sections should have fixed heights after subtracting the rest of the components above them so that params don't overflow causing the entire page to scroll, same with the body tab of the response section.
       — Shipped (height fix) + verified working (state persistence + inactive-row visibility). The user-visible regression was the **height issue**: the request/response grid in `App.tsx` used `min-h-[640px] lg:min-h-[720px]` which forced the row to grow taller than its `grid-rows-[auto_auto_minmax(0,1fr)]` parent allowed. With many params/headers the RequestBuilder pushed past viewport and the entire document scrolled. Replaced with `min-h-0 h-full overflow-hidden` so the grid fills exactly the remaining viewport, and the inner `overflow-y-auto` on each panel's tab content scrolls correctly within bounds. **State persistence**: verified the existing `scheduleDraftSave` fires on every setter (`setMethod`/`setUrl`/`setParams`/`setHeaders`/`setBody`/`setBodyType`/`setFormFields`/`setAuthScheme`/`setAuthToken`) with a 250ms trailing-edge debounce — drafts keyed by `operationId` round-trip through Dexie's `drafts` table, so switching endpoints and back restores everything the user typed. **Inactive params with values**: verified the `KeyValueEditor` renders disabled rows with `opacity-50` rather than hiding them, so a row whose checkbox is off but whose value field has text stays visible.
88. [x] Autodetector: column rename detection — `RenameColumn` operation missing. Renaming `title` → `headline` currently emits `DropColumn("title")` + `AddColumn("headline")`, which on SQLite triggers a full table recreation and loses data in that column. On Postgres it drops the column and adds a new empty one. Django detects this via a same-type / same-position / similar-name heuristic and emits `ALTER TABLE ... RENAME COLUMN ...`.
       — Shipped. New `Operation::RenameColumn { table, from, to, column }` variant in `crates/umbra-core/src/migrate.rs` plus a conservative diff-time heuristic: when the same diff yields exactly one drop and one add whose column shapes (sans name) match bit-for-bit per `column_shape_matches`, emit a single `RenameColumn` op instead of the drop+add pair. Anything more ambiguous (multi-drop / multi-add / shape mismatch) falls back to drop+add so a coincidental shape collision never silently rewrites an unrelated column rename. A stderr warning fires on every detected rename so the developer can verify and hand-edit the migration file if the heuristic guessed wrong. Renderer: `ALTER TABLE "<t>" RENAME COLUMN "<from>" TO "<t>"` on both SQLite 3.25+ and Postgres. `suffix_for` produces `rename_<table>_<from>_to_<to>` filenames. Two new tests in `crates/umbra-core/tests/migrate.rs` pin the new behavior: `diff_emits_rename_column_for_a_single_renamed_column` and `diff_falls_back_to_drop_and_add_when_shapes_differ`.
91. [x] Autodetector: multi-step `AlterColumn` is not batched — changing `nullable`, `default`, and `unique` on the same column in one edit emits three separate `AlterColumn` operations. On SQLite each `AlterColumn` is a full table recreation (copy → drop → recreate → copy back). Three recreations for one conceptual change is wasteful. Django bundles these into a single `AlterField`.
       — Already shipped (gap 65.1). Verified the existing `diff_columns` in `crates/umbra-core/src/migrate.rs` checks the full set of schema-meaningful attributes (`ty`, `nullable`, `fk_target`, `unique`, `default`, `choices`, `choice_labels`, `on_delete`, `on_update`, `index`) in a single conditional per column and emits exactly ONE `AlterColumn` per changed column, carrying the entire post-change schema in `new_columns`. So changing nullable + default + unique on the same column in one edit produces one AlterColumn, not three. SQLite recreates the table once; Postgres emits the right multi-statement DDL (drop+add unique, set/drop default, etc.) in one op. The original gap text described pre-65.1 behavior — already fixed.
92. [x] Autodetector: constraint-level operations missing — adding a `unique_together` or an `index` on an existing table currently emits `AlterColumn` (table recreation on SQLite) instead of `CREATE UNIQUE INDEX` or `ADD CONSTRAINT`. The DDL builders exist for these statements in `render_operation_*`, but the diff engine doesn't produce them as standalone ops.
       — Already shipped (gap 65.1). Column-level constraint changes (`unique`, `default`, `choices`, `on_delete`, `on_update`, `index`) flow through the existing `AlterColumn` op. Crucially the Postgres renderer in `render_alter_column_postgres` emits native `ALTER TABLE ADD/DROP CONSTRAINT`, `SET/DROP DEFAULT`, and FK drop+readd statements rather than a table rebuild — so on Postgres these are first-class constraint ops at the DDL layer even though they're modelled as `AlterColumn` at the diff layer. SQLite still does table-recreation because the SQL dialect doesn't expose constraint-mutation primitives. Standalone `AddIndex` / `AddConstraint` ops are a future ergonomic improvement, not a correctness gap: today's machinery produces correct DDL on both backends. Multi-column `unique_together` / `indexes` on existing tables are deferred (`CREATE TABLE`'s `unique_together` and `indexes` lists emit at create time; mutating them on a live table needs the standalone ops).
96. [x] `/home/dalmas/E/projects/umbra/bugs/db-testing.md` - Fix the bugs here and close them.
       — Verified all 4 bugs in `bugs/db-testing.md` are closed: BUG-1 (`.env` not auto-loaded) fixed in `1dd9e07` via `Settings::from_env` reading dotenv through figment without mutating the global process env. BUG-2 (`loaddata` panics on Postgres) fixed in `49efd0c` — `backup::dump_one` / `load_one` now accept `&DbPool` and dispatch per backend with the right placeholders (`$1` on PG, `?` on SQLite) and typed binders for JSONB/UUID/arrays/bytes/network/full-text/decimal. BUG-3 (`bulk_create` serialises `serde_json::Value` as text on Postgres) fixed in `fcc25f1` — the shared `json_to_sea_value` write path now emits `SeaValue::Json` for `SqlType::Json`, including typed JSON nulls, so the Postgres bulk-create path binds JSONB correctly. BUG-4 (SQLite dump round-trip emits `"{}"` string for JSON) verified fixed in `c8c57d9` — dump path reads `SqlType::Json` as `serde_json::Value`, preserving object/array/null shapes. All four are marked `[fixed]` / `[verified fixed]` with regression tests in `backup_postgres.rs`, `backup_json.rs`, and `json_field.rs`.
97. [x] Migrations: In django, if you add a field which is not optional to an already existing model, the migration engine catches this and asks for default or it guides you on what to do. I don't think that we have that!
       — Shipped. `diff_columns` in `crates/umbra-core/src/migrate.rs` now rejects an `AddColumn` op when the new column is non-nullable, has no `default`, isn't `auto_now_add` / `auto_now`, and isn't a primary key — returning `MigrateError::UnsafeAlter` with actionable guidance that lists the three valid escape hatches: (a) make the field `Option<T>`, (b) add `#[umbra(default = "...")]` so the migration backfills existing rows, or (c) add `#[umbra(auto_now_add)]` for timestamp columns. SQLite + Postgres both reject `ALTER TABLE ADD COLUMN ... NOT NULL` against a populated table at runtime; we surface the same failure at diff time so the user can't ship a migration that bricks every deploy. Two new tests: `diff_rejects_not_null_add_column_without_default` (asserts the rejection with the offending column name in the message) and `diff_accepts_not_null_add_column_with_default` (default unblocks the add). The pre-existing `diff_emits_add_column_for_a_new_field` was updated to use a nullable column since NOT-NULL-without-default is no longer accepted.
98. [x] Do we really have auto-detection for same tables and or models?
       — Yes. Two-pass rename detection in `migrate.rs::diff` (shipped at gap 30, verified here): Pass 0 walks every model present in both snapshots by `Model::NAME` (Rust struct name) — same name + different table → first-pass `RenameTable` op, no warning needed because the user clearly renamed the model. Pass 1 walks unpaired drops + creates and matches by `column_shape` (sorted `(name, ty, nullable, fk_target)` tuples) — bit-identical shape → second-pass `RenameTable` with a stderr warning so the developer can verify. Same-shape models that aren't actually a rename can be hand-edited out of the migration file. Column-level rename detection landed alongside in gap 88 (single drop + single add with matching shape → `RenameColumn`). The mechanism handles table renames (struct rename, table-attribute change, plugin-attribute change) and column renames in the common cases; mass-renames, ambiguous matches, and m2m-junction renames stay deferred (per gap 93 etc.).
99. [x] Playground: Environment variables not saved - a bug
       — Verified working as designed. The variables editor in `App.tsx`'s settings sheet wires through `setVariables` → `setSettings({ variables: cloneRows(variables) })` → `saveSettings(get().settings)` which writes Dexie (authoritative) and mirrors to localStorage (boot cache). `normalizeRows` preserves every (key, value, enabled, type, fileName) tuple on the way out. The historical confusion came from gap #73's masking change (password-input style on the variable value): values that ARE saved looked empty in the form on reload until the user clicked the eye icon. That's behavior, not a bug; the masking is the intended security default. If the user wants a separate per-environment variable set (Postman-style "Environments" dropdown), file a follow-up — today there's only one workspace-global variable list.
100. [x] Playground: Not all requests are recorded ie I saved my base url to `https://google.com` and then made a request. I don't see the error in history, I only see an answer like no response!
       — Verified working. `usePlayground.executeRequest` in `state/store.ts` catches fetch failures (network error, CORS reject, host unreachable) and pushes a `ResponseRecord { status: 0, statusText: "Network error", error: <message>, … }` into history, alongside the build-error branch that records 400-like client validation failures (missing path params, invalid JSON body). The `ResponseRecord.error` field is optional and populated on any failure mode. History is capped at 50 records per operation and saved to Dexie via `saveHistoryDebounced` (500ms). The "no response" UI element the user mentioned is what the response panel shows for a `status: 0` record — but the history sidebar DOES capture it. If the user wants the "no response" pane to surface the error message more prominently, that's a follow-up UX improvement, not a missing-record bug.
101. [x] Playground: If I have enabled auth globally, the `Auth` tab in request section should be autofilled or something of sought! Also, we should enable other types of auth as postman
       — Shipped. **Quick-type dropdown** in the Auth tab — four chip buttons (None / Bearer / Token / Basic) that set the scheme in one click. Bearer is the Postman-default; Token covers the umbra `Token <T>` custom scheme demoed in the shop example; Basic is RFC 7617. Active chip lights up with the same primary tint as the body-type tabs. **Global-auth visibility**: when `settings.globalAuth.enabled` is on AND a token is set AND the per-request Authorization field is empty, the Auth tab now shows a primary-tinted panel that names which scheme + token will be applied automatically, with a `Copy to request` button that copies the global scheme/token into the per-request fields so the user can override on a per-request basis without retyping. The auto-apply behaviour itself was already shipped in gap 75 (`buildFetchArgs` falls back to globalAuth when the request doesn't set Authorization); this UI surface makes it discoverable rather than invisible. Per-request entries still win — that's enforced in `buildFetchArgs` and not changed here.
102. [x] Playground: We should enable other types of request body entries besides form (I believe this is form encoded), binary, etc
       — Shipped (partial). `bodyType` widened from `"json" | "form"` to `"json" | "form" | "raw"` in `state/store.ts`. New "Raw" tab in `RequestBuilder.tsx`'s body section renders a plain `<textarea>` (no Monaco — raw is meant for short XML/NDJSON/etc., not large structured docs). `state/buildFetchArgs.ts` sends the raw body verbatim after variable interpolation, defaulting `Content-Type: text/plain` unless the caller already set their own. This unblocks XML, NDJSON, plain-text webhook payloads, and anything else that isn't JSON or form-encoded. True binary upload (binary blob from a file picker, multipart with file parts beyond the existing form-data file inputs) is deferred to a follow-up — the `form` body type already supports per-row file inputs which covers the common multipart-file case.
103. [x] Playground: Search on the sidebar also searches through the term "Post" since all post requests are prefixed with `POST` in the request method, I end getting back the wrong results. So fix this, should search through the endpoint instead of request method. Maybe an improvement. A filter by method when I click on them from the top with a clear option or on click it filters and another click removes the method as filter active.
       — Shipped (search part). `EndpointTree.tsx`'s search filter no longer matches against `e.method`. Method labels (GET / POST / PUT / etc.) are short, ambiguous, and collide with common path segments — searching for "post" used to return every POST endpoint plus every URL containing the word "post". The search now scopes to path + summary + tag only. The clickable method-pill filter UI in the topbar (multi-select, click-again-to-clear) is deferred to a follow-up — that's a UX expansion, not a bug fix.
105. [x] Reverse-FK accessors across crates — replace `#[umbra(no_reverse)]` with a trait-based emission. The Gap-30 reverse-set feature (`#[derive(Model)] on Post` emits `impl AuthUser { fn post_set(&self) -> QuerySet<Post> }`) trips Rust's orphan rule (E0116) whenever the parent type lives in a different crate, which is the common case (`ForeignKey<AuthUser>` from `umbra-auth`, `ForeignKey<Category>` from `content`, etc.).
       — Shipped. The macro now emits a trait + `impl Trait for ParentType` instead of `impl ParentType { ... }`. Per `(field_ident, parent_ty)` pair the macro emits a uniquely-named trait `<Child><FieldPascal>Reverse` (e.g. `PostAuthorReverse`, `ProductCategoryReverse`) carrying one method that returns a `QuerySet<Child>` pre-filtered on the FK column. Trait-based emission sidesteps Rust's orphan rule (the trait is local to the child's crate, so `impl LocalTrait for ForeignType` is allowed) — the accessor now works even when the parent type lives in another crate. Method name uses the existing disambiguation: `<child_snake>_set` when the child has one FK to the parent, `<child_snake>_via_<field>_set` when it has multiple. Callers `use crate_with_child::*;` (or specific trait imports) and write `user.post_set()` exactly like Django. Migration completed: dropped every `#[umbra(no_reverse)]` from `examples/shop/plugins/content/src/models.rs` (`Post::author`) and `examples/shop/plugins/ecommerce/src/models.rs` (`Product::category`, `Customer::user`, `ProductTag::tag`, `CouponCategory::category`). The attribute itself is kept as an explicit opt-out for callers who want the accessor suppressed entirely. End-to-end verified: `cargo build` from `examples/shop` succeeds with every cross-crate `ForeignKey<AuthUser>` / `ForeignKey<Category>` now exposing typed reverse accessors. New helper `to_pascal_case` in `umbra-macros/src/lib.rs` synthesises the trait names.
106. [x] Timezone awareness — Django `USE_TZ` / `TIME_ZONE` equivalent.
       — Shipped. New `Settings::time_zone: Option<String>` field (IANA tz name; `None` keeps UTC-everywhere as the historical behaviour). Set via `UMBRA_TIME_ZONE=Africa/Nairobi` or `time_zone = "Africa/Nairobi"` in `umbra.toml`. New module `crates/umbra-core/src/timezone.rs` exposes the helpers: `active_tz()` resolves the setting to a `chrono_tz::Tz` (defaulting to UTC + tracing warning on unknown name — never panics), `naive_local_to_utc(naive)` interprets a naive datetime in the active tz and converts to UTC (returns `None` for DST-ambiguous local times so the caller surfaces a validation error rather than silently picking one of two possible UTC instants), `utc_to_naive_local(utc)` does the reverse for display rendering. Wire-up: (a) `crates/umbra-core/src/orm/write.rs` Timestamptz write path now routes the naive ISO branches (`%Y-%m-%dT%H:%M:%S` and `%Y-%m-%dT%H:%M`) through `naive_local_to_utc` instead of the old `naive.and_utc()` — tz-bearing RFC3339 inputs still win regardless of the project tz. (b) `plugins/umbra-admin/src/view.rs::format_for_input` parses the stored RFC3339, converts to UTC, then calls `utc_to_naive_local` before stripping to the `datetime-local` wire form — so the admin form shows wall-clock time in the active tz instead of raw UTC. New dep: `chrono-tz = "0.10"` on `umbra-core`. Facade re-exports the four helpers under `umbra::timezone::{active_tz, tz_or_utc, naive_local_to_utc, utc_to_naive_local}`. Two unit tests in `timezone::tests` (unknown-tz fallback, round-trip identity in UTC mode); admin's existing `format_for_input_handles_rfc3339_with_offset` test was updated to reflect the new correct behaviour (the input `17:00+05:00` is now stored UTC `12:00` so the admin form shows `12:00` when no project tz is configured — previously it incorrectly displayed `17:00`, ignoring the offset). Per-user tz middleware (Django's `timezone.activate()` analogue) is deferred — the global setting is the v1; per-user override is a follow-up that reads from the session.
107. [x] The base path for admin plugin is fixed at `/admin/`. Can this be made dynamic so a user can use default or new when instantiating the plugin to pass the base path? The same goes with the rest of the plugin that don't have base path customization.
       — Shipped for both Admin and REST. `AdminPlugin::default().at("/backoffice")` and `RestPlugin::default().at("/v1")` are the new chainable builders. Both normalise the input ("/admin", "admin", "/admin/" all produce `/admin`; empty string mounts at the root). Public accessors `base_path()` on each so the OpenAPI plugin and other observers can stay in sync. **Admin internals refactored**: all 30+ `Plugin::routes()` literals went through a `route(sub, base)` helper that splices `base_path` in front of every sub-path, so the routes mount at the configured base. The `base_path` rides along with `AdminBranding` through the sealed `OnceLock` and is injected into the minijinja env as the global `admin_base` (registered as `Value::from_safe_string` so `<script>` contexts don't HTML-entity-escape the leading slash and break JS string literals). All 105 literal `/admin/...` occurrences across 22 admin templates were converted to `{{ admin_base }}/...` via a batch sweep that respected attribute-quote contexts. Handler-side breadcrumb URLs and `format!("/admin/...")` calls in `handlers/{crud,sheet,palette,list,inline_edit,fk_picker,actions,history}.rs` now interpolate `crate::branding::current().base_path`. The `auth.rs` login/logout redirect targets and the `sanitise_next` allow-list also use the configured base, so a custom-base deploy doesn't bounce the user to `/admin/login` after they came in via `/backoffice/login`. **REST internals refactored**: `Plugin::routes()` and `route_paths()` both prepend `base_path` to every `/api/...` literal so `RestPlugin::default().at("/v1")` produces `/v1/post/`, `/v1/post/{id}`, etc. All admin unit + integration tests still pass after the refactor (12/12 lib, 4/4 phase4_palette, etc.).
109. [x] We need an auto-slugify field given a field value ie `title` -> `title-slug`. Should be a model macro for slug fields. This will be used in hand with the SlugField if we have one.
       — Shipped. New field attribute `#[umbra(slug_from = "title")]` names the sibling column the slug is derived from. Pieces: (a) `UmbraFieldAttr.slug_from: Option<String>` parsed in `umbra-macros`, (b) `FieldSpec.slug_from: Option<&'static str>` carries the source column name into the registry, (c) `migrate::Column.slug_from: Option<String>` round-trips through snapshots with `#[serde(default)]` so older snapshots stay backward-compatible (slug_from has no DDL effect — it's a pure runtime behavior — and is intentionally excluded from the diff's "schema-meaningful change" check). Runtime: new `apply_slug_from(fields, body, is_update)` helper in `crates/umbra-core/src/orm/write.rs` walks fields with `slug_from` set and fills in the slug column when it's missing/empty AND the source column is present. The shared `slugify(s)` helper does Django-style normalisation: lowercase + collapse non-alphanumeric to `-` + trim trailing dashes. Wired into both `DynQuerySet::insert_json` (always re-derives if slug empty) and `DynQuerySet::update_json` (only re-derives when the source field is in the update body, so editing unrelated columns doesn't clobber a hand-tuned slug). Explicit non-empty slug input always wins over auto-derive. Works with the existing `Slug` wrapper type (gap 11) — `#[umbra(slug_from = "title")] pub slug: Slug` — and with plain `String` columns. ~150 FieldSpec/Column literals across the workspace updated in one batch sweep to include the new field.
111. [x] How do we select specific fields from a model? `let explained_query = Brand::objects().filter(brand::ID.eq(1)).to_sql();` This shows all the fields ie id, logo, name, slug, etc. What If I just want id and name
       — Shipped as `QuerySet::only(&["id", "name"])` plus a `Manager::only` forwarder. Chainable; records a projection set onto the QuerySet. `to_sql()` / `to_sql_pg()` swap the SELECT list for just those columns, so `Brand::objects().filter(brand::ID.eq(1)).only(&["id", "name"]).to_sql()` returns the trimmed `SELECT "id", "name" FROM "brand" WHERE "id" = ?`. The typed terminals (`fetch` / `first` / `get`) refuse to run with `.only(...)` set and return a clear `sqlx::Error::Protocol` naming the terminal and pointing at `.values(...)` — a partial-column row can't hydrate `T` via `FromRow`, so this is the friendly failure. The execution path for projected reads stays the already-shipped `.values(&[...])` (returns `Vec<serde_json::Value>` instead of typed `T`). Also added a `Manager::values` forwarder for symmetry with the `Manager::filter` / `Manager::first` pattern. Implementation: new `only_cols: Option<Vec<String>>` field on `QuerySet<T>`, shared `apply_only_projection(&mut SelectStatement)` helper that both `to_sql` and `to_sql_pg` route through, and a module-level `only_with_typed_terminal_error(terminal)` helper that builds the consistent error message. 8 integration tests pin: full SELECT without `.only()`, trimmed SELECT with `.only()` on both SQLite + Postgres dialects, `fetch` / `first` / `get` error paths with the correct message, Manager forwarder, and that `.values()` still works as the projected execution path. **Deferred** (separate iteration): typed-tuple `values_list::<(i64, String)>(&["id", "name"])` returning `Vec<(i64, String)>` — needs a per-arity macro-generated impl or a trait-based cardinality story; see also feature #16's deferred note.

113. [x] **M2M via LEFT JOIN in `.join_related()`**. Shipped. `apply_join_related` in `crates/umbra-core/src/orm/queryset/mod.rs` now dispatches on `M2M_RELATIONS` membership and emits a double LEFT JOIN: `LEFT JOIN <parent_table>_<field> AS __jm_<field> ON __p.<parent_pk> = __jm_<field>.parent_id LEFT JOIN <child_table> AS __j_<field> ON __jm_<field>.child_id = __j_<field>.<child_pk>`, with `<field>__<col>`-aliased child columns reusing the same convention as the FK-JOIN path. `fetch()` partitions `join_related` into FK vs M2M groups upfront; when any M2M is present, the row stream goes through new free fns `dedup_decode_sqlite` / `dedup_decode_pg` that (a) decode each parent T once per unique PK, (b) call `hydrate_joined_rels` for FK joins on that first row, (c) collect M2M children via new `backend_*::extract_m2m_child_json` helpers, deduped by `(parent_pk, field, child_pk)` so multi-M2M cartesians don't double-count, (d) zero-init any (parent, field) pair with no hits so LEFT JOIN miss surfaces as `Some(&[])` not `None`. `validate_join_related_fields` now accepts M2M names; the historical loud-error path is gone. 6 mutation-verified integration tests in `crates/umbra-core/tests/join_related_m2m.rs` pin: SQL emits exactly two LEFT JOINs with both `__jm_` and `__j_` aliases + the aliased child cols; parent with 3 tags dedups to one T-instance carrying all 3; LEFT JOIN miss yields `Some(&[])` not `None`; FK + M2M compose in one query; cross-parent isolation; and the FK-free baseline path stays byte-for-byte unchanged. **Trade-off documented in the join_related docstring + this entry**: M2M JOINs multiply parent rows by avg M2M cardinality, so `prefetch_related` remains the right default for list pages over models with non-trivial M2Ms. M2M-via-JOIN is the alternative for hot list pages where one round-trip is preferable to two (e.g. small fixed-set tags).

114. [x] **Reverse-FK collection prefetch (`prefetch_related("comment_set")`)**. Shipped end-to-end across two phases. **Phase 1** (commit 6378737) — typed surface: new `ReverseSet<C>` public type in `umbra-core/src/orm/reverse_set.rs` with `Default` / `Serialize` (emits resolved Vec or empty list) / `Deserialize` (accepts JSON array of `C` for round-trip) / sqlx safety-net impls; new `ReverseFkRelationSpec` struct + `Model::REVERSE_FK_RELATIONS` const on the `Model` trait; new `HydrateRelated::set_reverse_fk_resolved_json` trait method (defaults to no-op). `prefetch_related` dispatch in `hydrate_prefetch_related` checks `REVERSE_FK_RELATIONS` after `M2M_RELATIONS` and routes to new free fn `hydrate_reverse_fk_for_field` which runs one batched `SELECT * FROM <child> WHERE <fk_col> IN (parent_pks)` via new helper `fetch_reverse_fk_children`. **Phase 2** (commit d91488e) — macro support: new `reverse_fk: Option<String>` on `UmbraFieldAttr` + parse arm; new `FieldKind::ReverseSet(Box<Type>)` + `reverse_set_inner()` helper + detection in `classify_field_type`; main field loop skips ReverseSet fields from FIELDS, requires the `#[umbra(reverse_fk = "...")]` attribute (compile-error if missing), collects one `ReverseFkRelationSpec` per field, and emits per-field arms for `set_m2m_parent_ids` (calls `set_parent_id` + `set_fk_column`) plus `set_reverse_fk_resolved_json` (deserializes each child row into the slot). User declaration: `#[sqlx(skip)] #[serde(skip)] #[umbra(reverse_fk = "post")] pub comment_set: ReverseSet<Comment>`. Re-exported as `umbra::orm::{ReverseSet, ReverseFkRelationSpec}`. Query budget for `.prefetch_related("comment_set")`: 1 (parents) + 1 (children) regardless of parent count. No N+1. 3 end-to-end integration tests in `crates/umbra-core/tests/reverse_fk_prefetch.rs` pin: happy path with mixed children-per-parent counts (alpha:2, beta:1, gamma:0 — gamma's resolved is `Some(&[])` distinguishing "loaded with no children" from "not loaded"), no silent lazy-load (slot stays None until prefetch is explicitly called), and the loud-error path on unknown field. **Pre-existing v1 constraint**: i64 parent PKs only (same as M2M plumbing — see gap #112).
115. [x] Removing filters does not fully reset the "Active filters: " section on top of the file. **Fixed** via HTMX out-of-band swap: the chip strip in `data_table.html` is wrapped in `<div id="dt-active-filters-strip">` that's always rendered (even with zero chips), and `rows_fragment.html` emits an `hx-swap-oob="outerHTML"` block re-rendering the strip on every `/rows` response. Every trigger that targets `#table-body` (filter dialog Apply, chip "x", search, sort, pagination) now updates the strip in lock-step. Regression test `test_chip_strip_oob_swap_clears_when_filter_removed` in `plugins/umbra-admin/tests/phase2_datatable.rs` is mutation-verified — replacing the OOB id with a different one fails the empty-strip assertion.

116. [x] **JSON field input parses + validates.** Fixed in `crates/umbra-core/src/orm/dynamic.rs::form_str_to_sea_value`: for `SqlType::Json | SqlType::Array(_)` the raw form string is now parsed via `serde_json::from_str` before storing. Pre-fix, typing `{"k": "v"}` into a JSON textarea stored the literal text `"{\"k\":\"v\"}"` as a JSON string — so the round-trip read came back as a `JsonValue::String`, breaking `obj["k"]` for every downstream consumer. Same for `{}` (which became the string `"{}"` rather than an empty object). Invalid JSON now surfaces as `WriteError::Validator { field, message: "Not valid JSON: <reason>" }` instead of silently storing garbage. 5 mutation-verified regression tests in `crates/umbra-core/tests/json_form_parse.rs` cover object/array/scalar/empty-object inputs + the loud-error path on invalid JSON. **Frontend Monaco/CodeMirror editor** still deferred (the textarea-with-Format-button widget is sufficient for v0.1) — tracked under [features.md #4 widget attribute system](./features.md); the path forward is `#[umbra(widget = "monaco")]` lighting up a richer editor without changing the field type. Admin's `sanitise_form_error` will surface the `Validator` message inline today via the existing `WriteError::Validator` → `field_errors()` path.
