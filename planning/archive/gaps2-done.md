# Archived done entries from `bugs/gaps2.md`

Moved verbatim from `bugs/gaps2.md` to keep the active tracker small. Entry numbers are unchanged — a reference like `gaps2 #N` resolves here once the entry ships.

1. [x] **Save-feedback toast in the admin sheet — SHIPPED in commit `d2916d5` as gaps2 #13.** Same symptom; `d2916d5` wired `showToast` alongside the existing `closeSheet` + `refreshTable` HX-Trigger events on every CRUD success path (`sheet::sheet_create`, `crud::update`, `crud::htmx_delete`). 3 regression tests in `plugins/umbra-admin/tests/phase2_sheet.rs` pin the trigger payload. The failure-path toast already worked via the inline error fragment (commit `5b163ab`); this commit completed the success-side symmetry.
3. [x] **Change-password dialog extracted to an HTML `<template>` — SHIPPED in commit `5b22cc5`.** Dialog markup lives at `plugins/umbra-admin/templates/wrapper.html` as `<template id="umbra-change-password-dialog-template">`; the JS opener (`umbra._openChangePasswordDialog`) clones the template content and patches the form's `hx-post` URL to `{{ admin_base }}/<table>/<id>/change-password`. Form gained a `data-change-pw-form` attribute hook (single call-site-varying piece — the URL). Designers can edit the dialog markup without touching `<script>` tags; Tailwind's content scanner finds the classes natively; no JS string concat. Pinned by `change_password_dialog_uses_html_template_not_js_concat` in `plugins/umbra-admin/tests/phase4_dashboard.rs` — three asserts (template present, hook present, old shape absent).
15. [x] **REST `?include=fk1,fk2` query-param plumbing → DynQuerySet.select_related().** — Shipped.

    `DynQuerySet::select_related_dyn(&[String])` mirrors the typed `QuerySet::select_related` (validates each name is an FK on the meta, dedups, no-ops on bad names). `fetch_as_json` runs `hydrate_select_related_into` after the main fetch — one batched `IN (...)` per FK regardless of parent row count, splicing the resolved row's JSON in place of the integer id. Reuses the existing `queryset::hydration::fetch_related_as_json` helper that powers the typed path (now `pub(crate)`) so SQLite + Postgres dispatch stays in one place.

    REST list + retrieve handlers parse `?include=fk1,fk2` via `parse_include` — rejects unknown / non-FK names with a 400 (loud failure, unlike `?fields=` which silently drops) because an unknown include is almost always a typo or stale-client expectation.

    OpenAPI: `include_parameter` emits a per-resource `?include=` entry only when the model has FK columns. `x-umbra-include-fks` extension carries the candidate FK names so the playground builds a multi-select. Mirrors `x-umbra-fields-columns` shape.

    One-hop only on day one (matching the typed `select_related`'s current scope; nested `?include=author.manager` lands when typed `select_related("author__manager")` does — already supported on the typed side but not yet wired through the dynamic helper).

    Demo: `GET /api/customer/?include=user` returns customers with `user` as the full AuthUser object instead of `7`. `GET /api/customer/?include=banana` returns 400 with "?include=: unknown field `banana` on `customer`".

~~15. ~~ (the originally-open description below kept for archive trail)

    The ORM ships `select_related` (features.md #18 marked `[x]`) and FK fields serialize as the full object after the call. But the REST plugin's standard list / retrieve handlers use `DynQuerySet::fetch_json` / `first_json`, which DON'T accept a select_related hint — there's no query-param hook that turns `GET /api/customer/?include=user` into `Customer::objects().select_related("user").fetch()`.

    Today's workarounds:
      - **Custom action** (`ResourceConfig::action("with_user", ...)`) that calls the typed `Customer::objects().select_related("user")` and serializes the row by hand. Works, but every "I want the user expanded" call site needs its own action — doesn't scale to "expose the standard endpoint with optional expansion."
      - **`.computed("user_obj", |row| ...)`** does a second lookup synchronously inside the response transformer. Wrong shape — turns one query into N+1, defeats the whole point of select_related.

    **Fix shape**:
      - Add a `?include=` (or `?expand=`) reader to the REST list / retrieve handlers (alongside the existing `?fields=` sparse-fieldset, `?search=`, `?filter_*=` readers).
      - Parse comma-separated FK column names; reject unknown / non-FK fields with 400 (consistent with the rest of the validator surface).
      - Forward to `DynQuerySet` via a new `.select_related_dyn(&[...])` method — mirror the typed `.select_related()` shape but driven by `ModelMeta` lookups instead of compile-time field constants.
      - Emit the param into the OpenAPI spec as an `enum` of valid FK field names so the playground builds a checkbox/multi-select for it.

    **Scope reality check**: this is the ORM gap that flowed through to the REST surface — the typed path was completed in feature #18 but DynQuerySet was left behind (because it predates select_related landing). It's a focused ~150-line PR on the REST + ORM dynamic surface, mostly mechanical wiring.

    **One-hop only on day one** (matching the typed `select_related`'s current scope; nested `?include=author.manager` lands when typed `select_related("author__manager")` does).

20. [x] **Shop example ships render-blocking CDN Tailwind + Google Fonts — replace with compiled CSS + self-hosted Inter.** SHIPPED in commit `726fba6` (2026-06-09). Mirrored `plugins/umbra-admin`'s setup at the shop: `examples/shop/styles/{input.css,tailwind.config.js,package.json}` source dir + `examples/shop/build.rs` runs `npx tailwindcss --minify` when `styles/node_modules` exists. `@fontsource/inter` (Latin subset) gets inlined into the compiled `static/css/shop.css` (21 KiB minified, 5× smaller than CDN). 5 weights × {woff,woff2} = 10 Inter binaries committed at `static/css/files/`. `StaticPlugin::new("/static", "./static")` mounted in `src/main.rs`. `wrapper.html` lost the 4 CDN/Google-Fonts lines + the inline `<style>` block; gained a single `<link rel="stylesheet" href="/static/css/shop.css">`. Live verification: `shop.css → HTTP 200 / 21368 B`, `inter-latin-400-normal.woff2 → HTTP 200 / 23664 B`, zero `cdn.tailwindcss.com` / `googleapis.com` references in served HTML. Framework-level follow-up (`umbra startproject` should scaffold this shape by default) tracked under gap #8. Original diagnosis preserved below:

      - `cdn.tailwindcss.com/3.4.17` — **124 KiB**, ~310ms. JIT-in-browser bundle that Tailwind's own docs explicitly say is "for development only." Ships the full compiler to every visitor, runs JS before paint, causes FOUC.
      - `fonts.googleapis.com/css2?family=Inter` — **1.2 KiB CSS, ~430ms blocking**. Even with `preconnect`, the CSS itself blocks render; woff2 files are extra round-trips against an external origin.

    For an example app meant to teach production-shaped patterns, this is hostile — newer devs reading the shop will absorb "umbra apps use a Tailwind CDN" as the canonical shape and ship the same setup in their own apps.

    **Fix shape** (~30 minute job):

      1. **Compile Tailwind to a static CSS file at build time.** Same pattern `umbra-admin` already uses (see `plugins/umbra-admin/build.rs`). Add `examples/shop/styles/input.css` + `tailwind.config.js`, run `npx tailwindcss -i styles/input.css -o static/css/shop.css --minify --content "templates/**/*.html"` (either manually documented in the example's README, or wired via `build.rs`). Output: ~25 KiB minified — 5× smaller than the CDN bundle, zero runtime JS.

      2. **Self-host Inter as woff2.** Drop 5 woff2 files (Regular / Medium / SemiBold / Bold / ExtraBold) into `examples/shop/static/fonts/`. Add `@font-face` blocks to `input.css` (gets compiled into `shop.css`) with `font-display: swap` so text paints in the fallback immediately and re-renders when Inter loads.

      3. **Drop three lines from wrapper.html**: the cdn.tailwindcss.com script, the two `<link rel="preconnect">` lines, the `<link href="googleapis...">`. Add one: `<link rel="stylesheet" href="/static/css/shop.css">`.

    **Expected gains** (table values from the diagnosis):

    | Metric | Before | After |
    |---|---|---|
    | Render-blocking resources | 2 (310ms + 430ms) | 1 (~20ms local) |
    | CSS transfer | 125 KiB | ~25 KiB |
    | JS execution before paint | ~100 KiB Tailwind runtime | 0 |
    | External domains hit | 3 (cdn, googleapis, gstatic) | 0 |
    | LCP estimate | 700ms+ | ~150ms |

    **Framework-level fix lands via gap #8 (bootstrap project layout)**: `umbra startproject <name>` should scaffold the compiled-Tailwind setup by default — `styles/input.css`, `tailwind.config.js`, the build step in `build.rs` (or a documented npm script), `static/fonts/Inter-*.woff2`, and the wrapper template with `<link rel="stylesheet" href="/static/css/<name>.css">`. The CDN shape becomes the deliberate opt-out, not the default. Until startproject grows that scaffold, the shop should manually demonstrate the right shape so it doesn't actively teach the wrong one.

    **Triggering case to fix**: load `http://localhost:8000/` in Lighthouse → 700ms+ LCP from the two blocking resources. Post-fix the same page should hit ~150ms LCP locally.

19. [x] **`Form<T>` extractor + `#[derive(Form)]` validation — Shipped.**

    New surface in `crates/umbra-core/src/forms.rs`:

    - `Form<T>` axum extractor (struct) — wraps `Result<T, FormErrors>` so handlers ALWAYS receive a `Form<T>` and branch via `form.into_result()`. The HTTP layer never rejects on validation failure; the handler decides whether to re-render with errors or return 4xx.
    - `FormErrors` — thin wrapper around `WriteError` exposing `field_errors()` / `non_field_errors()` (DRF shape) AND `as_template_ctx()` (template-friendly flat shape: each field key maps to its FIRST error string, plus a `form` key for the non-field message).
    - `From<ValidationErrors> for WriteError` lifter — every per-field message becomes a `WriteError::Validator { field, message }`; non-field messages become an anonymous-field validator. Bundles under `WriteError::Multiple` for >1 error. **This is the architectural unifier**: ORM-validator errors, REST 400 bodies, admin form spans, and HTML form errors now all flow from the same `WriteError` source — no per-surface translator drift.
    - **Trait rename**: `forms::Form` (old) → `forms::FormValidate`. The name with generics went to the extractor (matches `axum::extract::Form<T>` shape); the trait got the more descriptive `FormValidate`. The derive macro name stays `Form` (it lives in a different namespace).

    Macro additions in `crates/umbra-macros/src/lib.rs`:

    - `#[form(required, ...)]` field-level — explicit Django-style declaration (no-op since Required is the default; accepted so users can mirror the spec's verbose shape).
    - `#[form(length(min = N, max = M))]` field-level — validator-crate-style combined syntax. Lowers to the same MinLength/MaxLength validators the legacy `min_length` / `max_length` keys produce.
    - `#[form(normalize_strings)]` container-level — auto-trims every `String` field before validation runs. Eliminates the per-field `form.name = form.name.trim().to_string()` boilerplate.

    Shop's `contact_submit` ported (`examples/shop/src/views/public.rs`) — the bespoke `ContactErrors` struct + `has_any()` + `validate_contact_form()` + `normalize_contact_form()` + `looks_like_email()` are GONE. Form now declares its rules inline:

    ```rust
    #[derive(Debug, Deserialize, Default, umbra::forms::Form)]
    #[form(normalize_strings)]
    pub struct ContactForm {
        #[form(required, length(min = 1, max = 100))]    name: String,
        #[form(required, email, max_length = 254)]        email: String,
        #[form(optional, max_length = 30)]                phone: String,
        #[form(required, length(min = 1, max = 200))]     subject: String,
        #[form(required, length(min = 10, max = 5000))]   message: String,
    }
    ```

    Handler is 25 lines instead of ~80.

    Live verification on the shop:
      - `POST /contact` with empty body → HTTP 422, every required field carries a rose-bordered input + "<field> is required" message, form-level banner "Please fix the highlighted fields and send again."
      - `POST /contact` with `email=not-an-email` → HTTP 422, only the email field highlights, message "email must contain `@`"
      - `POST /contact` with valid body → HTTP 303 redirect to `/contact?sent=1` (matches pre-port behaviour)

    Tests: 5 new in `crates/umbra-core/tests/form_extractor.rs` (happy path, missing-required, bad-email, normalize_strings, flat-template-ctx). 12 existing in `tests/form_derive.rs` still pass after the trait rename. Full workspace `cargo test`: 1219 passed, 0 failed.

    **Architectural rule shipped with this work** (per the spec): validation errors originate at the ORM's `WriteError`. Every surface MAPS them, none REDEFINES them. The `From<ValidationErrors> for WriteError` lifter is the proof — if a new surface (Form<T>, REST, admin, custom) needs to render validation errors, it consumes `WriteError`'s accessors. New custom field-type validators declare a `Validator` variant once and flow through every surface for free.

    **Deferred** (not gating the PrimaryKey swap):
      - Validator-crate integration (`#[validate(email)]` / `#[validate(url)]` / `#[validate(range)]` attrs from the `validator` crate). The macro accepts the simple shapes today; the rich rule set lands behind a cargo feature when a real consumer surfaces a need.
      - Multipart / file-upload bodies. Current extractor is x-www-form-urlencoded only.
      - Input-preservation on re-render: today the failure branch re-renders with a default form (not the user's typed input). Pairing `axum::Form` + `Form<T>` in the same handler would give both shapes; v1 keeps the surface simple at the cost of one extra retyping on rare validation failures.

19. ~~ (the originally-open description below kept for archive trail)

    Reference: `examples/shop/src/views/public.rs:21-279` is the canonical "what every handler ends up writing today" — a bespoke `ContactForm` struct, a parallel `ContactErrors` struct with one `Option<String>` per field, a `has_any()` accessor that ORs every Option, a `validate_contact_form()` that walks the form imperatively setting error strings one by one, and a `normalize_contact_form()` that trims + lowercases before validation. ~80 lines of boilerplate for what should be 5 lines of declarative validators on the form struct.

    Three things wrong with the hand-rolled pattern:

    1. **Single-error-per-field**. `errors.name: Option<String>` can't represent "name is too short AND contains numbers" — the second error overwrites the first or the validator short-circuits. The REST plugin's `WriteError::field_errors() -> BTreeMap<String, Vec<String>>` (lives in `crates/umbra-core/src/orm/write.rs:120`) is the right shape; HTML form handling should reuse it.

    2. **Diverges from REST's shape**. The REST plugin returns `{"email": ["This field is required."], "phone": ["Bad format."]}` on validation failure (DRF-style). HTML form handlers return their own ad-hoc envelope (`ContactErrors`) that templates have to wire to per-field placeholders by hand. Same validation problem, two different solutions, two different error shapes — the framework should pick one.

    3. **No reusable rules**. `looks_like_email`, `trim()`, "is required" — every form re-implements the same checks. The validator crate (already in the Rust ecosystem) provides `#[validate(email)]`, `#[validate(length(min = 5))]`, etc. as attribute macros; integrating it is the front-runner.

    **Proposed shape — `Form<T>` extractor + structured errors**:

    ```rust
    use umbra::forms::{Form, FormErrors};

    #[derive(Form, Deserialize)]
    #[form(normalize_strings)]                  // auto-trim every String field
    struct ContactForm {
        #[form(required, length(min = 1, max = 100))]
        name: String,

        #[form(required, email)]                // validator crate's email rule
        email: String,

        #[form(length(max = 30))]
        phone: String,

        #[form(required, length(min = 1, max = 200))]
        subject: String,

        #[form(required, length(min = 10, max = 5000))]
        message: String,
    }

    pub async fn contact_submit(form: Form<ContactForm>) -> Result<...> {
        // form is Result<ContactForm, FormErrors> — either valid input
        // or the structured error map. No manual checks at the call site.
        match form.into_result() {
            Ok(valid) => { /* persist, render success */ }
            Err(errors) => render_with_errors(errors),    // ← FormErrors
        }
    }
    ```

    `FormErrors` shape:

    ```rust
    pub struct FormErrors {
        pub fields:     HashMap<String, Vec<String>>,   // per-field
        pub non_field:  Vec<String>,                    // form-level
    }
    ```

    Renderable directly in templates with `{% for msg in errors.fields.email %}<p class="err">{{ msg }}</p>{% endfor %}` — no per-field `Option<String>` plumbing.

    **Architectural rule: validation errors originate at the ORM. Every surface MAPS them, none REDEFINES them.**

    The ORM is the only layer that knows the truth about a column — its type, its NOT-NULL-ness, its UNIQUE constraints, its `#[umbra(...)]` validators, its `#[validate(...)]` rules, its FK target's existence. So the ORM's `WriteError` (`crates/umbra-core/src/orm/write.rs`) is the **canonical, structured error type** that every higher surface must use as input. Not as inspiration, not as "shape we mirror" — as the actual `serde_json`-friendly value they parse and render.

    Today the REST plugin already does this correctly: its 400 body is `WriteError::field_errors()` serialised verbatim (DRF-style `{"field": ["msg"]}`). The work this gap names is **bringing the other two surfaces in line**:

    ```text
                       ┌──────────────────────────────────────────────┐
                       │  ORM: WriteError (the single source of truth)│
                       │  - RequiredFieldMissing { field }            │
                       │  - BlankNotAllowed     { field }             │
                       │  - ForeignKeyNotFound  { field, target, val }│
                       │  - UniqueViolation     { field, value }      │
                       │  - Validator           { field, message }    │
                       │  - TypeMismatch        { field, ... }        │
                       │  - .field_errors() -> HashMap<String, Vec>   │
                       └──────────────────────────────────────────────┘
                                          │
                  ┌───────────────────────┼───────────────────────┐
                  ▼                       ▼                       ▼
            ┌──────────┐           ┌──────────────┐         ┌───────────┐
            │ REST 400 │           │ Admin form   │         │ HTML Form<T>│
            │ body     │           │ template     │         │ extractor   │
            │  (today  │           │ (gap #12)    │         │ (gap #19)   │
            │  works)  │           │              │         │             │
            └──────────┘           └──────────────┘         └───────────┘
        DRF-shaped JSON       per-field error spans      template ctx for {% for %}
    ```

      - **Re-use `WriteError::field_errors()` directly.** `FormErrors` is NOT a new type — it's a type alias / thin wrapper around `WriteError::field_errors()`'s `HashMap<String, Vec<String>>` + `non_field_errors()`'s `Vec<String>`. The validator crate's `#[validate(email)]` failure produces a `WriteError::Validator { field, message }` that flows through the same accessor. There is no second error map.

      - **HTML `Form<T>` failure path** = constructs `WriteError::Multiple { errors: vec![...] }` from validator rules, returns it via the existing accessor. Same type, same shape, just a different population source than the ORM's own.

      - **Admin form-submit handlers** = already receive `WriteError` from `insert_json` / `update_json` (today they stringify it via `sqlx::Error::Protocol` — see commit 5b163ab and gap #12). Stop stringifying; thread the `WriteError` through to the template context as `field_errors` + `non_field_errors`.

      - **REST handlers** = already do this correctly. No change.

    **Rule for plugin authors**: a new custom field type (`#[derive(Model)]` field with custom validators) declares a `Validator` variant on `WriteError` once. The error appears in REST 400s, admin form spans, and HTML form extractors with zero per-surface plumbing. That's the test of whether the unification worked.

    **Anti-pattern to forbid**: a surface inventing its own error type (`AdminFormErrors`, `ContactErrors`, `MyEndpointErrors`) and translating from `WriteError` at the boundary. The translation IS the bug; every surface's "translation" drifts independently and the framework grows three subtly-different error shapes nobody can map between. Every gap entry that mentions form errors should reference `WriteError` as the source.

    **Triggering case for v1**: `examples/shop/src/views/public.rs::contact_submit` — porting that handler to `Form<ContactForm>` would drop the file by ~80 lines (the bespoke validators and the `ContactErrors` struct disappear). Use that as the proof-of-shape consumer.

    **Estimate**: ~400 lines across:
      - `umbra-core::forms` — the trait + `FormErrors` + `Form<T>` extractor + `normalize_strings` impl.
      - `umbra-macros::Form` — derive that walks `#[form(...)]` attributes, emits a `validate(&self) -> Result<(), FormErrors>` method.
      - Validator-crate integration (feature-gated so REST-only apps don't pull it).
      - One worked example (shop's contact form) + one passing integration test.

    **Related to**: gap #12 (admin per-field rendering), features.md #51 (form validation framework — currently a stub).

18. [x] **Nested `?include=` (dotted / `__` chain) — ORM half shipped.**

    ORM-side fix landed in `crates/umbra-core/src/orm/dynamic.rs`:

    - `normalize_sr_token` accepts both `.` and `__` separators (mixed in one token is fine too) and normalises to the canonical dotted form.
    - `validate_sr_chain` walks the FK graph hop-by-hop against `registered_models()`; rejects on missing meta / unknown column / non-FK column.
    - `select_related_dyn` calls `validate_sr_chain` and silently drops invalid chains (preserves the pre-existing single-hop drop contract for power-user/internal callers).
    - `hydrate_select_related_into` rewritten to mirror `queryset::hydration::hydrate_select_related_nested`: per-hop batched IN through `fetch_related_as_json`, bottom-up embed of each level into the previous one's hop slot, then splice level-0 into the root rows. Query budget = `1 + len(hops)` per chain regardless of parent count.

    REST surface (`plugins/umbra-rest/src/lib.rs::parse_include`) now validates the same way upstream — loud 400 with the resolved chain on the failing hop (not a silent drop), depth-capped at 3 hops per the spec.

    Demo: `GET /api/post/?include=author.profile` issues 3 queries total (posts → authors IN → profiles IN); `GET /api/post/?include=author__profile` is identical; `GET /api/post/?include=author.banana` returns 400 with "?include=: unknown field `banana` on `author` (resolving chain `author.banana`)".

    **Deferred to a follow-up**: `?fields=user.profile.email` recursive sparse-fieldset walk (gap's part 4) — REST-side concern in `apply_sparse_fields`, untouched by this turn. The ORM contract is now nested-capable; the sparse-fieldset reader just needs to recurse instead of `split_once('.')`.

18. ~~ (the originally-open description below kept for archive trail)

    Today (commits f6f204a + 182703e) `?include=` and `?fields=` are ONE HOP only on the dynamic / REST path:

      - `?include=author` works; `?include=author.profile` is silently dropped.
      - `?fields=user.id` works; `?fields=user.profile.email` no-ops (the inner `profile.email` is treated as a literal key on the user object).

    The typed ORM IS nested-capable — `crates/umbra-core/src/orm/queryset/hydration.rs::hydrate_select_related_nested` handles `__`-containing names like `Post::objects().select_related("author__manager")`. One batched query per hop, no recursion, query budget = `1 + len(hops)` regardless of parent count. The dynamic side (`select_related_dyn` + `hydrate_select_related_into`) was implemented against the SINGLE-hop helper only.

    **Worked target**: `GET /api/post/?include=author.profile&fields=id,author.profile.github_url`
      - Issues 3 queries total: posts → authors (IN) → profiles (IN).
      - Returns each post with `author.profile.github_url` accessible, everything else dropped.

    **Circular cases stay safe**: `?include=user.customer.user` doesn't loop infinitely. Each hop is one query against a known target table; the framework never traverses anything the caller didn't explicitly name. The depth in the URL IS the budget.

    **Fix shape**:

      1. **URL convention — accept both `.` and `__`.** Either separator works on `?include=` and on the dotted path of `?fields=`. Both of these resolve to the same chain:

         ```
         ?include=author.profile&fields=author.profile.github_url
         ?include=author__profile&fields=author__profile__github_url
         ?include=author.profile&fields=author__profile__github_url   ← mixed is fine too
         ```

         Two reasons to accept both:
           - **`.`** is what most REST APIs use (URL-natural, no underscore-encoding considerations) and matches the dotted-fields semantic already shipped in commit 182703e.
           - **`__`** is what Django / DRF users reach for muscle-memory-wise (mirrors `Post::objects().select_related("author__profile")` from the typed side).

         Cost: trivial — a one-line normalization (`name.replace("__", ".")` or vice versa) at the top of `parse_include` + `apply_sparse_fields` before any other processing. The internal representation stays canonical; the URL-facing surface accepts both. No semantic difference, no precedence rules to remember.

         **Edge case to pin in tests**: a column NAMED with a double underscore (e.g. `meta__source`) would alias to `meta.source`. Real models don't do this, but the test should assert that an unknown-after-normalisation path returns 400 with a message that surfaces the resolved chain — gives the caller enough info to spot the collision if it ever happens.

         Internally normalise to the Django-style `__` only when calling into the existing typed `hydrate_select_related_nested` helper, since that's what it expects.

      2. **`select_related_dyn`** — accept dotted names. For each token containing a dot, validate the first hop is an FK on the current meta (existing check); look up the FK target's `ModelMeta` from `registered_models()`; validate the next hop on that meta; repeat. Reject the whole token with 400 if any hop fails.

      3. **`hydrate_select_related_into`** — for dotted names, delegate to a `hydrate_dyn_nested(target_chain, ids, rows)` helper that mirrors `hydrate_select_related_nested`'s pattern: per-hop IN query, splice each level's JSON into the prior level's object.

      4. **`apply_sparse_fields`** — recursive dotted matching. Replace the current `split_once('.')` with a real path walk: for `user.profile.email`, descend into row["user"], then ["profile"], then keep ["email"]. The "most-specific wins" rule extends naturally (any dotted token under a parent filters that parent's nested object at the named depth).

    **Day-one constraints**: depth limit (cap at e.g. 3 hops in the REST handler — pathologically deep `?include=a.b.c.d.e.f.g` is almost always a typo). Integer PK targets only on every hop (matches existing select_related constraint).

    **Estimate**: ~250 lines across `dynamic.rs` (recursive resolver + chained hydration) + `lib.rs::parse_include` (multi-hop validation) + `apply_sparse_fields` (recursive walk). Pre-existing typed helper covers the SQL pattern; the dynamic side is wiring + meta-graph traversal.

17. [x] **Playground multi-select pickers for `?include=` and `?fields=` — SHIPPED in commit `3ff8d22`.** New `plugins/umbra-playground/frontend/src/components/IncludeFieldsPicker.tsx` component renders a popover-anchored checkbox list driven by the OpenAPI spec data the SPA already has in memory (`fieldInfosFromSchema(listItem, spec)` for the resource columns + `f.fkTarget` for the FK badges + `components.schemas[fkTarget]` for nested sub-trees). Two variants:

    - `?include=` — one checkbox per FK column with a `→ <target>` badge. Empty resource → "No FK columns on this resource" message instead of a broken-feeling empty popover.
    - `?fields=` — top-level checkboxes for every column, plus a nested sub-tree per FK column with dotted notation (`user.email`, `user.username`). Cross-coupling: ticking `user.email` auto-enables `?include=user` so the response actually carries the nested keys.

    Filter box, alphabetical-sort serialization for stable URLs, lossless round-trip with the legacy free-text input shape (a user with `user,billing_address` typed in pre-fix sees both pre-checked on first open). Wired into `RequestBuilder.tsx` via a 60-line dispatch: when `p.name === "include"` or `p.name === "fields"` AND a `listItem` schema is available, render the picker instead of the plain `<Input>`. Empty-schema fallback to the free-text input so an in-flight spec fetch doesn't yield a useless greyed-out button. Vite + tsc build clean; full umbra-playground tests (8) + full workspace sweep (1243) green. Live-verified: `/api/playground/assets/index-Bus4dlCi.js` HTTP 200, 912 KiB. Original symptom preserved below:

    **The spec already publishes the data the SPA needs:**

      - `?include=` → `x-umbra-include-fks` vendor extension (commit f6f204a) is an array of FK column names the resource exposes.
      - `?fields=` → `x-umbra-fields-columns` is an array of every column name on the resource.

    Both are sitting in the OpenAPI document waiting to be read. The SPA's existing string-input renderer ignores them.

    **Fix shape**:

      1. **`?include=` picker** — checkbox group. Read `x-umbra-include-fks`, render one checkbox per FK name. Checking emits comma-joined names into the query-string state. Single click, no typing.

      2. **`?fields=` picker** — two-level checkbox tree. Top level lists `x-umbra-fields-columns`. Each FK column expands (when included via the `?include=` picker above) into a sub-tree of the FK target's columns, accessed via dotted notation (`user.id`, `user.email`, ...). Checking nested boxes emits `user.id, user.email` etc. The dotted-vs-plain semantic from commit 182703e: plain box = full nested object, any nested box = filtered to those keys.

      3. **Cross-coupling**: enabling a checkbox in the `?include=` picker should unlock the corresponding sub-tree in the `?fields=` picker (otherwise the SPA would let users tick `user.email` without realising they also need `?include=user`). Or — be helpful and auto-enable the include when a dotted field is ticked.

    **What the spec is missing for full nested rendering**: the FK target's column list. Today `x-umbra-include-fks` is just FK names; the SPA can render the include picker but not the sub-tree for fields until it also knows what columns each FK target has. Two paths:

      - **Server-side**: extend `x-umbra-include-fks` from `["user", "billing_address"]` to an object `{"user": ["id", "username", "email", ...], "billing_address": [...]}`. One extra walk through `models_for_plugin` at spec-build time; no runtime cost.

      - **Client-side**: the SPA already loads the full OpenAPI spec; it has every `components.schemas.<X>` entry. When rendering Customer's fields tree, look up the `User` schema from the spec's components for the `user` FK's nested columns. Zero server change; just SPA lookup work.

    Client-side is cleaner — the spec already has the data and we don't need a new extension. The component-resolution pattern is already in `components/EndpointDetail.tsx` for the body-schema panel.

    **Related**: gap #15 (FK include — shipped) and the dotted-fields work (commit 182703e) built the backend surface. This gap is the SPA UI making it usable without manual typing. Until shipped, users have to know columns by name; once shipped, building a request is point-and-click.

16. [x] **M2M echo on `DynQuerySet::fetch_as_json` is N+1.** — Shipped.

    `hydrate_m2m_batched(meta, pk_name, &mut rows)` in `crates/umbra-core/src/orm/dynamic.rs` runs ONE `SELECT parent_id, child_id FROM <junction> WHERE parent_id IN (...)` per registered M2M relation, then groups by parent and splices each row's `<relation>: [child_id, ...]` array. Per-row, per-relation `hydrate_m2m_into` calls were removed from `fetch_as_json`'s row loop and replaced with a single post-loop batched call. Query budget drops from `1 + N*M` to `1 + count(M2M relations)` regardless of N.

    Preserves the existing contract: parents with no junction rows still surface the field as an empty array (initialised up front before the SELECT). Mixed integer + string PK shapes both work (`pk_json_key` namespaces the group key as `n:` vs `s:` so a numeric PK and a stringified-equal PK can't collide).

    Single-row insert / update paths still use the per-row `hydrate_m2m_into` (they only have one row — batching there would be ceremony, not savings).

    Demo: `GET /api/post/?fields=id,title,tags` on a 20-post page now issues 2 queries (1 + 1) instead of 21.

16. ~~ (the originally-open description below kept for archive trail)

    `crates/umbra-core/src/orm/dynamic.rs` lines ~744 + ~766: for every row in `fetch_as_json`, if `meta.m2m_relations` is non-empty, the loop calls `hydrate_m2m_into(meta, pk, &mut entry).await?` — which runs ONE `SELECT child_id FROM <junction> WHERE parent_id = ?` per parent per M2M relation. So `GET /api/post/` against N posts with M m2m relations issues `1 + N*M` queries total.

    Pre-existing — predates the `?include=` work in commit f6f204a — but the same batched-IN pattern that fixed FK expansion fixes this too:
      - Collect all parent PKs across the N rows (already in scope).
      - For each M2M relation, run ONE `SELECT parent_id, child_id FROM <junction> WHERE parent_id IN (...)` query.
      - Group results by parent_id, splice each row's children in via the existing `hydrate_m2m_into` shape.

    Same query budget guarantee `?include=` gets: `1 + count(M2M relations)` regardless of N. The typed-ORM path already does this via `prefetch_related` (features.md #19 marked `[~]` partial); the dynamic path was left behind, same way `select_related` was before this turn.

    **Recommendation**: extract `batched_m2m_for(meta, parent_pks, &mut rows)` from the per-row helper, fan out per-relation IN-queries, splice results. ~80-line PR mirroring the FK expansion shape from commit f6f204a. Worth doing in the same revisit window — the two helpers (`hydrate_select_related_into` for FKs, the new `hydrate_m2m_batched` for M2Ms) sit next to each other in dynamic.rs and share the "collect ids → batched IN → splice" pattern.

    **Triggering case to fix**: `GET /api/post/?fields=id,title,tags` on a 20-post page currently issues 21 queries (1 + 20). Should be 2 (1 + 1).

14. [x] **Template-side reverse-O2O / forward-FK traversal on `user` — Shipped.**

    `user_context_layer` in `plugins/umbra-auth/src/session_user.rs` now expands relations on the serialized user up to depth 2, recursively, with `(table, pk)` cycle detection. Templates can write `{{ user.customer.loyalty_points }}` directly and get the resolved value — no handler-side prefetch declaration, no `tokio::Handle::block_on` ceremony, no template-level `.await`.

    What's expanded at each hop:
      - **Forward FKs**: every FK column on the current row is replaced with the full target row (mirrors the dynamic `select_related_dyn` semantics from gap2 #15).
      - **Reverse-O2O**: every other registered model with a UNIQUE FK pointing at the current table gets injected under the child's table name as the key (`Customer { user: FK<AuthUser> (unique) }` → `user.customer`). Naming follows Django's lowercase-model convention.
      - **Skipped**: M2M arrays (different shape; pre-resolving every parent's tag set on every request was the wrong trade-off), reverse-FK arrays without UNIQUE (one-to-many; same reason).

    Query budget per authenticated request: `1 (user) + count(relations within depth 2)`. For the shop's `AuthUser`, that's `1 + 1 (customer)` = 2 queries — the second hop walks back into `auth_user` from `customer.user` and hits the cycle guard, so no extra query.

    Always-on once `.with_user_in_templates()` is set; the depth is fixed at 2 via `USER_RELATION_DEPTH` constant. Anonymous requests stay on the cheap path (no expansion, just the `{ is_authenticated: false }` sentinel).

    Verified live (shop, shopadmin session):
    ```
    user.username = shopadmin
    user.customer is defined = true
    user.customer.id = 1
    user.customer.loyalty_points = 0
    user.customer.phone = +15555550100
    user.customer.user.username = (stopped — cycle detected)
    ```

    **What's still not shipped** (intentional out-of-scope for this turn):
      - `request` namespace (Django's `request.user.X`) — umbra exposes `user` directly; adding `request` would mean materializing a per-request context object in templates. Worth a separate gap entry if anyone needs Django-shape compatibility.
      - Reverse-FK arrays (`user.orders` returning all of a user's orders). Different cardinality, different cost model — the M2M-style fan-out cost would be wrong for the "every authenticated request" budget. Keep this opt-in via handler-side prefetch.
      - Custom `UserModel` impls (non-AuthUser): the expansion currently hard-binds to the `auth_user` table lookup in `serialize_authenticated_with_relations`. A custom user would need its own middleware variant or a generalised hook. Backlog if a custom-user app surfaces.

14. ~~ (the originally-open description below kept for archive trail)

    A user wrote `{{ user.customer.id }}` in a template, expecting the Django shape `{{ request.user.profile.email }}` — where you walk the related object directly in the template. It failed because:

    1. **`user` in templates is the JSON-serialized AuthUser** (commit `bd48bf8` shipped this — see [auth/user-in-templates.mdx](/v0.0.1/auth/user-in-templates)). Reverse-OneToOne accessors like `user.customer().await` are Rust async methods; they don't survive serialization to `serde_json::Value`.
    2. **Templates can't `.await`** — minijinja is sync. So even if we exposed a method, the template couldn't drive the DB read.
    3. **Pre-resolving every possible relation** on every request would be wasteful (a typical AuthUser has 10+ reverse-FK candidates, none of which most pages need).

    Today the canonical workaround is what the shop's `/me` handler does — resolve the relation in Rust, stuff the resolved value into the template context explicitly:

    ```rust
    let customer = user.0.customer().await?;
    let customer_id = customer.as_ref().map(|c| c.id);
    render("me.html", &context!(username, customer_id))
    ```

    Template: `{{ customer_id }}` — works, but the handler had to know in advance what the template needs.

    **Possible shapes for a real fix** (none ship today; this is the design space):

    - **A) Synchronous resolver registration**. Templates expose a callable `user.related("customer")` that the framework satisfies by enqueueing the lookup and blocking on it via a `tokio::runtime::Handle::block_on`. Works but couples templates to the runtime; very easy to misuse (one big N+1 risk).
    - **B) Eager prefetch declaration in the handler**. `render_with_prefetch("me.html", user.with_prefetch(&["customer", "orders"]))` declares which relations the template will walk; the framework pre-resolves before rendering. Honest about cost (the handler picks what to load), Rust-typed, no runtime surprises. Closest fit to Django's `select_related` + `prefetch_related` pattern.
    - **C) Custom minijinja function**. `{{ resolve(user, "customer").id }}` — explicit "I'm hitting the DB here," same block_on machinery as A but no surprise (the call is visible in the template).

    **Recommendation**: ship B first. It's honest about cost, integrates with the existing `select_related` / `prefetch_related` ORM surface, and the handler-declared prefetch list is greppable. Defer A / C until a real consumer surfaces — most templates need the same 3-4 relations across most pages anyway, and the handler is the right place to declare them.

    **Why not just throw away the request**: the user's expectation ("walk relations like Django does") is right — it's the framework's job to figure out HOW to deliver that within Rust's constraints. The async-sync gap is the obstacle; the prefetch declaration is the answer.

    **For now**: documented as a manual handler-side resolution in [auth/user-in-templates.mdx](/v0.0.1/auth/user-in-templates) and [orm/relationships.mdx](/v0.0.1/orm/relationships). Users hitting the wall here should reach for option (B)'s ergonomics via the existing `OneToOne<C>` parent-side field + `select_related`.

13. [x] **Admin form success: no toast + no table refresh after sheet-create / sheet-edit.** Reported 2026-06-09 after a clean Customer create — the request succeeded, the row landed in the DB, but the user saw nothing happen on the page:

    - **No success toast.** A failed submit fires a clear inline error (commit `5b163ab`), but a successful submit is silent. The user can't tell the sheet's submit handler from the sheet's "I closed without saving" handler.
    - **No row in the list.** After the create, the sheet closes (or doesn't — see below), but the changelist behind it still shows the pre-create page. The user has to hit refresh to see the row they just created.
    - **Sheet stays open / closes inconsistently.** Depending on whether the user clicked "Save" vs "Save & continue editing," the sheet behaviour differs in opaque ways.

    **Root cause**: the sheet's create handler (`plugins/umbra-admin/src/handlers/sheet.rs` + `handlers/crud.rs::create_post`) returns either a `Redirect::to(...)` on success or the re-rendered form on error. The Redirect is the wrong shape for an HTMX sheet — HTMX follows the 303, swaps the redirect target's body into `#umbra-sheet-slot`, and the user's browser sits on a janky half-state. There's no first-class "success → close sheet + toast + refresh table" path.

    **Fix shape**:
      - Success response sets `HX-Trigger` header with two events: `umbra:rowCreated` (carries the new row id) + `umbra:showToast` (carries the message). The wrapper.html JS already has `showToast` listener wiring (line ~1212); add a `rowCreated` listener that:
        - Closes the sheet (`umbra.closeSheet()`).
        - Triggers an `htmx.trigger('#changelist-table', 'umbra:reload')` so the table fetches the new page.
      - Save vs Save&Continue: Save closes; Save&Continue keeps the sheet open BUT swaps the body to the edit form for the just-created row (HX-Location to the edit-sheet URL with `hx-target=#umbra-sheet-slot`).
      - The same shape applies to edit/update: success → toast "Updated" + reload the row's `<tr>` in place (the existing row-update HTMX pattern in `rows_fragment.html` already supports this).

    **Why HX-Trigger over JS**: keeps the contract server-side. The handler decides "this was a success → fire these events"; the JS is generic listener wiring. A future custom action (mark order as shipped, etc.) reuses the same `umbra:showToast` listener without touching the handler-side ergonomics.

    **Related**: gap #12 (per-field error rendering) and this success path land naturally in the same refactor — both touch the form-submit handlers and both depend on a structured `WriteError` flowing through the response, not a stringified one. Both could ship in one focused commit.

11. [x] **Persist all admin UI state into `AdminUserPref` — filters, sort orders, page sizes, search, per-table preferences.** SHIPPED in two commits: `89fba96` (round 1: schema + helpers + filter/search/sort/per_page round-trip) and `d569fcb` (round 2: last_path, column visibility, widget period overrides). `AdminUserPref` grew `preferences: Option<String>` — one nullable TEXT column, one auto-generated migration (`0002_add_admin_user_pref_preferences`), zero backfill. JSON blob shape (`tables.<table>.{filters,search,sort,per_page,hidden_cols}` + `last_path` + `dashboard.widget_periods.<key>`) sits behind seven typed helpers in `plugins/umbra-admin/src/models.rs`: `get_table_pref` / `set_table_pref` / `toggle_table_col` / `get_last_path` / `set_last_path` / `get_widget_period` / `set_widget_period`. Read-modify-write merges throughout so sibling keys coexist (pinned by `last_path_coexists_with_table_pref_writes`). Three handler wirings:

    1. **Changelist (`/admin/{table}/`)** — paramless visit + saved prefs → 303 to the saved URL; every render writes `(filters, search, sort, per_page)` and `last_path`; render-time `display_cols` filter against `hidden_cols`. `POST /admin/{table}/columns/{column}/toggle` flips visibility and returns `HX-Trigger: refreshTable + showToast`.
    2. **Admin index (`/admin/`)** — first visit AFTER a changelist visit 303-redirects to the user's `last_path`. Opt-outs: `?dashboard=1` (explicit dashboard intent) and HTMX requests (the dashboard's own widget hx-gets pass through this handler).
    3. **Dashboard widget data** — period resolution priority: URL `?period=` → saved `preferences.dashboard.widget_periods.<key>` → widget's registration-time `default_period`. URL `?period=` writes the user's override as a side-effect so chip clicks become sticky cross-tab / cross-device.

    `plugins/umbra-admin/tests/phase4_user_prefs.rs` carries 12 tests (was 3); 9 new pins cover every helper + their interactions (per-table namespacing, malformed-JSON-graceful, idempotent toggle, sibling-coexistence). Full umbra-admin suite: 128 passed.

    What's deliberately deferred as separate items: per-table density override (low value vs. the global density toggle); `favorites` sidebar pinning (no consumer yet); the changelist front-end UI affordance for column visibility (a chip strip / dropdown in the header would invoke the toggle endpoint — storage + endpoint are in place, just the template surface left). Original symptom + design preserved below: `plugins/umbra-admin/src/models.rs:43` already has `AdminUserPref { theme, density, sidebar_collapsed, dashboard_layout, updated_at }` and the upsert plumbing (`fetch_or_default`, `upsert`). Only two pieces of UI state currently round-trip through it: theme + sidebar-collapsed (typed columns) and the dashboard layout (a JSON blob in `dashboard_layout: String`). Everything else lives in the URL and dies on refresh / new tab / restart:

    - Active list filters (`?filter_status=active&filter_brand=acme`).
    - Search input (`?search=widget`).
    - Sort column + direction (`?sort=-created_at`).
    - Page size (`?per_page=50`).
    - Page number (`?page=3`).
    - Per-table column visibility (the "hide cost column" affordance from feature #57).
    - Density override per-table (a designer might want the products table compact even when the rest is comfortable).
    - Last-viewed model (so `/admin/` can land them back in the table they were working on instead of the dashboard).

    Symptom: an admin filters Products to `status=active, brand=acme, sort=-price, per_page=50`, opens a product to edit it, hits "Save and continue editing," and lands back on a Products list that's lost every filter — the URL the form-action POST'd to doesn't carry the changelist's query string, and the redirect-after-save goes to the canonical `/admin/product/` with no params. Today the user re-applies the four filters by hand every time.

    **Proposed shape**: extend `AdminUserPref` with a `preferences: serde_json::Value` field — a free-form JSON map keyed first by feature/table, then by setting:

    ```jsonc
    {
      "tables": {
        "product": {
          "filters":      { "status": "active", "brand": "acme" },
          "search":       "widget",
          "sort":         "-price",
          "per_page":     50,
          "hidden_cols":  ["cost", "external_id"],
          "density":      "compact"          // overrides global
        },
        "order": {
          "filters":      { "status": "shipped" },
          "per_page":     20
        }
      },
      "dashboard": {
        "widget_periods": {
          "shop_daily_sales_chart": "7d",    // overrides Widget::default_period
          "shop_activity_chart":    "30d"
        }
      },
      "last_path": "/admin/product/?filter_status=active",
      "favorites": ["/admin/product/", "/admin/order/"]
    }
    ```

    Read path: the changelist handler reads `preferences.tables.<table>` on first hit and rewrites the URL with the persisted query params (HTTP redirect, so the URL is the source of truth from there on). The dashboard's widget chip strip reads `preferences.dashboard.widget_periods.<key>` instead of `Widget::default_period` when present (the existing `default_period` becomes a "first-ever-visit" default).

    Write path: every URL-state-changing interaction (filter chip click, sort header click, page-size dropdown, search submit, widget period chip) fires an HTMX `hx-trigger="every change"` to `POST /admin/api/prefs/tables/<table>` (or `.../dashboard/widget_periods/<key>`) with the new value. The handler debounces (~500ms client-side via `hx-trigger="change delay:500ms"`) and merges into `preferences` via `JSON_SET` (Postgres) / `json_replace` (SQLite) so two tabs filtering different tables don't clobber each other.

    **Why JSON blob over typed columns**: the surface grows. Every new admin feature adds at least one piece of per-user state (column visibility, density per-table, favorite filters, saved searches with a name, dashboard widget overrides per period, etc.). Each typed column requires a migration + a typed accessor; the JSON path is one migration + a `serde_json::Value` to read into. The framework still gets to type-check what it cares about — the existing `theme` / `density` / `sidebar_collapsed` columns stay typed for the global cross-page settings; `preferences` is for the per-table / per-widget state where churn is highest.

    **What this unblocks**:
      - Feature #8 (per-user widget reordering) — `preferences.dashboard.widget_order: ["shop_total_sales", "shop_orders", ...]` is the natural place.
      - "Save and continue editing" landing back on the filtered changelist instead of the canonical URL.
      - Cross-device continuity — a user filtering on their laptop sees the same filters on their phone, because the URL gets rehydrated server-side.
      - Per-table "Show me what I last saw" — `last_path` redirect at `/admin/`.

    Migration: add `preferences: serde_json::Value` (Postgres `jsonb`, SQLite `TEXT` storing JSON) to `AdminUserPref`. Default `{}`. The existing `dashboard_layout: String` field could fold into `preferences.dashboard.layout` long-term but stays typed for one release as a compatibility shim — gives the framework's own dashboard-layout code one cycle to migrate without breaking on-disk rows.

9. [x] **`render_500` swallows secondary template errors silently** — Fixed. `render_500` in `crates/umbra-core/src/errors.rs` now `match`es on the recovery template's render result instead of `.ok()`-ing it. Secondary failures get `tracing::error!`'d with the template name + the failure cause + a hint about the likely `{% extends "wrapper.html" %}` chain bug. In dev mode the body of the plain-text fallback includes both errors so the developer sees the recovery failure inline instead of having to grep logs.

    **Sibling issue (also fixed)**: the 500-rendering path runs OUTSIDE the user-context middleware's task-local scope, so `user` was undefined in the recovery template even when AuthPlugin's middleware was mounted. Fixed in `crates/umbra-core/src/templates.rs::merge_ambient_user` by injecting an anonymous-user sentinel (`{ is_authenticated: false, is_staff: false, is_superuser: false }`) when no task-local is set — so the 500 template gets a defined `user` to evaluate against regardless of where in the layer stack it renders.

    Originally found while debugging the `request.user.is_staff` template error in `examples/shop/templates/wrapper.html:37`. Re-hit while debugging `request.user.customer.id` in `me.html:19` — same chain (handler 500s → 500.html extends wrapper.html → secondary failure → silent plain-text fallback). Both halves of the chain now behave correctly: the wrapper renders cleanly with the anonymous-user fallback, and any further secondary failure surfaces with full diagnostics.

7. [x] **Wire `AuthPlugin::with_user_in_templates()`** — Shipped. The builder method lives on `AuthPlugin` and flips a `user_in_templates: bool` field; the impl uses `Plugin::wrap_router` (which was already there — no new framework hook needed) to mount `user_context_layer` on the full merged router. Templates now write `{% if user.is_staff %}` cleanly; anonymous requests see `{ is_authenticated: false }` and the link hides; staff requests see the populated context and the link surfaces. Shop wired with `.with_user_in_templates()` on its AuthPlugin call. The doc-comment-references-a-nonexistent-method case is what triggered the new "Fix, don't patch" CLAUDE.md rule.

28. [x] **`allowed_hosts` request-time enforcement — SHIPPED.** `crates/umbra-core/src/hosts.rs` adds a `host_guard` middleware wired automatically by `App::build()` (outermost layer, after CORS) that rejects a request whose `Host` header isn't in `settings.allowed_hosts` with a **400** — the request-time half of Django's `ALLOWED_HOSTS` that defends against Host-header injection (cache poisoning, poisoned password-reset / absolute-URL links). Enforced **only in `Environment::Prod`** (dev passes through so localhost / LAN-IP / tunnel hosts don't 400 mid-iteration). Pattern matching mirrors Django: exact (`example.com`), subdomain wildcard (`.example.com` → domain + any subdomain), and `*` (allow-any escape hatch / "disable"); port and IPv6 brackets are stripped from the Host value. Driven by the existing `settings.allowed_hosts` (default `["localhost","127.0.0.1"]`, `UMBRA_ALLOWED_HOSTS` / `umbra.toml`) — pairs with the pre-existing `check.rs::settings_allowed_hosts` boot warning. 7 tests in `hosts.rs` (exact / case-insensitive / subdomain / star / port+IPv6 stripping / middleware 400-vs-200 in prod / dev pass-through). NOT CORS — that's browser cross-origin response policy; this is server-side Host validation.

29. [x] **CORS path scoping — SHIPPED.** `AppBuilder::cors_for(prefix, CorsConfig)` (`crates/umbra-core/src/app.rs`) layers CORS only onto requests whose path starts with the prefix (e.g. `"/api"`), leaving HTML pages same-origin — the django-cors-headers + DRF shape. Implemented as a `ScopedCorsLayer`/`ScopedCors` tower layer in `cors.rs` that holds both the CORS-wrapped and bare inner service and dispatches per `req.uri().path()`. Batch `allow_origins(vec![...])` also shipped. The shop now uses `.cors_for("/api", CorsConfig::strict().allow_origins(vec![...]).allow_credentials(true))`. Pinned by `cors::tests::scoped_cors_only_affects_matching_prefix` (a `/api` request gets `access-control-allow-origin`, a non-`/api` request doesn't). Global `.cors(CorsConfig)` retained for whole-app policies. Remaining (deferred, low priority): nested/regex path patterns and folding CORS into a dedicated `umbra-cors` plugin for config-struct symmetry with `umbra-security`.
26. [x] **Signed/session-bound CSRF (`SecurityConfig::signed_csrf`) is now the default — SHIPPED in commit `f145daf` (2026-06-10), with the admin's mint unification in `38ff747`.** The original blocker ("the admin mints raw tokens via `generate_token`, a signature-requiring middleware would 403 admin login") was removed by making the middleware the ONLY mint: `csrf_middleware` now mints **before** the handler runs on safe methods, scopes the token into the new `umbra::templates::CURRENT_CSRF` task-local (commit `08f6ce2`), and the admin's `ensure_csrf_token` prefers that ambient token over self-minting (self-mint survives only for SecurityPlugin-less deployments, where the admin's own `login_post` comparison — now constant-time via the newly-`pub` `umbra_security::tokens_match` — is the validator). The deploy-safety mechanism for the flip is **rotation**: `CsrfState::token_acceptable` rejects any cookie token that can't pass signed-mode validation (typically a pre-upgrade unsigned cookie) and the middleware re-mints + re-sets it on the next safe request, so existing browsers converge instead of 403ing. Also fixed in the same commit: the middleware's `Set-Cookie` attach switched from `insert` (which clobbered handler-set cookies, e.g. the session cookie) to `append`; `ensure_csrf_cookie` + the `response_sets_csrf_cookie` deference logic were deleted outright. Pinned by 6 integration tests in `plugins/umbra-security/tests/csrf_flow.rs` (first-visit pre-handler mint, append-not-replace, POST re-render scoping, 403 mismatch, unsigned→signed rotation, no-rotation for valid signed cookies) and the rewritten `middleware_token_wins_over_handler_minted_cookie` in `tests/integration.rs`. Design: `docs/decisions/2026-06-10-automatic-csrf.md`. Original directive preserved below:

    Make signed/session-bound CSRF (`SecurityConfig::signed_csrf`) the default once every token-minting path uses the signed mint. SHIPPED as opt-in (HMAC-SHA256 over the random token keyed by `secret_key`, optional `session_bind_cookie`; closes the AUTH-7 sibling-subdomain cookie-injection gap and finally gives `secret_key` a job). It defaults off because the admin mints raw tokens via `umbra_security::generate_token()` (`plugins/umbra-admin/src/auth.rs` `ensure_csrf_token`) and a signature-requiring middleware would 403 admin login. Route the admin's mint through a shared signed mint (e.g. expose `umbra_security::mint_csrf(&headers)`), then flip the default to on.

42. [x] **FK save binds text not bigint — SHIPPED (Plan B, 2026-06-11).** Saving a foreign-key value through the dynamic JSON write path hit `column "plugin" is of type bigint but expression is of type text` (Postgres) because `json_to_sea_value`'s `ForeignKey` arm (`crates/umbra-core/src/orm/write.rs`) bound *every* string-valued FK id as `SeaValue::String` (TEXT). The function's signature was `(SqlType, &JsonValue, bool, &str)` — it had **no access to `fk_target`**, so it couldn't tell an i64-PK FK from a String-PK one and defaulted any `JsonValue::String` FK id to text. The earlier WIP's `form_str_to_sea_value` FK arm (`orm/dynamic.rs:1954`) was already correct because it short-circuits FK before calling `json_to_sea_value` and resolves via `fk_target_pk_sql_type`; the bug lived on the JSON path (`DynQuerySet::insert_json`/`update_json`) and latently on the typed `create` path (`build_insert_one_for`).

    **Fix (root-cause, not a patch):** threaded the resolved target-PK `SqlType` into `json_to_sea_value` as a new `fk_target_pk: Option<SqlType>` parameter. The FK arm now coerces against it — `Some(Text)` binds the id as text, `Some(Uuid)` parses+binds a UUID, and a numeric-PK target (or unresolved `None`, the common i64 case) coerces the string/number → `BigInt` via the existing `coerce_i64` (which already accepts `JsonValue::String("1")`). Every caller across the workspace was updated to pass the hint: the dynamic paths (`insert_json`/`update_json` in `orm/dynamic.rs`) pass `fk_target_pk_sql_type(col)`; the typed paths (`build_insert_one_for`/`build_insert_many_for` in `orm/queryset/write_helpers.rs`, plus the `update_values` / bulk-update / save paths in `orm/queryset/mod.rs`) pass a new `pub(crate) fk_pk_hint(field)` helper resolving `FieldSpec.fk_target` → `pk_meta_for_table`; PK-bind call sites and the umbra-admin junction binds (which pass the PK's own `SqlType` directly, never `ForeignKey`) pass `None`. Non-FK behavior is byte-for-byte identical (the new arm only triggers on `SqlType::ForeignKey`).

    **Tests (`crates/umbra-core/tests/fk_save_coercion.rs`):** four pins. The deterministic SQLite proof is `json_fk_arm_coerces_numeric_string_to_bigint` — pre-fix it returned `SeaValue::String(Some("1"))`, post-fix `SeaValue::BigInt(Some(1))` (and a `Some(Text)` target still binds text). Three behavioral round-trips drive the REAL public paths — `insert_json` with `{"parent": "1"}`, typed `Manager::create` with `ForeignKey::new(id)`, and `insert_form` with a string FK id — each seeds an i64-PK parent, writes the child, reads it back, and asserts `child.parent.id()` + `resolve().name` link the real parent row. NB: on SQLite a TEXT bind into an INTEGER column is silently corrected by column affinity, so the round-trips can't go red on SQLite (only Postgres rejects outright); the unit-level `SeaValue` assertion is what makes the bug deterministic on SQLite. The typed + dynamic parallel pins keep the two write paths from diverging.

    Verified: `cargo build`, `cargo test -p umbra-core` (each binary green in isolation; the only aggregate-run failures are the pre-existing gaps2 #30 cross-binary contention — `json_form_parse`/`annotate_count`, which pass alone and don't touch the FK path), `cargo test -p umbra-admin` (32+ tests, the original failing consumer), `cargo test -p umbra-rest`.

39. [x] **`annotate_count` follow-ups: child-side filters + child soft-delete; Form derive auto-skips `ReverseSet` — SHIPPED (Plans A + D, 2026-06-11).**

    **(a) Child-side filters + soft-delete awareness (Plan D).** `soft_delete: bool` was lifted onto `ModelMeta` (`crates/umbra-core/src/migrate.rs`, `#[serde(default, skip_serializing_if = "is_false")]` so old migration snapshots still deserialize) and onto `ReverseFkRelationSpec` (`orm/model.rs`), both filled by the Model derive / `ModelMeta::for_` from the child model's `Model::SOFT_DELETE` (commits `bd21c79`, `bd99808`). `annotate_count` now auto-folds `AND <child>.deleted_at IS NULL` into the correlated subquery when the resolved child relation is soft-delete (`1fc971b`). A new `annotate_count_where::<C: Model>(alias, relation, Predicate<C>)` renders a typed child predicate into the same subquery WHERE alongside the FK correlation — Django's `Count("comments", filter=Q(moderation="visible"))` (`1dd3961`). M2M relations also annotate (count junction rows). The umbra.dev homepage moved to `annotate_count_where::<PluginComment>("comment_set_count", "comment_set", plugin_comment::MODERATION.eq("visible"))` so it counts visible-only, soft-delete excluded automatically (`a518803`). Behavioral tests in `crates/umbra-core/tests/annotate_count.rs`: soft-deleted child excluded (3→2 via the real `delete()` path), filtered visible-only count, M2M junction count, zero-child parent still returned as 0. The generic predicate's bare child columns resolve to the subquery's innermost FROM (the child table) — verified sound in review.

    **(b) Form derive auto-skips `ReverseSet` (Plan A).** `#[derive(Form)]` previously rejected a `ReverseSet<C>` field with the unsupported-type error, forcing a manual `#[umbra(noform)]`. `expand_form`'s `form_is_reverse_relation` now skips both `ReverseSet<C>` AND reverse `OneToOne<T>` (the `#[sqlx(skip)]` variant) before type classification — they are never user-submittable by construction, mirroring what the Model derive already does for `FIELDS` (commit `27ce2ec`). Test `reverse_relations_absent_from_fields` asserts both a `ReverseSet` and a reverse-O2O field compile without `#[umbra(noform)]` and are absent from the derived `fields()`. This is what let `PluginComment` re-derive `Form` cleanly.

    Both halves verified green; the holistic branch review (1363 tests, 0 real failures) cleared it.

40. [x] **Foreign keys work with `#[derive(Form)]` (`ModelChoice`) — SHIPPED (Plan A, 2026-06-11).** `umbra_website/plugins/plugin_directory/src/models.rs` `PluginComment` (the ln-343 case) had its `Form` derive commented out as an admin workaround because the derive couldn't handle FK fields. Now a `ForeignKey<T>` (or `Option<ForeignKey<T>>`), and equally a forward `OneToOne<T>` (a unique FK), become a `ModelChoice` form field — Django's `ModelChoiceField`. The submitted id is parsed to the FK target's PK kind (`PkKind` resolved from the registry via `pk_kind_for_table`); `FormValidate` went async (`#[async_trait]`) so `validate()` verifies the referenced row exists through the ORM (`DynQuerySet::for_meta(meta).filter_eq_string(pk, id).count()`) before insert, and `render_html` fetches `(id, label)` rows to emit a populated `<select>` (label = first non-PK text column). Commits: `2ede012` (async `FormValidate`), `4aa060e` (FK/forward-O2O → `ModelChoice` + a gated `ForeignKey<T>: Default`), `41b0106` (async existence check), `382ba8f` (async render fetches options), `844eabc` (re-enable `derive(Form)` on `PluginComment`, delete the hand-rolled `Default`). A latent footgun from `ForeignKey<T>: Default` (a non-nullable FK left at the id-0 placeholder being silently inserted off the form path) was closed at the contract level by a zero-FK insert guard in `build_insert_one_for`/`build_insert_many_for` that returns a clear `WriteError` instead of persisting a dangling `FK = 0` (`b9a80ca`) — without breaking the REST `FK=0`→"not found" client contract (the guard lives in the INSERT builders, not the validation layer). Behavioral tests in `crates/umbra-core/tests/form_fk.rs` (FK round-trip + `resolve()` returns the real parent; nonexistent id → field error + no row; forward-O2O UNIQUE violation) and the `PluginComment` acceptance test. The parallel M2M `ModelMultiChoice` (junction write) + choices `Select` shipped in the same plan.

45. [x] **Seamless reverse-FK relations (Django parity) — SHIPPED (2026-06-11).** "The ORM should automatically handle reverse keys for all the FKs" — both halves now work with ZERO declaration on the parent.

    **(a) Annotate auto-discovery (`44165c2`).** `annotate_count` / `annotate_related` resolve a reverse relation even when the parent declares no `ReverseSet` field: `discover_reverse_relation::<T>()` scans `registered_models()` for any child `Column` whose `fk_target == Some(T::TABLE)` and feeds the same `(child_table, fk_column, parent_table, parent_pk)` + `child_soft_delete` tuple the declared path uses. The caller's relation string matches the child's conventional name forms (table, `snake_case`/lowercase struct name, each `+_set`). All five aggregate kinds (count/sum/avg/min/max) work over the discovered relation (verified `sum=60/avg=20/min=10/max=30`); auto-discovered soft-delete children are excluded; declared `REVERSE_FK_RELATIONS`/`M2M_RELATIONS` always take precedence; >1 candidate (two child models, or one child with two FKs to the parent) → a clear ambiguity error directing to a declared `#[umbra(reverse_fk=…)]`; an unknown name lists the auto-discoverable children. Tests: `crates/umbra-core/tests/annotate_autodiscover.rs` (6).

    **(b) Instance reverse accessor (`918baaf`).** A generic, zero-declaration accessor on every model instance: `parent.reverse::<Child>()` returns a chainable `QuerySet<Child>` of children whose FK points at this instance; `parent.reverse_via::<Child>("fk_col")` disambiguates when the child has >1 FK to the parent. Implemented as a `ReverseRelations: Model + HydrateRelated` trait with a blanket impl (every `#[derive(Model)]` type gets it free), re-exported through the facade + prelude. FK discovery scans `Child::FIELDS` for `fk_target == Self::TABLE` (exactly-one / zero→`NoForeignKey` / many→`Ambiguous`); the instance PK is read via `HydrateRelated::pk_i64` (i64 is the documented v1 constraint, non-i64 → `NonI64Pk`); the typed filtered queryset is built from the runtime column name via the crate-internal `Predicate::new` (why this lives in core, not a plugin). Tests: `crates/umbra-core/tests/reverse_accessor.rs` (6) — returns the right children, chainable `.filter()/.count()`, zero-children → empty not error, ambiguity → `reverse_via`, and agreement with the declared `prefetch_related("child_set")` path.

    **The one Rust limitation (not laziness):** the literal `post.comment_set` *named attribute* still needs a declared `ReverseSet` field, because a proc-macro on the parent can't enumerate children defined in other modules/crates (macros are local; coherence forbids cross-crate inherent-method injection) — the same constraint that makes Django resolve reverse accessors at runtime via its app registry. The CAPABILITY (annotate over, and fetch, reverse-related rows with no declaration) is fully delivered via the generic accessor. Docs: `orm/aggregates.mdx` (auto-discovery) + `orm/relationships.mdx` (`instance.reverse::<Child>()`).

47. [x] **M2M junction write batched into one multi-row INSERT — SHIPPED (`aaa3afa`, 2026-06-11).** `set_junction_dynamic` (`crates/umbra-core/src/orm/m2m.rs`, both SQLite + Postgres arms) previously ran one DELETE then a loop of one `INSERT` per child id. Now: 1 DELETE + 1 multi-row INSERT (repeated `values_panic([...])` on a single `InsertStatement`), M INSERTs → 1. Empty-children builds no INSERT (the statement is `Option<InsertStatement>` = `None`; the DELETE still runs) so it never emits `INSERT … VALUES ()`; `OnConflict::columns([parent_id, child_id]).do_nothing()` preserved; same enclosing transaction + DELETE-by-parent semantics. Behavioral test `set_junction_dynamic_batched_insert_round_trip` ({1,2,3} → re-set {2,3,4} → in-batch dup {5,5,6}→{5,6} → empty clears). Admin M2M write path (same helper) + `m2m_round_trip`/`dyn_m2m_batched` all still green. Throughput-only; correctness was already fine.

48. [x] **`validate_multi_fk_exists` no longer masks a DB error as "id not found" — SHIPPED (`4d14a0f`, 2026-06-11).** `crates/umbra-core/src/orm/forms_runtime.rs` did `…fetch_as_json().await.unwrap_or_default()`, so a real DB error silently produced an empty row set → every submitted M2M id flagged "id has no matching record" (a bogus per-field flood masking the real failure, the error swallowed). The macro-generated `FormValidate::validate -> Result<Self, ValidationErrors>` can't carry a `sqlx` error cleanly, so the fix replaces the `unwrap_or_default` with a `match`: on `Err` it `tracing::error!`s the real error (field, target_table, error) AND records exactly one NON-FIELD error ("could not validate `<field>` references (database error)"), returning before the per-id loop — fail-closed but honest, never the per-field flood. The genuine-miss path (query succeeds, id truly absent) is untouched and still emits the per-field error. Tests (`crates/umbra-core/tests/form_m2m.rs`): `m2m_validation_db_error_is_non_field_not_per_id_misses` (a registered-but-uncreated `Phantom` table → IN-query fails → empty fields bucket + one non-field "database error" + no staged ids; fails against the old code) and `m2m_validation_genuine_miss_still_per_field`. Closes the `.ok()`/swallow anti-pattern CLAUDE.md flags.

49. [x] **Facade re-exports `with_actor` / `current_actor` from `umbra::signals` — SHIPPED (2026-06-11).** Surfaced auditing `orm/signals.mdx`: the doc's `use umbra::signals::with_actor;` didn't compile because the facade module `umbra::signals` (`crates/umbra/src/lib.rs`) only re-exported `subscribe`/`subscribe_async`/`emit`/`clear_for_tests`, while `with_actor`/`current_actor` (the actor task-local helpers, `umbra_core::signals`) were reachable only through the internal crate. Fix (same family as the `JoinKind` facade gap): added `with_actor, current_actor` to the `umbra::signals` re-export list, and restored the signals doc examples to the clean `use umbra::signals::with_actor;` path (dropping the `umbra_core::signals` workaround + the `gaps2 #49` breadcrumb). Facade builds green.

46. [x] **Session plugin created a DB row per cookie-less request (3 rows on a fresh browser load) — SHIPPED (`cc87676`, 2026-06-11).** Root cause: `session_layer` (auto-applied to the WHOLE router via `SessionsPlugin::wrap_router`, so it covered favicon/CSS/JS too) eagerly called `create_session(None, None)` — an immediate INSERT — on every cookie-less request, on entry. A fresh browser fires several parallel cookie-less requests (page + favicon + assets); none had received the `Set-Cookie` yet, so each independently INSERTed an anonymous session row → the "random 3 entries." (The "login didn't add a session" half was already correct: `login_user_id` destroys the anon row + creates the authed one, net zero — which is why the user said "the first part is the wrong one.") Eager creation fundamentally can't avoid this — parallel cookie-less requests share no key to dedup on. Fix: Django-style **lazy** creation in `plugins/umbra-sessions/src/lib.rs`. (a) Middleware mints a candidate token **in memory only** (`Uuid::new_v4`, no INSERT) on a cookie-less/stale request, injects it, and on exit sets `Set-Cookie` only if a row actually materialized (`read_session(&token).is_some()`) and the handler didn't already set one. (b) `set_data` changed from "row missing → silent no-op" to **create-if-missing** (id `= hash_token`, anonymous, default TTL), with `WriteError::UniqueViolation` race handling that re-reads + falls through to the UPDATE. Net: a no-write request (favicon/asset/anonymous page) creates **0 rows and no cookie**; the first real write (flash message or login) materializes exactly **1** row, reused thereafter via the cookie. Session-fixation defense, hash-on-write, `current_user`/`current_session`/`Messages` extractors, and `without_auto_layer()` all unchanged (verified). Tests (each in its own binary for ambient-pool count isolation): `tests/lazy_session.rs::cookieless_requests_to_non_writing_handler_persist_no_rows` (3 cookie-less no-write requests → 0 rows, RED at 3 before the fix), `lazy_session_write.rs` (first write → exactly 1 row + cookie; cookie reuse → still 1), `lazy_session_login.rs` (login → exactly 1 authed row). Reviewed: umbra-sessions 28 / umbra-auth 43 / umbra-admin 161 tests green; no consumer relied on the eager row.

53. [x] **Playground shell resolves its asset prefix from the configured `static_url` instead of a hardcoded `/static/` — SHIPPED (2026-06-12).** Follow-up to the embedded→pipeline migration (gaps #67): the shell's `<script>`/`<link>` URLs were built from a hardcoded `STATIC_ASSET_PREFIX = "/static/playground/assets"` const in `plugins/umbra-playground/src/routes.rs`, so a deploy overriding `static_url` (a CDN origin or `/assets/`) got a shell whose asset URLs still said `/static/...` and 404'd. The original premise that `Plugin::routes()` "gets no Settings handle" was wrong: `routes()` runs at App::build **Phase 5** (`crates/umbra-core/src/app.rs:689`), and ambient settings are installed at **Phase 3** (`settings::init`, `app.rs:561`), so the configured `static_url` IS readable inside `routes()`. Fix: `PlaygroundState` gained an `asset_prefix: Arc<str>` field; `PlaygroundPlugin::routes()` snapshots it via `umbra::templates::resolve_static_url("playground/assets")` (the same ambient-`static_url` reader the `static()` template global uses) and passes it to `PlaygroundState::new(.., asset_prefix)`; `render_shell` builds the CSS/JS URLs as `format!("{}/{}", state.asset_prefix, CSS)`. Carrying the *resolved* prefix in state (rather than reading the global inside `render_shell`) keeps rendering deterministic and side-steps the `settings` `OnceLock` being process-global-set-once. Removed the hardcoded const + its `TODO(gaps2 #53)` breadcrumb. Test: `plugins/umbra-playground/tests/rust_integration.rs::shell_follows_configured_static_url` constructs a state with a `https://cdn.example.com/s/playground/assets` prefix, drives the real `router()`, and asserts the shell links the CDN URL and does NOT fall back to `/static/` — built directly (no global mutation) so it can't poison the sibling default-prefix test. `cargo test -p umbra-playground` green (9/9 integration), fmt + clippy clean on changed files.

37. [x] **FileField + ImageField ORM types, wired end-to-end through multipart form submission to a storage backend — SHIPPED (2026-06-12).** The user's "image submission" item: a `FileField` model type whose value is a storage key (TEXT column), `ImageField` = the same with a default `widget="image"` (admin shows a thumbnail; otherwise identical at storage/serde/SQL layers), file processed on form submit, with `MediaPlugin` providing the backend. Built as a 4-wave vertical (branch `feat/file-image-fields`):
    - **Storage foundation** (`4f42031`): a storage-agnostic `Storage` trait in `umbra-core` (`store(filename, content_type, bytes) -> StoredFile{key,url}`, `retrieve`, `delete`, `url`) registered ambiently (`umbra::storage::{set_storage, storage, storage_opt}` — same sanctioned global family as the DB pool, so the ORM's file fields depend on the trait, never on the media plugin). `umbra-media` refactored: `FsStorage` impl moved the filesystem write logic out of `MediaPlugin::save`; `MediaPlugin::new(mount, dir)` stays source-compatible (builds an `FsStorage`), `with_storage(mount, Arc<dyn Storage>)` added for S3-later, and `on_ready` registers the storage as the ambient default. Realizes the `MediaBackend` plan in `docs/decisions/2026-06-02-media-and-s3.md` (as `Storage`, in core not the plugin).
    - **Field types + macro** (`a6c428e`): `FileField(String)` / `ImageField(FileField via Deref)` in `crates/umbra-core/src/orm/file_field.rs` — sqlx Type/Encode/Decode (TEXT, Sqlite+Postgres), serde as the bare key string, `key()`/`url()` (url resolves via ambient storage, falls back to the raw key when unset so templates never panic), `Default`/`From`/`Display`. `#[derive(Model)]` classification taught to recognize the `FileField`/`ImageField` idents (+ `Option<>` nullable) → `SqlType::Text` with default `widget` `"file"`/`"image"` (explicit `#[umbra(widget=...)]` wins). New `Plugin::provides_storage()` capability flag (MediaPlugin → true) + a boot `field.storage_backend` system-check: a model with a file/image field but no storage-providing plugin fails `App::build()` with a clear message. The check reads the capability flag, NOT the ambient `storage_opt()`, because MediaPlugin registers storage in `on_ready` (after the Phase-4 system check).
    - **Multipart** (`b2ec437`): `umbra::web` gained `is_multipart`, `parse_multipart` (multer-based, captures filename/content_type/bytes; binary-safe), and `parse_and_store_multipart` which stores each non-empty file part via the ambient `Storage` and returns the same flat `Vec<(String,String)>` shape `serde_urlencoded` yields — an empty file part is skipped so an edit-without-new-file keeps the existing key.
    - **Admin** (`272e7d6`): `input_kind` recognizes `widget="file"/"image"`; `field_editor.html`/`form.html`/`sheet.html` render a file input (+ image thumbnail via the resolved `value_url`); the change form switches to `enctype="multipart/form-data"` only when it has a file field (plain forms stay urlencoded + backend-independent); `create`/`update`/`sheet_create` take `body: Bytes` and branch on Content-Type → `parse_and_store_multipart`; the absent-file-on-edit path preserves the existing key; single-cell inline edit of a file/image column is refused (can't carry an upload).
    Docs: `documentation/docs/v0.0.1/orm/file-image-fields.mdx` + the `plugins/media.mdx` Storage section. Closes features #40 (file uploads + multipart); features #41 (media storage) advanced — `Storage` trait + LocalStorage shipped, S3/R2/GCS + image library still deferred. Each wave's targeted tests green; full cross-cutting gate (umbra-core/macros/media/static/admin/umbra) exit 0.

56. [x] **Grouped ORM aggregate (`.values().annotate(Count())`) was already shipped + documented; reachable from the bare manager now, shop widgets + doc refactored off in-memory counting — DONE (2026-06-12).** The ORM feature the gap asked for already existed: `QuerySet::annotate(group_cols: &[&str], aggs: &[(&str, Aggregate)]) -> Vec<JsonValue>` (`crates/umbra-core/src/orm/queryset/mod.rs`) runs `SELECT <group_cols>, AGG(col) AS name ... GROUP BY <group_cols>`, one JSON object per group — the exact equivalent of Django's `Order.objects.values("status").annotate(count=Count("id"))`. The `Aggregate` enum (Count/Sum/Avg/Max/Min) + single-row `QuerySet::aggregate(...)` were present too, doc'd at `documentation/docs/v0.0.1/orm/aggregates.mdx`. One real gap surfaced: `aggregate`/`annotate` lived only on `QuerySet`, so `Order::objects().annotate(...)` (a Manager, no filter) failed with "no method named `annotate`" — yet the `aggregate.rs` docstring advertised exactly that call (an `ignore`d doctest, so it never compiled). Fix-don't-patch: added `Manager::all()` (Django's `.objects.all()` → bare `QuerySet`), `Manager::aggregate(...)`, and `Manager::annotate(...)` forwarders (each delegates to `self.queryset()`), making the documented unfiltered API real. Then the cleanup the gap requested: `examples/shop/src/widgets/charts.rs` — the order-status donut now does `Order::objects().annotate(&["status"], &[("count", Aggregate::count())])` (one grouped query; status stored lowercase via `#[choices(rename_all="lowercase")]` so the JSON `status` matches the canonical-order labels directly), and the activity line chart's per-day `.fetch().await.len()` became `.count().await` (COUNT in SQL, no rows loaded). Widgets doc `admin/widgets.mdx` Donut section updated to the `annotate` pattern + a Callout linking `orm/aggregates`. Verified: `cargo check -p umbra-core` clean with the forwarders; the shop's `charts.rs` compiles (the only remaining shop error is an unrelated in-progress `umbra_media` import in the user's WIP).

51. [x] **Inline CSS in the umbra-site is the legitimate "custom" case — explained, no fix needed (2026-06-12).** The user asked why the umbra_website templates carry `<style>` blocks + `style="..."` attributes when "we agreed to just use tailwind." Audit: the `<style>` blocks (base.html + several plugin templates) hold CSS that Tailwind UTILITIES structurally cannot express — the header-dropdown component (`.nav-dd__panel::before` hover-bridge pseudo-element, `[aria-expanded="true"] .nav-dd__chev` descendant-state selectors, `:hover` states, multi-stop box-shadows), the `:root` design tokens (`--paper`/`--ink`/`--accent`…), and a site-wide width override (`.max-w-[1200px] { max-width: 1280px }` overriding a purged arbitrary-value class). The `style="..."` attributes are DYNAMIC per-element values impossible as static classes: `--i: {{ loop.index }}` (animation stagger), `width: {{ plugin.progress_pct }}%` (data-driven bar), and `object-fit: cover` (object-cover isn't in the purged CSS). All fall under the user's own "Unless it's for custom" caveat — these ARE the custom cases. No Tailwind-avoidance to remove. (If hand-rolled prose styling ever appears, Tailwind's typography `prose` plugin is the right tool — none present today.)

52. [x] **Flaky `createsuperuser_noinput_errors_without_password_env` env-var race — FIXED (2026-06-12).** Two `#[tokio::test]`s in `plugins/umbra-auth/tests/integration.rs` raced on the process-global `UMBRA_SUPERUSER_PASSWORD`: `dispatch_routes_createsuperuser_command_with_noinput` sets it while `createsuperuser_noinput_errors_without_password_env` removes it then asserts the CLI errors — under parallel runs the "no password" test intermittently saw the other's value and failed its `expect_err`. Fix: a module-level `static SUPERUSER_ENV_LOCK: tokio::sync::Mutex<()> = tokio::sync::Mutex::const_new(())`; both tests acquire `let _env_guard = SUPERUSER_ENV_LOCK.lock().await;` right after `boot().await` and hold it for their env-dependent body, so they can never overlap (async-safe across the `.await`s in each test). Verified: both pass together under parallel `cargo test -p umbra-auth --test integration` (11/11). Also closes the (a) clause of gaps2 #30.

54. [x] **CLI lists registered commands on help / unknown-command — SHIPPED (`62660a1`, 2026-06-12).** Previously an unknown command produced a generic clap error and `umbra help` had no unified listing of plugin-contributed commands. Now: a command catalog merges the built-ins (read off the derived clap `Command` via `<Cli as CommandFactory>::command().get_subcommands()` → name + about) with every plugin's `commands()` (`umbra_core::cli::command_catalog(plugins)` → name + about; first-registered wins; a command with no `about` still lists with a dash and emits a debug nudge to the author). `render_help` (pure fn in `umbra_core::cli`) dedupes, sorts by name, column-aligns name→description, and frames it with a header + `Usage:` + a `Run \`umbra <command> --help\`` footer. Wiring in `umbra-cli`'s `dispatch_with_argv`: `help`/`--help`/`-h` as the first token → print the catalog to stdout, exit 0; an unknown subcommand (intercepting clap's `InvalidSubcommand`/`UnknownArgument`/`InvalidValue` after plugin dispatch returns Unmatched) → `error: unknown command \`<x>\`` + the catalog to stderr, exit non-zero. A bare `umbra` keeps its serve default; command-specific `--help` still routes through clap. Tested in `umbra-core` (catalog + render) and `umbra-cli` (help/unknown screens list both a built-in `migrate` and a plugin `tasks-worker`, aligned).

32. [x] **Boot check `field.choices_default`: reject a choices default that isn't one of its choices — SHIPPED (2026-06-12).** A choices field's default lands verbatim in DDL (`migrate.rs` `def.default(col.default.clone())`), so `#[umbra(default = "PostStatus::Draft")]` — the Rust enum *path* instead of the stored DB literal `"draft"` — ships a broken schema (Postgres rejects the row at insert via the `CHECK (col IN (...))` constraint; SQLite stores undecodable text and errors on the next `SELECT` when the `ChoiceField` decoder can't map it back). The user hit this on `umbra_website/plugins/plugin_directory/src/models.rs` (four path-shaped defaults, since fixed to DB literals). Per "backend mismatches caught at boot": new check `field_choices_default` in `crates/umbra-core/src/check.rs` (registered in `framework_checks()` as `field.choices_default`) walks every registered model via `migrate::registered_plugins()`/`models_for_plugin()` and pushes a `Severity::Error` `CheckLocation::Field { plugin, model, field }` finding when `!field.choices.is_empty() && !field.default.is_empty() && !field.choices.contains(&field.default)`. The message names the model+field+bad default and lists the valid choices. The hint: when the default contains `::` (the tell-tale of a pasted Rust path), it lowers the part after the last `::` and — if THAT matches a real choice — emits `Did you mean the DB literal \`<x>\`? Choices defaults are the stored value …, not the Rust enum path`; otherwise (and for non-`::` bad defaults) it lists the stored values to pick from. Works off `Column.choices` (already the DB values from `FieldSpec::choices`), so `choices` IS the allowed set — no need for `ChoiceField::VALUES`. Guards on `migrate::is_initialised()` so low-level `run_all` tests that never boot an App skip silently (same pattern as the other model-walking checks). The optional boolean/numeric default sanity-check the gap floated was DEFERRED: expression/function defaults (`CURRENT_TIMESTAMP`, arithmetic) would false-positive, and the choices case is the one the user actually hit. Tests: two binaries (each `tests/*.rs` owns its own process-wide registry/settings/db OnceLocks, and only one `App::builder().build()` per binary can publish phase-3 OnceLocks) — `system_check_choices_default.rs` asserts a `Mood::Sad` path default fails `build()` with a `BuildError::SystemCheckFailed` carrying a `field.choices_default` Error at `Field{plugin:"app", model:"BadDefault", field:"mood"}` whose hint says `Did you mean the DB literal \`sad\``; `system_check_choices_default_valid.rs` asserts a valid member default builds cleanly. Verified: both pass; full `cargo test -p umbra-core` green (no existing model trips the new check); clippy clean; whole-workspace gate green.

43. [x] **Admin full-page forms return ALL validation errors at once, each below its field — SHIPPED (2026-06-12).** Django returns every form error together; umbra's admin full-page create/edit (`templates/form.html`, NOT the sheet's `field_editor.html` macro) used to render a single error at the top (`form.html:9`) because the handlers (`crud.rs` `create`/`update`) just called `insert_row`/`update_row` and surfaced whatever single DB error came back — so a user fixed one violation, resubmitted, hit the next ("1 by 1"). Fix: a dynamic up-front validation pass `validate_form(model, form, cfg) -> BTreeMap<field, message>` (`view.rs`) that reuses `form_fields_for` (so the pk/noform/auto_now/password exclusions can't drift) and collects EVERY failure without short-circuiting: **required** (non-nullable column with empty `default` and a blank submit; bool checkboxes exempt; file/image/password skipped because their values aren't plain form fields), **number** (parse `i64`, or `f64` for Real/Double/Decimal), **date**/**time**/**datetime-local** (lenient chrono parse via `valid_time`/`valid_datetime_local`), **select/choices** (value must be one of `field.choices`), and **max_length** (char count, only if no type error already claimed the field). `FormField` gained an `error: String` slot; `create`/`update` run `validate_form` BEFORE the DB write — if non-empty they re-render `form.html` at HTTP 400 with each `field.error` populated from the map and the top-level `error` blanked (field errors carry the message now); if empty they proceed exactly as before, and a later UNIQUE/constraint violation validation can't predict still surfaces at the top via `sanitise_form_error`. `form.html` renders `{% if field.error %}<p class="field-error" …>{{ field.error }}</p>{% endif %}` below each input. Tests (`view.rs` `#[cfg(test)]`): `validate_form_collects_all_field_errors` (blank required text + non-numeric number → both reported together), `validate_form_accepts_valid_submission`, `validate_form_default_satisfies_required`, `validate_form_rejects_invalid_choice`. Verified: 20 umbra-admin test binaries green, clippy clean. (An unrelated single-line regeneration of the minified `admin.css` was left out of the commit — the per-field error uses inline styles, no new selector.)

44. [x] **Changelist refresh after a sheet save reads an authoritative rows URL instead of synthesizing it — SHIPPED (2026-06-12).** Traced the whole post-save refresh chain end-to-end and found it otherwise correct: the sheet form is HTMX (`_macros/sheet.html:85` `hx-post`, target `#umbra-sheet-slot`), and both `sheet::sheet_create` and `crud::update` success paths return an empty body + `HX-Trigger {closeSheet, refreshTable, showToast}` (the gaps2 #13 work); `admin.js` listens for `refreshTable` on `document.body` and re-fetches the rows fragment into `<tbody id="table-body">`. The one fragile link — the thing the gap fingered as "the url is not receiving the refresh signal correctly" — was that the JS *string-synthesized* the rows endpoint as `window.location.pathname.replace(/\/+$/,'') + '/rows' + window.location.search`. That only holds when the changelist path is exactly `<base>/<table>/`; it silently 404s (no refresh, no error) under a customized admin base path, a different trailing-slash shape, or any locale/prefix segment. Fix: `_macros/data_table.html` now stamps `data-rows-url="{{ admin_base }}/{{ model.table }}/rows"` onto the tbody (the SAME authoritative endpoint every search/sort/filter/pagination control already uses), and the `refreshTable` handler reads `tbody.getAttribute('data-rows-url')` for the base — appending only `window.location.search` (the query state, which IS authoritative because the changelist controls push it with `hx-push-url="true"`). Falls back to the old pathname synthesis when the attribute is absent (custom templates predating this). Verified: `cargo build`/`cargo test -p umbra-admin` green (20 binaries), attribute embedded + read. Caveat: this hardens the exact URL-derivation link the gap described, but the original symptom is browser-only — the user should confirm in-browser (the dev server is theirs to run); if a row still fails to appear after save, the next suspect is the `htmx.ajax('GET', url, {target, swap})` call or an event-bubbling timing issue, not the URL.

58. [x] **"Same struct = model + form + serializer" surfaced on the website — SHIPPED (2026-06-14).** Scoped to `umbra_website` (the gap is a messaging/docs ask, not an ORM change). Umbra's differentiator — a plain Rust struct *is* the database model, the validated form, and the JSON serializer, with no parallel schema type — was nowhere on the public site. Three changes: (a) **`/features` editorial callout** — a "The big idea" panel above the catalog (`plugins/features/templates/features/features.html`) with the headline "One struct. Model, form, and serializer.", a paragraph, three role pills (ORM model / Form & validation / REST serializer), and a dark code snippet showing one `#[derive(Model, Form, Serialize, Deserialize)] struct Post` used three ways (`Post::objects()`, `Post::validate(&data)`, `Json(post)`). It's editorial framing, deliberately NOT a catalog card, so it doesn't violate the design doc's "no hardcoded feature cards" rule (the cards below stay DB-backed). (b) **DB catalog entry** — a new flagship `FrameworkFeature` "One struct, three roles" seeded at the front of the "ORM & Migrations" category (`plugins/features/src/seed.rs`). (c) **`/docs` callout** — the same one-struct message as a highlighted banner above the docs grid (`plugins/site_content/templates/site_content/docs.html`), linking to `/features`. To make the new catalog card appear on an already-seeded DB (the running site) rather than only on a fresh install, the features `seed()` was made **self-healing**: instead of the old all-or-nothing `if count > 0 { return }` short-circuit, it now get-or-creates each category by slug and inserts only features whose unique slug is missing — so adding a `CATALOG` entry surfaces on the next boot with no duplicates and no DB wipe (aligns with CLAUDE.md "never wipe the DB to re-seed"). Tests: `plugins/features/tests/render_features.rs` gained assertions that `/features` surfaces "One struct" + "Model, form," + "serializer"; the existing catalog/status assertions still pass. Verified: `cargo build` (full site) green, Tailwind rebuilt, `render_features` passes. Docs note: the external `documentation/` MDX site lives outside `umbra_website` and the active ORM agent's scope, so the framework docs page for this idea is left for that surface; the website docs landing carries the message in the meantime.

62. [x] **Browser live-reload — framework-level, opt-in, push (no polling) — SHIPPED (2026-06-14).** Before: `umbra dev` (cargo-watch) rebuilt/restarted the server on `.rs` and hot-reloaded templates in-process on `.html`, but the developer still manually refreshed the browser. The ask was Vite-style live-reload. Built as a **new standalone plugin `plugins/umbra-livereload`** (added to the `crates/` workspace members) so it's reusable by any app and touches **no ORM/core internals** — it rides existing `Plugin` hooks only. Mechanism (the Vite shape minus the bundler): (a) `routes()` mounts `GET /__umbra/livereload` as an **SSE** stream — push, not polling; `EventSource` auto-reconnects, which makes the restart→reload case free. SSE chosen over WebSocket because a reload signal is one-way. (b) `on_ready()` (Dev only) spawns a `notify` OS file-watcher over the configured dirs; a debounced burst (90 ms window) publishes one message on a `tokio::broadcast` bus — `{"type":"css"}` when only stylesheets changed, else `{"type":"reload"}`. Path classification is a **denylist** (so non-standard layouts work by just adding the root — no extension allowlist to maintain): everything under a watched root reloads *except* editor/VCS/IDE/`target`/`node_modules` noise and Cargo build inputs. `.rs` is deliberately ignored by the watcher — source edits are handled by the rebuild→restart→reconnect→boot-id path, so reacting in-process would fire a premature reload before the new build is up (watching `src` is therefore harmless but pointless). (c) `wrap_router()` (Dev only) installs an `axum::middleware::from_fn` layer that injects a tiny client `<script>` before `</body>` on every `text/html` response — **zero per-app template work**, the "framework-level not per-app" requirement. (d) Client: on a `css` event it swaps every `<link rel=stylesheet>` href with a `?v=<ts>` cache-bust (no reload — the realistic "change only what changed"); on `reload`, or on reconnect to a server with a new per-process **boot id** (i.e. cargo rebuilt the binary), it calls `location.reload()`. True HMR/hydration ("re-run just this handler / swap just this fragment") is deliberately out of scope — it needs a client module graph + framework HMR hooks that a server-rendered app doesn't have; CSS hot-swap is the achievable partial-update. Everything gated to `Environment::Dev` (inert otherwise: no route, no watcher, no injection). Opt-in via `App::builder().plugin(LiveReloadPlugin::new())` — `new()` watches `./templates` + `./static`; `.watch(path)` adds more roots (a **directory**, recursive, or a **single file** like `site.config.toml`), `.watch_only(...)` replaces the list — so any project convention is configurable. Dogfooded in `umbra_website` (`.plugin(LiveReloadPlugin::new().watch("plugins"))`, since the page templates live under `plugins/*/templates`). Discoverability: a commented `# umbra-livereload` line added to the `startproject` scaffold Cargo.toml (`crates/umbra-cli/src/scaffold.rs`). Tests: `plugins/umbra-livereload` unit tests pin the HTML injection (before `</body>`, fallback-append) and the `classify()` css/reload/ignore routing; full website + `umbra-cli` scaffold tests green. Follow-ups (not blocking): a user-facing MDX doc page under `documentation/docs/v0.0.1/` per the "ship a feature, ship its doc page" rule, and optionally folding auto-enable into `umbra dev` so the plugin doesn't need an explicit `.plugin(...)` line in dev (kept opt-in for now, as requested).

22. [x] **Cross-database foreign-key integrity guard + `#[umbra(db_constraint = false)]` opt-out — SHIPPED (2026-06-16).** The (a)+(b) halves of the original entry; the broader `DatabaseRouter` trait (read/write split, per-request routing) stays open as #23/#28. A foreign key whose target model routes to a *different* database can't be a real DB constraint (`REFERENCES` can't span pools), but umbra silently emitted an invalid `FOREIGN KEY` line. Now: **(a) a boot-time guard** in `App::build()` (`crates/umbra-core/src/app.rs`, Phase 2.5b, right after the existing plugin/model alias validation) builds a `table → alias` map with the same precedence as `model_aliases` (plugin default → per-model `#[umbra(database)]` override → `"default"`), then walks every model's FK fields; if an FK's target table routes to a different alias than the model AND the field hasn't opted out, it returns the new `BuildError::CrossDatabaseForeignKey { model, field, model_db, target_db }` (sibling of `BuildError::PluginDatabaseAlias`, the pattern the entry pointed at — chosen over a `check.rs` finding because that's where the alias validation already lives, and it fails before any ambient `OnceLock` state is published). **(b) the opt-out** `#[umbra(db_constraint = false)]` (mirrors Django's `ForeignKey(db_constraint=False)`) keeps the FK *logical*: the column + `fk_target` are unchanged so joins / `select_related` / `check_fk_row_exists` pre-validation all keep working, but the migration engine emits no physical `REFERENCES`. Default is `true` (emit the constraint — today's behavior); the guard forbids a cross-DB FK *unless* the field sets it false. Three-touchpoint field-attribute pattern (same as `noform`/`noedit`): `umbra-macros` parses `db_constraint` in the field-attr loop (bool, `= false`/`= true`/bare-`true`) and emits it into the generated `FieldSpec`; `FieldSpec` gains `pub db_constraint: bool` (and `migrate::Column` mirrors it, `#[serde(default = "default_true", skip_serializing_if = "is_true")]` so existing migration JSON round-trips); the migration emitter (`build_column_def_sqlite` / `build_column_def_postgres` CREATE TABLE paths + the Postgres `ALTER ... ADD CONSTRAINT` path) skips the `REFERENCES` / `FOREIGN KEY` line when `!db_constraint` (the DROP CONSTRAINT still fires, so flipping the flag tears down a prior constraint). Strict TDD: `crates/umbra-core/tests/cross_db_fk.rs` (5 tests, all green) — cross-DB FK without opt-out → `CrossDatabaseForeignKey`; same with opt-out → builds; `db_constraint` lands on the generated `FieldSpec` (true default / false opt-out); same-DB FK with `db_constraint=false` emits no `REFERENCES`/`FOREIGN KEY` in the DDL (sqlite + postgres); normal same-DB FK still emits the constraint (regression guard). Adding the `FieldSpec`/`Column` field is a breaking change to every literal; updated all hand-built ones across `crates/` (`orm/post.rs`, `inspect.rs`, plus `#[cfg(test)]` modules in `migrate.rs`/`dynamic.rs`/`validation.rs` and the integration test fixtures) and `plugins/` (`umbra-admin/src/rows.rs`, plus the macro-generated block). Verified: `cargo build --workspace`, `cargo test -p umbra-core` (927 passed), `umbra-rest`/`umbra-admin`/`umbra-openapi` tests (378 passed), `cargo fmt`, `cargo clippy --workspace --all-targets` all clean. Follow-up (not blocking): a user-facing MDX note on `documentation/docs/v0.0.1/orm/database-routing.mdx` — that page's "Not yet supported → cross-DB FK has no guard (#22)" bullet can now be dropped; left for the docs surface per scope.

61. [x] **Batch resource / model registration — `RestPlugin::resources` + `AdminPlugin::register_many` / `register_for_many` — SHIPPED (2026-06-16).** Resources and admin configs are best declared per app (next to the models, the way DRF keeps serializers per app), then registered in one call instead of a `.resource(...)` / `.register(...)` per model in `main.rs`. Added `RestPlugin::resources(impl IntoIterator<Item = ResourceConfig>)` (`plugins/umbra-rest/src/lib.rs`) and `AdminPlugin::register_many(impl IntoIterator<Item = AdminModel>)` + `register_for_many(plugin_name, iter)` (`plugins/umbra-admin/src/lib.rs`) — trivial delegating loops over the existing single-value builders, so the semantics (additive, last-wins on duplicate table, precedence) are unchanged. Intended pattern: a plugin exports `pub fn rest_resources() -> Vec<ResourceConfig>` / `pub fn admin_models() -> Vec<AdminModel>` and the app calls `.resources(blog::rest_resources())` / `.register_many(blog::admin_models())` per app. Test: `plugins/umbra-rest/tests/resource_config.rs::resources_batch_registers_every_config` (registers two configs via the batch form, asserts both landed via the plugin's `Debug` summary). Docs: `rest/exposure.mdx` ("Registering many resources at once") + `plugins/admin.mdx` ("Registering many models at once"). The admin batch wasn't given a separate full-app test — `register_many` is the identical loop over the already-tested `register`, and the registry isn't reachable for a cheap unit assertion (the plugin field is private); covered by compile + the REST behavioral test.

38. [x] **`Model::COL` associated-const column form (alongside `module::COL`) — SHIPPED (2026-06-16).** `.filter(ContactMessage::CREATED_AT.gte(week_ago))` (the qualified `Model::COL` form) did not compile — only the `contact_message::CREATED_AT` module form did, because `#[derive(Model)]` emitted the column predicate consts only inside `pub mod <snake_struct> { pub const COL: ColType<super::Struct> = … }`. Fix (`crates/umbra-macros/src/lib.rs`): `column_const_for` now returns `(module_const, assoc_const)` — the second is `pub const COL: ColType<Self> = <module>::COL;`, an alias of the module const (one source of truth for the column name/type), collected into `assoc_consts` and emitted in a new `#[allow(dead_code)] impl Struct { … }` block beside the module (the inline `choices`/`multichoice` const path mirrors it). `#[allow(dead_code)]` restores parity with the `pub mod` form — an unused pub const inside an `impl` of a *non-`pub`* model would otherwise warn, where the pub-module const never did. Both forms resolve to the same column object, so `Model::COL` and `module::COL` are interchangeable at the filter site. Test: `crates/umbra-core/tests/model_qualified_column.rs` (real rows; `Doc::TITLE.eq(..)` + `Doc::VIEWS.eq(..)` qualified forms filter; `doc::TITLE` module form still works — no regression). Verified across the whole workspace (the macro runs on every model): `cargo build --workspace`, `cargo test -p umbra-core` (green), the plugins, and `umbra_website` all build — no const collisions, no new warnings.

74. [x] **OAuth: PKCE (S256) + single-use expiring `state` — SHIPPED (2026-06-17).** The flow previously ran without PKCE and (originally) with a replayable `state`. Both halves are now closed. **PKCE (this commit):** a new `plugins/umbra-oauth/src/pkce.rs` (`pub mod pkce`) provides `generate_verifier()` (32 CSPRNG bytes → base64url-nopad → 43 chars, all unreserved) and `challenge_s256(verifier)` (`BASE64URL-NOPAD(SHA256(ascii))`). `begin_flow` (`routes.rs`) mints a verifier, computes its S256 challenge, persists the verifier in the session `FlowState` (new `code_verifier: String` field, `#[serde(default)]` so flows persisted before PKCE still deserialize), and passes the challenge to `authorize_url`. The callback replays `flow.code_verifier` to `exchange_code`. The `OAuthProvider` trait gained the threading args — `authorize_url(.., code_challenge: &str)` and `exchange_code(.., code_verifier: &str)`; both providers (Google, GitHub) append `code_challenge` + `code_challenge_method=S256` on the authorize URL and `code_verifier` on the token POST. Always-S256 (plain is never emitted); sending the challenge is safe even against a non-PKCE provider because RFC 6749 §3.1 requires servers to ignore unrecognized params (Google enforces it; defense-in-depth on GitHub). **Single-use + expiring `state` (already in place):** the callback validates `state` via constant-time `ct_eq` then consumes the flow (`set_data(FLOW_KEY, Null)`) *before* the token exchange, so a captured callback URL can't be replayed; the flow rides the session, so it expires with the session TTL. New deps on umbra-oauth: `rand = "0.8"`, `sha2 = "0.10"`, `base64 = "0.22"` (versions match the sibling auth/security plugins). Tests: `src/pkce.rs` unit tests (RFC 7636 Appendix B vector `dBjf… → E9Me…`, 43-char unreserved alphabet, per-call uniqueness); both providers' `authorize_url` tests assert `code_challenge` + `S256` are carried; and `tests/pkce_flow.rs` is an end-to-end behavioral proof — it drives the *real* `GET /oauth/google/login` through the *real* `session_layer`, parses the `code_challenge` from the redirect `Location`, reads the persisted `code_verifier` from the session row (`SELECT data FROM session`), and asserts `challenge_s256(verifier) == challenge` (the PKCE bind, through the public HTTP path, not internals). Docs: `documentation/docs/v0.0.1/auth/oauth.mdx` updated (custom-provider trait signatures + a new "How the flow is hardened" section covering PKCE / single-use / expiring `state`). Verified: `cargo test -p umbra-oauth` (23 tests green), `cargo fmt`, `cargo clippy -p umbra-oauth --all-targets` clean. Contained entirely to umbra-oauth — repo-wide grep confirmed no caller of the two trait methods outside the plugin, so the signature change breaks nothing downstream. → `reviews/security.md`.

75. [x] **Secret / auth hardening — SHIPPED (2026-06-20).** All three sub-findings closed on branch `hardening/plugin-fixes`. (a) **Empty `SECRET_KEY`** — `SecurityPlugin::on_ready` calls `check_secret_key`: when CSRF + signed-CSRF are enabled and `secret_key` is empty/whitespace, `Prod` fails boot (`Err(PluginError)`), `Dev`/`Test` warn loudly; a `debug_assert` guards the `sign()` call site. 8 tests in `plugins/umbra-security/tests/empty_secret_key.rs`. Commit `71c75a0`. (b) **`password_hash` leak** — a hard `HARD_DENIED_FIELDS` denylist applied at the tail of `apply_overrides_depth` in `umbra-rest` strips `password_hash` from every serialized response, un-overridable by `.expose()`. (A first attempt adding `#[serde(skip_serializing)]` + a `Model::to_db_row` derive-macro override was REVERTED — `e93d0b8`/`92be470` — because it broke test compilation workspace-wide; the narrow REST-layer fix replaced it.) Test `plugins/umbra-rest/tests/password_hash_never_exposed.rs`. Commit `e7e70ab`. (c) **Inactive superuser keeps perms** — `WithPermissions::authenticate` stores `is_active` in extras; `HasPermission::check` gates on it before the superuser bypass, so an inactive user (incl. superuser) is denied. 4 tests. Commit `e2dd1ae`. Full workspace green after each. → `reviews/security.md`.

77. [x] **Dedup `to_snake_case` (×3) / `pascal_case` (×2) — SHIPPED (2026-06-20).** New no-dependency workspace crate `crates/umbra-casing/` is the single source: `to_snake_case` (canonical acronym-aware impl that drives the `TABLE` constant in `#[derive(Model)]`), `pascal_case_from_table` (inspectdb — lowercases), `pascal_case_from_ident` (scaffold + openapi — no lowercasing). The five prior impls (`umbra-macros`, `umbra-core/src/inspect.rs`, `orm/queryset/mod.rs`, `umbra-openapi`, `umbra-cli`) now call the crate; duplicates removed. The three groups were NOT byte-identical (queryset lacked the acronym-collapse; the two pascal variants differed on `-`/lowercasing) — each call site's existing output was preserved, proven by `cargo test --workspace` (unchanged). Commit `4b92067`. → `reviews/architecture-modularity.md`.

86. [x] **Plugin doc drifts — SHIPPED (2026-06-20).** Each claim verified against code, then corrected: `plugins/signals.mdx` — the "bulk methods are signal-free" callout was false (`bulk_create`/`update_values`/`update_expr` fire `bulk_post_save`, `QuerySet::delete` fires `bulk_post_delete`); `m2m_changed` "not shipped" bullet removed (it ships via `emit_m2m_changed`). `plugins/umbra-openapi/src/lib.rs` `//!` — "no securitySchemes / pagination deferred" both false (securitySchemes from `registered_security_schemes()`, `pagination_parameters()` on list endpoints). `plugins/tasks.mdx` — stale Postgres-locking note replaced (the conditional UPDATE in `claim_one()` is correct on both backends post-BROKEN-1; `FOR UPDATE SKIP LOCKED` is a future optimization). Commit `5d5f745`. → `plugins-review/*.md` + `docs-audit/*.md`.

81. [x] **Plugin security (net-new) — SHIPPED (2026-06-20).** All eight sub-findings closed on `hardening/plugin-fixes`. `umbra-security`: `csrf_exempt_paths` matches on a path-segment boundary, `/api` no longer exempts `/api-internal` (`f75a974`). `umbra-email`: CRLF/header-injection guard on subject/from/reply-to/recipients (`e0a71cf`); console backend fails closed in prod instead of printing bodies/tokens, + 10s SMTP send timeout (`a2dbb03`). `umbra-playground`: single-pass shell substitution stops placeholder re-expansion / template-injection breakout (`23c5ca0`). `umbra-cache`: `cache_page` key includes Host + bypasses session-cookie requests (cache poisoning) (`d311d77`). `umbra-static`: ETag/304 conditional GET on embedded assets + symlink-escape guard on the ServeDir path (`2762eaa`). `umbra-oauth`: shared reqwest client with request/connect timeouts (`2eb5936`) + `unique_username` INSERT-with-retry on UNIQUE violation closing the TOCTOU (`2923115`). Full workspace green throughout. → `plugins-review/{umbra-security,umbra-email,umbra-playground,umbra-cache,umbra-static,umbra-oauth}.md`.

73. [x] **Silent wrong-writes + per-request panic — SHIPPED (2026-06-20).** All sub-findings closed on `hardening/plugin-fixes`. `f32`/`f64` `min`/`max` validation enforced via `validate_numeric_bounds`/`as_f64` (was already fixed; pinned with 7 tests, `6053fe0`). `inline_edit` returns 400 on a parse failure instead of writing `""` (already fixed via `DynQuerySet::update_one`; pinned, `e029202`). `Masked` malformed key now returns `BadKey` instead of a silent `None` keyring that writes plaintext — keyring lifted to `OnceLock<Result<Option<MaskKeyring>, MaskError>>` (`d979e9c`). `storage.rs` per-request panic de-fanged: `try_storage()` returns `NoBackend` and the `field.storage_backend` boot system-check already guards `App::build()` (`5ffc9ed`). Non-i64 M2M child ids no longer dropped from form junction writes — root cause was UUID-PK columns bound as TEXT in `DynQuerySet::filter_in_strings`/`filter_eq_string` while SQLite stores UUIDs as BLOB; a typed `Uuid` arm fixes the bind (`e5bee71`). REST `?format=csv` writer errors surface as `ApiError::Internal` → 500 instead of a silent truncated 200 (`dc888d1`). Full `cargo test --workspace` green. → `reviews/correctness-domain.md` + `reviews/static-analysis.md`.

83. [x] **admin — custom-base-path bugs + authz disclosure — SHIPPED (2026-06-20).** All three net-new admin bugs fixed and tested on `hardening/plugin-fixes`. (1) Inline cell-edit + FK-picker pagination fragments honor `branding::current().base_path` instead of hardcoding `/admin/...`, so they work under `.at("/backoffice")` (`006033a`, `tests/base_path_fragments.rs`). (2) The sidebar `apps(_viewer)` now filters models by the viewer's `view_<model>` permission (one codename load via `user_perms`, superuser + no-PermissionsPlugin bypass), closing the model-existence disclosure (`c365c47`, `tests/sidebar_perm_gate.rs`). (3) Bulk actions handle non-i64 PKs — `ActionInvocation::ids` is `Vec<String>` (was `Vec<i64>`), `delete_selected` uses `filter_in_strings` (dispatches on the PK column's `SqlType`) and reports the real affected-row count from `DynQuerySet::delete()` instead of `ids.len()`, so String/Uuid-PK selections are no longer silently dropped with a wrong "Deleted N" toast (`c2fcdd0`, `tests/phase3_actions_str_pk.rs`). Also added the previously-absent authz / `.at()` / non-i64-PK admin tests alongside each fix. (Soft-delete-on-admin testing tracked under #35.) → `plugins-review/umbra-admin.md`.

84. [x] **Plugin-contract & shared framework primitives (net-new) — SHIPPED (2026-06-20).** All three on `hardening/plugin-fixes`. `umbra-health` `probe_database` routed through a new `umbra::db::ping()` (ORM surface, no raw `SELECT 1`) + each `HealthCheck` wrapped in `tokio::time::timeout` so one blocked check can't hang `/ready` (`14c30c4`). Shared `umbra::plugin::block_on_ready` bridge added (`crates/umbra-core/src/plugin.rs`): handles multi-thread (`block_in_place`), current-thread `#[tokio::test]` (scoped OS thread + own current-thread runtime — the old `umbra-rls` panic site), and no-ambient-runtime; both `umbra-rls` and `umbra-permissions` route through it (`8a48b5f`). `umbra-realtime` no longer hard-depends on `umbra-auth`: it now uses the facade identity contract (`umbra::auth`, lifted in #76) via an injected `IdentityResolver` (boxed async `HeaderMap → Option<i64>`, default anonymous); `umbra-auth` is `optional = true` behind an `auth` feature with `with_auth_sessions()` the opt-in convenience, and `identity_resolver(f)` for arbitrary JWT/API-key auth with zero auth dep. Base `cargo build -p umbra-realtime` (no features) is clean — anonymous push-only feeds compile without auth (`7f96fd5`). → `plugins-review/{umbra-realtime,umbra-health,umbra-rls,umbra-permissions}.md`.

72. [x] **Endpoint scalability — unbounded fetches, per-row clones, missing indexes — SHIPPED (2026-06-20).** All sub-parts on `hardening/plugin-fixes`. REST `?format=csv` now clamps to `MAX_LIST_ROWS=1000` (the `page=None` bypass was already patched in `7078b57`; pinned with a 1003→1000 regression test + doc fix, `58d8c2e`). The migration engine already emits btree indexes for FK columns + `deleted_at` (`should_emit_btree_index`); pinned with 3 tests (`2d2864f`). Admin M2M form options are bounded to `M2M_OPTION_CAP=200` with currently-selected-beyond-cap rows preserved via a second `filter_in_strings` fetch (`9de4b4d`). `apply_overrides` no longer deep-clones the model registry per row — `apply_overrides_with_meta(&ModelMeta)` hoists the lookup to once-per-request for both JSON + CSV list loops (`cca87e1`). `AdminPerms::load` resolves all flags from a single `user_perms` query (was 12-14 serial); refactored to a pure `from_codenames` helper + 6 tests (`7af921a`). → `reviews/performance-scalability.md`.

12. [x] **Admin form errors — per-field rendering — SHIPPED (2026-06-20).** Part 1 (prior: `DynError::Write(WriteError)` enum lift + `AdminError::Write` + `sanitise_form_error`) plumbed the structured error through. Part 2 (this wave): `apply_write_error_to_fields(we, &mut fields)` in `util.rs` merges each column's `WriteError::field_errors()` messages into the matching `FormField.error` slot — the same slot the up-front validation (#43) renders as `<p class="field-error">` in `form.html` — so a UNIQUE violation on `user` shows beneath the `user` input, not just a joined string at the top. `parse_unique_violation_column` promoted to `pub(crate)` for the `AdminError::Sqlx` branch's column attribution; `non_field_errors()` (column-less constraints: `UniqueViolation{field:None}`, `CheckViolation`, whole-form `Validator{field:""}`) stay in the top banner; errors for off-form columns escalate to the banner so nothing is silently dropped. Both `create` + `update` error branches wired (`crud.rs:370,636`). Tests `write_error_per_field.rs` (create + update submit a duplicate UNIQUE column, assert `class="field-error"` appears after the column's input). Commit `d1bdccb`. → closes the part-2 follow-up that also unblocked the gaps2 #19 `Form<T>` template surface.

24. [x] **Multi-DB / database routing docs — SHIPPED (2026-06-20).** `documentation/docs/v0.0.1/orm/database-routing.mdx` now documents the full shipped surface. The current-state page (named pools, per-model/per-plugin routing, `resolve_pool` precedence, per-DB migrations, boot validation) landed earlier; this wave expanded it as #22/#23 shipped: a "Read/write (replica) split" section with a working `DatabaseRouter` impl (`db_for_read`/`db_for_write`, `.router(...)` registration, read-after-write note, matching `examples/read-replica`), and the confirmed "Cross-database foreign keys" section (`BuildError::CrossDatabaseForeignKey` guard + `#[umbra(db_constraint = false)]` opt-out). Genuinely-open items stay flagged as not-yet-supported (tracked under #69 Phase 2): schema-per-tenant / database-per-tenant management, dynamic per-request multitenancy routing, row-level tenancy / RLS, `.on()` SQLite-typed, `databases` settings map auto-connect. Commit `78863e8`.

88. [x] **M2M junction parent-id + routing — SHIPPED (2026-06-20).** (a) The "hardcoded to i64" worry was a false alarm (resolved 2026-06-16): `M2M<T,P>` is generic over the parent PK; the dead `pk_i64` was removed and `pk_uuid_m2m`/`pk_string_m2m` prove UUID/String parents add+prefetch+join_related. (b) This wave routed the M2M junction free-functions (`crates/umbra-core/src/orm/m2m.rs` `set_junction_dynamic`/`load_junction_selection`) through the `DatabaseRouter` instead of raw `pool_dispatched()`: new `junction_pool_for_write`/`junction_pool_for_read` key on the PARENT model's alias (a junction `<parent>_<field>` belongs to the parent's database), a `parent_model: Option<&str>` param was threaded through both fns + every call site (`dynamic.rs`, umbra-admin crud/view) + the `#[derive(Model)]` `write_pending_m2m` emission (using UFCS `<Self as Model>::NAME` to disambiguate from the inherent column-accessor `Self::NAME`); `None` preserves today's single-pool default. Write ops → `db_for_write`, reads → `db_for_read`. Test `router_m2m_junction.rs` (split router: write→write pool, read→read pool). Commit `9dbe17a`. → #69 follow-up; closes the last db-per-tenant/replica routing gap for junctions.

4. [x] **wrapper.html growing too large — SYMPTOM RESOLVED (archived 2026-06-20).** The original gap (wrapper.html bloat) is fixed: commit `e7747fa` extracted ~1080 lines of inline `<script>` IIFE blocks to an external `plugins/umbra-admin/src/assets/admin.js` served via `Plugin::static_files()` at `/admin/static/admin.js`; wrapper.html shrank 1636 → 563 lines (66%). One `umbraAdminBase` bootstrap stays inline to carry the Jinja-substituted base into the external file. CSS was already external (`/admin/static/admin.css` via `build.rs`). Pinned by `phase4_dashboard.rs::admin_js_served_as_external_asset_not_inline`. Correctly-still-inline: pre-paint theme bootstrap, the `window.umbra` stub, the Tailwind CDN config block, the third-party CDN tags. **Deferred follow-ups (NOT the symptom — separate categories, not closing-blockers):** (1) splitting admin.js into per-feature bundles — a perf optimization that wants measurement first; (2) self-hosting the third-party CDNs (htmx/lucide/apexcharts) for offline/air-gapped use — a distinct cleanup, consolidated with the editor-CDN self-host tracked under #36(b). These are enhancements, not the wrapper.html-bloat symptom, which is closed.

66. [x] **Document minijinja filters + custom filter registration — SHIPPED (2026-06-20).** New `documentation/docs/v0.0.1/web/templates-filters.mdx`: links the minijinja built-in filter index + common examples as they work in umbra; documents the umbra-registered custom filters/functions verified in `templates.rs` (`| img`, `| markdown`, `| sanitize`, `| currency`, `static(path)`, `media_url(key)`, `now(fmt?)`, `highlight_styles()`, + ambient `user`/`csrf_token`/`csrf_input`); and the real registration API `Plugin::template_registrars() -> Vec<TemplateRegistrar>` (`Box<dyn Fn(&mut Environment) + Send + Sync>`, collected by `App::build()`, re-applied on dev hot-reload) with a worked example. Commit `6a9a0bf`.

67. [x] **`umbra startapp` auto-registers the plugin + success message — SHIPPED (2026-06-20).** After scaffolding `plugins/<name>/`, `startapp` now adds `<name> = { path = "plugins/<name>" }` under `[dependencies]` in the project `Cargo.toml` (manual string surgery — `toml_edit` isn't a dep; finds/creates the `[dependencies]` section), idempotent (skips if the dep line already present; comment-line filtered to avoid the template's hint-comment substring trap), and soft-fails (writes the scaffold + warns) when the Cargo.toml is missing. Prints a clear success message — what was registered (or the no-duplicate/not-found note) — and the cleaned `next_steps` now shows only the `.plugin(...)` builder-wiring step instead of telling the user to hand-edit Cargo.toml. Tests: umbra-cli (21 pass) assert the path dep appears after `startapp` + a second run is idempotent. Commit `69a9350`. → ties #8/#25 scaffold work.

27. [x] **Cache plugin opt-in compression + cache-control headers — SHIPPED (2026-06-20).** `CachePlugin` gains three default-off builders that feed through `Plugin::wrap_router` (mirroring `SecurityPlugin`'s config-struct shape), orthogonal to the existing server-side `cache_page` store: `.with_compression()` (`tower_http::CompressionLayer` — gzip/br/deflate/zstd negotiated via `Accept-Encoding`), `.cache_control("public, max-age=…")` (`SetResponseHeaderLayer::overriding(CACHE_CONTROL, …)`), and `.vary("Accept-Encoding")` (same for `VARY`). Added the `compression-*` + `set-header` tower-http features. Tests `http_layers.rs` (compression negotiated, Cache-Control/Vary emitted when set, nothing added by default) + docs. Commit `0419fcf`.

33. [x] **Admin index restore_last_path — config flag — SHIPPED (2026-06-20).** The "where I left off" feature (gaps2 #11 r2) is now a proper opt-out-able `AdminPlugin` config: `restore_last_path: bool` (default `true`, builder `.restore_last_path(false)`), sealed into `AdminBranding` + exposed as a Minijinja global (same ambient pattern as `admin_base`). Three wiring points gated on it: (1) the index handler restores the `/admin/` → `last_path` redirect when on (with `?dashboard=1` forcing the dashboard), renders the dashboard directly when off; (2) the changelist `set_last_path` writer is gated so opting out stops dead-data accumulation; (3) the `base.html` Home breadcrumb emits `href="/admin/?dashboard=1"` when on (the escape hatch becomes a one-click UI affordance) / plain `/admin/` when off. Tests `gaps2_33_rlp_on.rs` (3) + `gaps2_33_rlp_off.rs` (4) — split because `BRANDING`/`ENGINE`/settings are process-global `OnceLock`s. Commit `eb2366f`.

34. [x] **Soft delete: `update_values`/`update_expr` respect the `deleted_at` scope — SHIPPED (2026-06-20).** The scoping was already present in the code (`build_update_for` lines 3251-3256 and `update_expr` lines 2948-2953 in `orm/queryset/mod.rs`) but lacked test coverage and the docs actively warned users that `update_values` was NOT scoped (a stale/incorrect callout in `soft-delete.mdx` referencing gaps2 #34 as open). Chose option (a) consistent-with-reads: default queryset → only live rows (`deleted_at IS NULL`); `.with_deleted()` → all rows; `.only_deleted()` → trashed only. Three new regression tests in `crates/umbra-core/tests/soft_delete.rs`: (a) `update_values_default_scope_skips_trashed_rows` — default update does not touch trashed rows; (b) `update_values_only_deleted_restores_trashed_row` — `.only_deleted().update_values({deleted_at: null})` is the clean restore path; (c) `update_values_with_deleted_covers_all_rows` — `.with_deleted()` reaches both live and trashed. Docs updated: restore example now uses `only_deleted()`, stale warning callout replaced with correct info callout, pinned-by line cites gaps2 #34. Commit in this branch.

34. [x] **Soft-delete update_values respects the deleted_at scope — SHIPPED (2026-06-20).** Chose option (a) consistent-with-reads. The scoping was already correctly implemented in `build_update_for` + `update_expr` (`crates/umbra-core/src/orm/queryset/mod.rs`): both inject `deleted_at IS NULL` by default, honor `.only_deleted()` (→ `IS NOT NULL`, the restore path) and `.with_deleted()` (no filter), so a bulk `update_values`/`update_expr` does NOT touch trashed rows by default. The real defect was doc rot — `soft-delete.mdx` carried a stale warning claiming it wasn't scoped. Shipped 3 regression tests in `soft_delete.rs` ((a) default skips trashed, (b) `only_deleted().update_values({deleted_at: null})` = restore, (c) `with_deleted()` = all) + corrected the doc (restore example now `only_deleted()`, warning → correct info). `update_expr` covered by the identical scope block. Commit `1628ce0`.

35. [x] **Soft delete on the dynamic path + admin trash UI — SHIPPED (2026-06-22).** Core (`DynQuerySet` soft-delete scope, `with_deleted`/`only_deleted`/`hard_delete` toggles, `delete()`→`UPDATE deleted_at=NOW()`) landed earlier and is pinned by `crates/umbra-core/tests/soft_delete_dynamic.rs`. This change ships the REMAINING admin trash UI, all gated on `meta.soft_delete` (non-soft-delete models are byte-identical to before). (1) **`DynQuerySet::restore()`** new terminal in `crates/umbra-core/src/orm/dynamic.rs` — `UPDATE … SET deleted_at = NULL WHERE deleted_at IS NOT NULL AND <filters>`, the inverse of a soft `delete()`, emitting `bulk_post_save` (created=false); a no-op on a non-soft-delete model. (2) **Changelist trash filter**: `?trash=1` flips the list to `only_deleted()` (live view already excludes trashed); threaded `trash: bool` through `rows::count_rows_filtered`/`fetch_rows_paged` and both `list` + `rows_fragment` handlers (`plugins/umbra-admin/src/handlers/list.rs`, `rows.rs`). A **Trash / Active toggle** + trashed-row count badge render in the changelist header for soft-delete models (`templates/_macros/data_table.html`). (3) **Built-in actions** `Action::restore_selected()` (`with_deleted().filter_in_strings(pk).restore()`, gated `Action::Change`) and `Action::delete_permanently()` (`hard_delete().with_deleted().filter_in_strings(pk).delete()`, behind a confirm, gated `Action::Delete`) in `plugins/umbra-admin/src/config.rs`. `effective_actions(configured, soft_delete, trash)` swaps the configured set for these two in trash view; `resolve_actions(cfg, table)` makes them dispatchable through the existing `run_action`/`dispatch_action` endpoints (with the extra `delete_<model>` gate on the permanent-delete key). In the trash view the per-row affordances become Restore + Delete-permanently (`data_table.html` + `rows_fragment.html`). (4) **Default delete already soft-deletes** — `Action::delete_selected()` routes through `DynQuerySet::delete()`, which honours `soft_delete`, so no fix was needed; on a soft-delete model it moves rows to trash. Tests: `plugins/umbra-admin/tests/soft_delete_admin.rs` (5: trash filter hides-then-shows, default-delete soft-deletes, restore clears `deleted_at`, delete-permanently hard-removes, non-soft-delete model has no toggle/restore) — real rows through the HTTP handlers + DB-state assertions. All `umbra-admin` (60 unit + every integration suite) + core soft-delete suites green; `cargo build --workspace` clean. Docs: `orm/soft-delete.mdx` + `plugins/admin.mdx` (trash filter, restore + delete-permanently, default-delete-to-trash, permission gates). gaps2 #35.

41. [x] **Markdown/code/RTE editor not populating the underlying input — FIXED (2026-06-20).** The EasyMDE (`markdown`), Quill (`rte`), and CodeMirror (`code`) editors mounted by `plugins/umbra-admin/src/assets/admin.js` rendered into their own DOM and only flushed to the backing `<textarea name=...>` on the NATIVE `submit` event — but HTMX serializes form fields at `htmx:beforeRequest`, which fires BEFORE the native submit in some code paths, so the server received the empty required field → "body is required, null" (the user's screenshot symptom: both the textarea and the editor showing after the error). Fix (admin.js): (1) continuous `change`/`text-change` sync — each editor writes to its textarea on every keystroke, so the value is always current; (2) a `_mountedEditors` registry of `{ta, flush}`; (3) `registerSubmitFlush(form)` wires the flush to BOTH `htmx:beforeRequest` AND `submit` (once per form, `WeakSet`-deduped), flushing only editors inside that form. DOMPurify sanitization preserved (server also ammonia-sanitizes). `cargo build -p umbra-admin` clean (asset embeds). Commit `8503382`. In-browser confirmation recommended (JS needs a real DOM + the editor CDN libs).

2. [x] **PostHog / analytics plugin — SHIPPED (2026-06-20).** New built-in plugin `plugins/umbra-analytics/` (`AnalyticsPlugin`, `name() = "analytics"`) wiring PostHog product-analytics into umbra. Config: project API key + host (default `https://us.i.posthog.com`) from `UMBRA_POSTHOG_API_KEY`/`UMBRA_POSTHOG_HOST` + builder overrides (`AnalyticsPlugin::new(key).host(...)`); installs an ambient `OnceLock<AnalyticsClient>` in `on_ready` (the framework's one-global pattern). Free functions `capture(distinct_id, event, properties)` + `identify(distinct_id, properties)` build the PostHog `/capture/` payload and **fire-and-forget** via `tokio::spawn` (never block the request; send errors logged, not propagated; clean no-op when no key is configured — never panics). Shared timeout'd `reqwest::Client` via `OnceLock` (mirrors `umbra-oauth::http`). Opt-in per-request `$pageview` middleware via `.capture_requests()` (`wrap_router` `from_fn`, default OFF, distinct_id from identity else anonymous). 7 unit tests (payload shape, no-op-without-key, client-with-timeout) + 1 `#[ignore]` live round-trip; doc page `plugins/analytics.mdx`. Added to the workspace `members`. Commit `405d09b`.

71. [x] **Concurrency / data-divergence hardening — FULLY CLOSED (2026-06-20).** `set_user_groups` transactional (`a4cdbd8`); `update_or_create`/`get_or_create` converge on UNIQUE race (`18b6a93`); `add_user_to_group`/`grant_user_permission` idempotent caught-UNIQUE (`c818cab`). The final sub-part — session `set_data` read-modify-write losing concurrent keys — is resolved by **Phase 2a (SessionStore)**: the request-scoped session loads the record ONCE at `session_layer` entry, `set_data` mutates it in memory (no per-call SELECT-modify-UPDATE), and a single save fires at exit, so intra-request key loss is eliminated (cross-request is last-write-wins, the standard session model). Phase 2a: `SessionStore` trait + `DbStore` (`5763cf7`), request-scoped session + `session_layer` load/save (`49c1740`), public API routed through it (`b6976fd`), `SessionsPlugin.store()` builder (`ff06898`) — 325-binary workspace gate green, behavior preserved (lazy creation #46, login rotation, sliding expiry, clearsessions all intact). → `reviews/race-conditions.md` + the 2a plan.

80. [x] **Plugin reliability & correctness (net-new) — FULLY CLOSED (2026-06-20).** signals async-panic isolation (`c186e71`); tasks non-retriable via typed `TaskError::HandlerNotFound` (`f9e19bd`); auth `is_superuser` propagated into `Identity` across all 4 paths (`710fe5b`); sessions opt-in sliding expiry + `clearsessions` command (`8989b89`); tasks orphan-reclaim visibility-timeout for at-least-once (`db76467`); and the final sub-part — permissions `table_app_label` model-collision — fixed by deriving the app_label from `#[umbra(plugin)]`: `Model::APP_LABEL` const (default `"app"`) emitted by the derive macro, threaded onto `ModelMeta.app_label`, and consumed by umbra-permissions (replacing the first-`_`-split heuristic), so a bare `post` and a plugin `app_post` no longer collide into `app.add_post`. ~12 hand-built `ModelMeta` literals + a `ModelMeta::default()` updated; `cargo test --workspace --no-run` clean (all literals + every model derive compile); collision tests `app_label_comes_from_model_meta_not_table_split` / `previously_colliding_models_now_diverge` / `distinct_models_get_distinct_app_labels` (`04cbd13`). → `plugins-review/{umbra-signals,umbra-sessions,umbra-tasks,umbra-permissions,umbra-auth}.md`.

55. [x] **collectstatic `StaticStorage` backend (local + s3) + manifest-hash cache-busting — SHIPPED (2026-06-20).** Two related features. (1) **Manifest-hash cache-busting** (Django's `ManifestStaticFilesStorage`): `collectstatic --hashed` writes a content-hashed copy of each collected file alongside the original (`css/app.css` → `css/app.<hash>.css`, hash = first 12 hex of SHA-256) and records a `static_root/staticfiles.json` manifest mapping logical→hashed. Loaded once at `App::build()` into an ambient `OnceLock` (same family as `settings`/`published_static`); `resolve_static_url()` + the `static()` template global return the hashed URL when a manifest is present and miss-through to the plain URL when not, so prod assets carry far-future cache headers with no template change. New core surface in `static_files.rs`: `content_hash`, `hashed_name`, `MANIFEST_FILENAME`, `load_manifest`/`manifest_lookup`/`manifest_loaded`/`set_manifest_for_tests`. (2) **Swappable `StaticStorage` backend** (Django's `STATICFILES_STORAGE`): `trait StaticStorage { put, exists } -> StaticError`, default `LocalStorage { root }` reproducing the filesystem copy exactly. `collect_into` refactored to write THROUGH the storage seam via the new `collect_into_with(contributions, root_dirs, static_root, storage, clear, hashed)`; the old `collect_into` is a thin LocalStorage wrapper (every existing collectstatic test stays green). Feature-gated **s3** backend in `plugins/umbra-static/src/s3_storage.rs` (`s3 = ["dep:rust-s3"]`, `rust-s3 0.35` blocking): `S3Storage::from_env()` reads `UMBRA_STATIC_BUCKET`/`UMBRA_STATIC_REGION`/`UMBRA_STATIC_ENDPOINT`/`UMBRA_STATIC_PREFIX` + standard AWS cred chain, uploads via `put_object_with_content_type_blocking`. `collectstatic` gained `--hashed` + `--storage local|s3` (also `UMBRA_STATIC_STORAGE`). Tests: core unit tests (hashed_name/content_hash/LocalStorage::put/hashed-collect manifest+copies/plain-collect-no-manifest), integration test `static_manifest_resolve.rs` (resolve_static_url hashed-vs-plain), s3 unit tests (object_key/prefix construction, no live bucket). Docs: `documentation/docs/v0.0.1/plugins/static.mdx` (--hashed cache-busting + --storage s3 sections). All `umbra-core`/`umbra-static` tests green incl. existing collectstatic suite; `cargo build -p umbra-static --features s3` + `cargo build --workspace` clean.

55. [x] **collectstatic → cloud / cache-busting — SHIPPED (2026-06-20).** Two parts on top of the existing `collectstatic` (which copied plugin `static_dirs()` into `static_root`). (1) **Manifest-hash cache-busting** (Django's ManifestStaticFilesStorage): `collectstatic --hashed` writes content-hashed copies (`name.<sha256-12>.ext`) alongside the originals + a `static_root/staticfiles.json` manifest; `resolve_static_url` (+ the `static()` template global) loads the manifest once at `App::build` and emits the hashed URL in prod (far-future cache headers), plain otherwise. (2) **Swappable `StaticStorage` backend**: trait `StaticStorage { put, exists }` with a `LocalStorage` default (`collect_into` refactored to write through it, behavior preserved) + a **feature-gated `S3Storage`** (`s3 = ["dep:rust-s3"]`, `rust-s3` blocking `put_object`) — `collectstatic --storage s3` / `UMBRA_STATIC_STORAGE=s3` + `UMBRA_STATIC_BUCKET` uploads to a bucket. Tests: manifest round-trip + hashed-URL resolution, LocalStorage put + collectstatic-through-storage, S3 key construction (no live bucket, `#[cfg(feature="s3")]`). Commit `601ab83`.

68. [x] **Plugin issues tracked distinctly — SHIPPED (2026-06-20) as part of the umbra_website plugin-moderation feature.** Previously plugin issues were undifferentiated `PluginComment` rows (a `[category]` body prefix). Now `PluginComment` carries `is_issue` / `is_public` / `is_resolved` (migration `plugin_directory/0007`); `create_report` sets `is_issue=true`; the plugin detail page renders a distinct **Issues** section separate from discussion **Notes**, each issue showing open/resolved status (green/amber badge + Lucide icon) with **Resolve**/**Reopen** controls. Part of the larger moderation feature: a `PluginModerator` model + `Plugin.created_by` ownership (set on submission) + `can_moderate`(owner OR moderator)/`is_owner` authz, 5 moderation routes (add/remove moderator, hide/unhide/flag note, resolve/reopen issue), and an owner-only moderator-roster UI — all visibility-gated. Commits `5b1fa02` (data+authz), `774292c` (routes), `018974a` (UI). Tests: `moderation_authz`, `moderation_actions`, `render_moderation` (owner sees controls, stranger doesn't), all green.

65. [x] **System / template pagination — SHIPPED (2026-06-20).** `umbra::pagination::{Paginator, Page}` in **umbra-core** (Django `django.core.paginator` parity; a core ORM-adjacent utility, NOT a plugin — so any view paginates with no registration). `Paginator::new(queryset, per_page)` counts once + slices each page via the by-ref `QuerySet` `limit`/`offset`/`count`/`fetch`; `Page` exposes `has_next`/`has_previous`/`has_other_pages`, `next`/`previous_page_number`, 1-based `start_index`/`end_index`, and `elided_page_range(on_each_side, on_ends)` (`1 … window … N` with `Ellipsis` markers); a serializable `PageContext` renders the nav from `{{ page }}`. Plus a core template global `querystring_with(query, key, value)` (rebuild a querystring replacing one key — the `?page=N&sort=…` fiddly bit) registered like `static`/`media_url`, and a reusable framework-agnostic `_pagination.html` partial. Backing change: `QuerySet<T>` gained T-bound-free manual `Clone`/`Debug` (as its doc already promised) so the paginator reuses the query per page. Tests (DB round-trip + elided-range/index math units) + doc page `web/pagination.mdx`. Commit `eb14237`. (Distinct from REST's JSON pagination, #79.)

90. [x] **Consolidate `umbra-auth::throttle` onto the core `RateLimiter` — SHIPPED (2026-06-21).** umbra-rest's API throttling introduced a reusable sliding-window primitive at `crates/umbra-core/src/ratelimit.rs` (`RateLimiter` + `Rate::parse`); `umbra-auth` shipped its own bespoke login/register throttle (`plugins/umbra-auth/src/throttle.rs`), written before the primitive existed, with a hand-rolled per-key sliding-window copy. This consolidation deletes that duplicate: `umbra-auth::throttle::Throttle` is now a thin wrapper holding a core `RateLimiter`, adapting its `RateDecision` down to the `bool` (allow/deny) the auth handlers need and exposing the clock-injectable `check_at`/`clear_at` the tests drive. The auth-specific policy stays in `throttle.rs` (secure-default budgets in `ThrottleConfig`, IP+username keying via `login_key`, the `enabled` master switch, the ambient `AUTH_THROTTLE` install, and the free helpers `login_throttle_check`/`login_throttle_clear`/`register_throttle_check`). `AuthThrottle::from_config` now builds `Throttle::new(login_max, login_window)` / `Throttle::new(register_max, register_window)` (each backed by `Rate::new(max as u32, window)` — `num == 0` denies the first attempt, preserving the old hard-lock semantics). The success-forgives path drove a new primitive method `RateLimiter::clear(&self, key)` (drops a key's entries from the bucket map) with its own unit test. Public auth throttle API (`Throttle`, `ThrottleConfig`, the three free helpers, the `AuthPlugin` builders `.login_throttle`/`.register_throttle`/`.disable_throttle`) and all behavior preserved: the existing auth throttle suite (`plugins/umbra-auth/tests/throttle.rs` + the in-module unit tests) passes UNCHANGED — N-attempts-then-throttled, independent keys, window-reset via injected clock, clear/success-forgives, disable, max-zero hard-lock. Docs: core `ratelimit.rs` module doc + auth `users-and-passwords.mdx` updated to say the auth throttle now shares the core primitive. gaps2 #90.

91. [x] **Postgres connection management / proper connection pooling — SHIPPED (2026-06-21).** Completed the partially-done pool config. Four new `Settings` fields (mirroring the existing `db_max_connections`/`db_acquire_timeout_secs` `#[serde(default)]` + `UMBRA_DB_*` env pattern): `db_min_connections: u32` (default 0), `db_idle_timeout_secs: Option<u64>` (default `Some(600)`), `db_max_lifetime_secs: Option<u64>` (default `Some(1800)`), `db_test_before_acquire: bool` (default true). The two `Option<u64>` timeouts use a `deserialize_zero_as_none` helper so `0`/empty env values map to `None` (= "disabled"). A shared `PoolConfig::resolve()` reads all knobs from `crate::settings::get_opt()` (falling back to the documented prod defaults when settings aren't installed) and `connect_postgres`/`connect_sqlite` both apply the full set to `PgPoolOptions`/`SqlitePoolOptions` (`max_connections`/`min_connections`/`acquire_timeout`/`test_before_acquire` always; `idle_timeout`/`max_lifetime` only when `Some`). `PoolConfig::log(backend)` emits one `tracing::info!` boot line with the effective config at pool-build time (the connect fns, since the pool is built in user code via `.database("default", db::connect(...))`). Graceful shutdown: `pub async fn umbra::db::close()` closes every registered pool (`Pool::close().await` per `DbPool` variant), re-exported through the facade. SQLite reads the same settings (doc-noted as effectively single-writer, so a big `max_connections` mainly helps readers). Tests: settings defaults + per-knob env overrides + `0`/empty→`None` (`settings.rs` unit tests), and `tests/db_pool_config.rs` (SQLite pool builds + `SELECT 1`/`SELECT 2` round-trip with the new knobs, close is terminal + non-panicking, ambient `close()` no-op when unset). Doc page `documentation/docs/v0.0.1/orm/connection-pooling.mdx`.

91. [x] **Postgres connection management / pooling — SHIPPED (2026-06-21).** Completed the connection-pool config (Postgres already had `max_connections` + `acquire_timeout`). Added 4 `Settings` knobs — `db_min_connections` (0), `db_idle_timeout_secs` (Some(600)), `db_max_lifetime_secs` (Some(1800)), `db_test_before_acquire` (true) — with `UMBRA_DB_*` env mapping (0/empty timeout → None) and prod defaults; a shared `PoolConfig::resolve()` applies the full set to BOTH `PgPoolOptions` and `SqlitePoolOptions`; one `tracing::info!` boot-log of the effective config; `umbra::db::close()` for graceful shutdown (facade re-export). Tests in `db_pool_config.rs` (defaults + per-knob env override + SQLite pool smoke + close). Commit `99d8798`. Requested by the user 2026-06-21.

76. [x] **Plugin-contract violation — `umbra-auth` depends on `umbra-rest` — FIXED (verified 2026-06-21).** The `Authentication`/`Identity` traits were lifted from `umbra-rest` into `crates/umbra-core/src/auth_contract.rs` (re-exported via the facade as `umbra::auth`). `umbra-auth` no longer declares an `umbra-rest` dependency (0 references in `plugins/umbra-auth/Cargo.toml`); dependencies point inward toward core, so a REST-free app with auth now compiles with zero serializer code — honoring the CLAUDE.md contract. (A few stale doc-comments in umbra-auth still reference "the umbra-rest Authentication" — cosmetic; the dependency itself is gone.) → reviews/architecture-modularity.md.

85. [x] **Plugin test-coverage holes (security/correctness-critical) — CLOSED (2026-06-21).** Every listed path now has a focused test. NEW (commit `8d9b2dd`): oauth callback **state-CSRF** rejection (`plugins/umbra-oauth/tests/state_csrf.rs` — forged-state→400 "oauth state mismatch" + zero `SocialAccount`, plus matching-state positive control); signals **async-panic-isolation** + `bulk_post_save`/`bulk_post_delete` (incl. empty-bulk-no-fire) + `m2m_changed` + **actor envelope** (`plugins/umbra-signals/tests/panic_bulk_m2m.rs`, 7 tests); tasks **concurrent double-claim guard** + **handler-panic recovery** (`plugins/umbra-tasks/tests/concurrency_panic.rs`, 2 tests — handler-panic was genuinely uncovered; `HandlerPanicked` is retriable, pinned `max_attempts=1`→terminal FAILED). PRE-EXISTING coverage confirmed: empty-key CSRF (`umbra-security/tests/empty_secret_key.rs`), email header-injection (`umbra-email/tests/header_injection.rs`), admin permcheck-authz + `.at()` base-path + non-i64-PK (`umbra-admin/tests/{phase3_action_permissions,sidebar_perm_gate,base_path_fragments,phase3_actions_str_pk}.rs`). No broken behavior found. → `plugins-review/*.md`.

92. [x] **umbra-media replace-cleanup — delete the OLD blob when a file field is updated to a new key — SHIPPED (2026-06-22).** Split out of gaps2 #82 (delete-cleanup landed 2026-06-21). Implemented via **option (a)**: a real `pre_update`/`post_update`-with-old-row signal pair that snapshots the pre-UPDATE row. New core signal surface in `crates/umbra-core/src/signals.rs`: `has_subscribers(name) -> bool` (true if ≥1 sync OR async handler under `name`); `emit_pre_update<M>(previous, instance)` / `emit_post_update<M>(previous, instance)` + `_by_table` variants, signal names `pre_update:<table>` / `post_update:<table>`, payload `{ "previous": <old row JSON>, "instance": <new row JSON>, "actor": ... }`. These fire **only on UPDATE** (never INSERT); the existing `pre_save`/`post_save` (which fire on both) are untouched — `*_update` are purely additive. **Subscriber-gated old-row read**: in the typed `Manager::save` UPDATE branch (`crates/umbra-core/src/orm/queryset/mod.rs` ~4643), BEFORE running the UPDATE, gate on `has_subscribers("pre_update:<t>") || has_subscribers("post_update:<t>")`; only then SELECT the current row by PK (the old snapshot) via a sea-query `Query::select` over `T::FIELDS` fetched into `T`. `pre_update` fires before the UPDATE, `post_update` after, each with (old, new). When no `*_update` subscriber exists the extra read is skipped entirely — zero overhead on the common UPDATE path. A best-effort TOCTOU window between snapshot and UPDATE is accepted (documented). The **dynamic `DynQuerySet::update_json`** path was deferred (it's a filter-chain bulk update matching a PK *set* and firing `bulk_post_save`, not a clean per-row save with a known PK — noted as a follow-up in the signals doc). Typed helpers in `plugins/umbra-signals/src/lib.rs`: `on_model::<M>().pre_update(|previous: M, instance: M| ...)` / `.post_update(...)` decoding both `"previous"` and `"instance"` keys via a shared `decode_key`. umbra-media (`plugins/umbra-media/src/lib.rs`): `register_cleanup` now wires BOTH a `pre_delete` (delete-cleanup) AND a `post_update` (replace-cleanup) handler; the replace handler compares `previous[col]` vs `instance[col]` per file column and `delete_blob_best_effort(old_key)` when they differ and the old key is non-empty (same key → no delete; new file → old blob removed; non-file-column update → no delete; INSERT → no fire). Tests: `crates/umbra-core/tests/update_signals.rs` (5: post_update fires on UPDATE with correct previous+instance, doesn't fire on INSERT/create, has_subscribers false→true→false, gated no-read path still updates + only post_save fires, create fires no update signals) + `plugins/umbra-media/tests/replace_cleanup.rs` (4: A→B deletes A keeps B, same-key deletes nothing, non-file update deletes nothing, INSERT deletes nothing). All `umbra-core`/`umbra-signals`/`umbra-media` suites green incl. existing pre_save/post_save tests (behavior unchanged); `cargo build --workspace` clean. Docs: `orm/signals.mdx` + `plugins/signals.mdx` (new pre_update/post_update, payload, fire-on-UPDATE-only, subscriber-gated read) + `plugins/media.mdx` + `orm/file-image-fields.mdx` (replace-cleanup now works, no more orphan on file replace). gaps2 #92.

92. [x] **umbra-media replace-cleanup — SHIPPED (2026-06-21, `35a3920`).** Implemented the CLEAN way: new ORM `pre_update`/`post_update` signals (payload `{ previous, instance, actor }`, fire on UPDATE only, never INSERT) snapshot the pre-update row, with the old-row SELECT **gated on `signals::has_subscribers`** — zero overhead when no `*_update` subscriber is registered (the common case), avoiding the per-save SELECT #82 warned against. `on_model::<M>().pre_update(|old, new| …)` / `.post_update(…)` typed helpers. umbra-media's file-cleanup opt-in now also registers a `post_update` handler that deletes the OLD blob when a file-field key changes (best-effort; same key → no-op). Existing `pre_save`/`post_save` unchanged (backward-compatible). Tests: `crates/umbra-core/tests/update_signals.rs` (fires-on-update-not-insert, `previous` carries the old value, `has_subscribers` gating) + `plugins/umbra-media/tests/replace_cleanup.rs` (replace deletes old blob, same-key no-op, non-file-update no-op, insert no-op). The typed `Manager::save` path is covered; the dynamic/admin `DynQuerySet` bulk-update path is deferred to #93.

82. [x] **Plugin completeness — missing breadth vs framework peers — FULLY CLOSED (2026-06-21).** Every sub-item shipped across the session: `umbra-sessions` SessionStore family (DbStore/CookieStore/RedisStore); `umbra-auth` password-strength validators + login/register throttle; the full `umbra-tasks` Celery suite (backoff/timeout/eta/beat/results/status/priority/admin+retry); `umbra-cache` redis backend + `cache_page` middleware; `umbra-static` collectstatic `--hashed` + S3 backend; `umbra-media` delete-cleanup (#82) + replace-cleanup (#92) + **streaming Storage** (`store_stream`/`retrieve_stream` + mid-stream `max_size`, PR #1 / `abecc13` — the final piece); `umbra-realtime` Last-Event-ID reconnect + connection cap; `umbra-rest` throttling + API versioning (UrlPath + AcceptHeader) + transactional bulk endpoints. → `plugins-review/*.md`.
