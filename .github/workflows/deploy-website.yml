# Deploy the umbra marketing/community site (umbra_website/ — an umbra app
# backed by Postgres).
#
# PLACEHOLDER: manual-trigger only, no real deploy steps yet. Intentionally NOT
# on push — wiring the trigger + a home for secrets now so the real build/deploy
# can drop straight in later. Fill in the TODO step (build, migrate, ship) when
# the hosting target is decided.

name: deploy-website

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-website
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: TODO — build and deploy umbra_website
        run: |
          echo "Placeholder: no deploy configured yet."
          echo "TODO: build umbra_website (cargo build --release), run migrate,"
          echo "      and ship to the chosen host. Add deploy secrets as env."
