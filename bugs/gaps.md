## Status

**51 of 52 closed (gaps 1-42, 44-52; open: 43 — plugin-extension backlog).** Last updated 2026-06-02.

| # | Gap | Status |
|---|-----|--------|
| 1 | Email plugin attachments | done — `abf502c` |
| 2 | Django-style 404 / 500 pages | done — `e42f408` |
| 3 | CLI: `umbra startproject` / `startapp` + dispatch shape | done — `95f0709` + `e5645a4` |
| 4 | Plugin docs + REST custom field rendering | done — `1cdbc18` (docs) + `dc84dbf` (code) |
| 5 | OpenAPI customisation params (`.description()`) | done — `e42f408` |
| 6 | Security audit across system + plugins | done — `cd656d5` |
| 7 | No `.create()` method on `Model` objects | done — `9ab3c00` |
| 8 | No `.update()` method on `Model` objects | done — `9ab3c00` |
| 9 | No `.delete()` method on `Model` objects | done — `9ab3c00` |
| 10 | No bulk create/update/delete on `Model` objects | done — `9ab3c00` |
| 11 | Complete plugin example (ORM access) | done — see commit log |

All known gaps closed. New gaps land below as they're surfaced.

## Seen/Known gaps

1. [x] Email plugin lacks the ability to do file attachments
       — Shipped: `Attachment { filename, content_type, data: Vec<u8> }` struct + `EmailMessage::attach(filename, content_type, bytes)` builder. Bytes-only by intent (no path-loading, no auto content-type, no inline images) — users with files do `std::fs::read(path)?` themselves; users wanting any of the deferred shapes get them when a real consumer surfaces a need. When attachments are present, `compose()` wraps the existing body in `multipart/mixed` with the body part(s) (text-only / html-only / alternative) nested inside per RFC 2046. 11 new tests in `tests/attachments.rs` pin the on-wire MIME shape (multipart/mixed wrapper, Content-Disposition: attachment, base64 transfer encoding for binary payloads, alternative-inside-mixed for text+html+attachment). See `documentation/docs/v0.0.1/plugins/email.mdx`.
2. [x] Django has special way of doing pages ie 404, 500 (Internal server error) which is quite symbol and direct
       — Shipped via `App::builder().not_found_template("404.html")` and `.server_error_template("500.html")`. Composes with `slash_redirect`. See `documentation/docs/v0.0.1/web/error-pages.mdx`.
3. [x] The current version does not make use of the cli. Ie instead of `cargo run`, you must use `umbra serve` to run the server. Or even better, I need something like umbra startproject, startapp commands to set up a new project like django. We need to make it easy to setup the app that way with `apps`. We talked about this, it might be implemented but docs are out of sync by far.
       — Shipped via commits 95f0709 (code) + e5645a4 (docs). `cargo install umbra-cli` installs a global `umbra` binary with `startproject` + `startapp`. User binaries call `umbra_cli::dispatch(app).await` from `main.rs` to host the management subcommands (serve / migrate / makemigrations / inspectdb / dumpdata / loaddata). `cargo run -- serve` is the project-local way to invoke them. See `documentation/docs/v0.0.1/cli/startproject.mdx` + the rewritten `management-commands.mdx`.
4. [x] Docs are out of sync with the current version ie how to use plugins. There is no plugin section on how to use `rest` plugin for example. So the rest plugin should make it easy to create rest views with minimal effort and with the ability to use custom fields like in django we define the view and then the fields, currently it returns the whole struct/model. DRF classes make it easy to even redefine fields ie you can hide emails ie using a method like get_email(obj) and it returns "***@mail.com" instead of the actual email by spliting at the `@` symbol and replacing the username with `***`.
       — Docs portion: rest.mdx + openapi.mdx + rls.mdx + sessions.mdx + tasks.mdx + admin.mdx + web/ section all landed (commit 1cdbc18).
       — Custom field rendering shipped: `RestPlugin::hide(table, field)`, `.transform(table, field, |v| ...)`, `.computed(table, name, |row| ...)`. Per-row, per-table, applied in hide→transform→computed order on every outbound response. The example `transform("user", "email", mask_email)` in rest.mdx now matches exactly the DRF `get_email(obj)` pattern called out in the gap. 8 new tests in `tests/field_overrides.rs`.
5. [x] The openapi plugin should allow the user to pass in params to customize the openapi schema. ie the base path, title, version, description, etc.
       — `.description(s)` added in commit e42f408; `.at()`, `.title()`, `.version()`, `.exclude()` already existed.
6. [x] Security audit across the system and the plugins
       — Shipped via `cd656d5`. Four parallel audit agents (ORM/SQL, Auth+Sessions+RLS, Templates+XSS+Headers, CLI+Scaffolding+IO) produced triaged reports. Combined findings: 2 HIGH (both fixed), 6 MEDIUM (all fixed or documented), 8 LOW (key ones fixed, rest acknowledged in canary comments). HIGH fixes: session tokens SHA-256 hashed before DB storage so a DB leak doesn't surrender live sessions; CSRF compare uses `subtle::ConstantTimeEq`. MEDIUM fixes: defense-in-depth for dev secret_key when bind_addr isn't loopback; `model.table` quote-doubled in REST/admin/backup SQL; RLS using/with_check raw-SQL trust boundary documented with `# SQL injection warning` sections; admin sanitises sqlx errors to a fixed string (logs full error server-side); Content-Type in error pages derived from render result (not template-was-set). LOW: CRLF redirect canary comment, autoescape extension-whitelist sync warning, ConsoleBackend warns in non-dev environments. Major verified-clean surfaces: argon2id w/ per-password CSPRNG salt, account-enumeration defense, HttpOnly+Secure+SameSite cookie flags, autoescape on by default for HTML, no open redirects, scaffold name validation blocks path traversal, no secret logging, lettre's typed APIs for email headers (no manual construction → no CRLF injection in From/To/Subject/filename).
7. [x] There is no `.create()` method on `Model` objects
       — Shipped via `Manager::create(instance)` in commit 9ab3c00. INSERT + RETURNING * for the populated row. Autoincrement sentinel: PK == 0 (ints), nil UUID, or empty String are omitted from the INSERT so the DB assigns them; explicit non-default PKs are bound as supplied. Requires `T: serde::Serialize` (which models already derive for REST).
8. [x] There is no `.update()` method on `Model` objects
       — Shipped via `QuerySet::update_values(map)` in commit 9ab3c00. Takes a `serde_json::Map<String, Value>` of column → new value. PATCH semantics (absent keys keep current values). PK in the map is silently skipped to prevent identity rewrite while filtering on the old PK. Unknown columns raise `WriteError::UnknownColumn` early.
9. [x] There is no `.delete()` method on `Model` objects
       — Shipped via `QuerySet::delete()` in commit 9ab3c00. Applies accumulated filter predicates as WHERE; returns affected-rows count. Without filter calls, deletes every row — same semantics as raw SQL DELETE FROM.
10. [x] There are no bulk create/update/delete methods on `Model` objects
       — All covered by gaps 7-9's primitives: `Manager::bulk_create(Vec<T>)` produces one multi-VALUES INSERT (cheap, batched); `QuerySet::filter(...).update_values(map)` and `QuerySet::filter(...).delete()` are naturally bulk via the WHERE clause. 10 new tests in `tests/model_writes.rs` + 5 unit tests in the `crate::orm::write` module pin the JSON→sea_query::Value dispatch. See the new "Writing rows" section in `documentation/docs/v0.0.1/orm/models.mdx`.
11. [x] We need to go deeper on how to create a plugin/an app - http://localhost:5173/docs/v0.0.1/plugins/plugins-are-apps ie how to get access to the ORM from a plugin. So do a short but complete example of a plugin here, not in split parts, a single codeblock.
       — Shipped: new "## A complete plugin" section in `documentation/docs/v0.0.1/plugins/plugins-are-apps.mdx` with a single-codeblock `BlogPlugin` example covering: model declaration in the plugin file, two route handlers that hit `Post::objects().fetch()` and `.first()` directly (no `State<DbPool>` or pool plumbing — the ORM is ambient), a `rest_resource()` function that exposes the plugin's REST customization (hide/transform/computed) for `RestPlugin::default().resource(...)` to pick up, and an `on_ready` hook that bridges sync→async via `Handle::current().block_on(...)` to seed initial rows. The body wraps with the three "what matters here" points: ambient ORM, plugin-bundled REST customization, and the sync-on_ready / async-sqlx bridge.
12. [x] In the docs, http://localhost:5173/docs/v0.0.1/plugins/plugins-are-apps, add a section for Rest Plugin. Actually, the better way to do it to avoid mixing things up, create a folder maybe examples/plugins/ and copy the `BlogPlugin` example there for the Rest plugin and now show all the rest plugin features.
       — Shipped: `examples/plugins/blog/` is a standalone runnable Cargo project (not a workspace member) that demos every plugin concern in one place: model declaration + `#[derive(Model)]`, `Plugin::models()` auto-registration (no `.model::<Post>()` in main.rs), HTTP routes (`Plugin::routes()` at `/blog` and `/blog/{id}`), REST customisation bundled in `rest_resource()` (hide/transform/computed + the new `@action` endpoints `recent` + `publish`), and a seed-on-empty `seed()` helper called after `migrate::run()`. `on_ready` does an `info!` log instead of seeding because schema isn't applied yet when it fires — that distinction is the only non-obvious bit. README in `examples/plugins/blog/README.md` covers the running shape (`cargo run` from inside the directory; curl examples for each endpoint). End-to-end verified — list shows masked `***@example.com`, `recent` returns N newest, `publish` flips the boolean.
13. [x] Something else, currently, we register the plugin, the models for that plugin and other resources for the plugin, can we like just register the plugin and the rest get autoregistered?
       — Already shipped at the code level: `Plugin::models()` was wired through `App::builder()` at M7 (`crates/umbra-core/src/app.rs:313,335`), and `tests/plugin_contract.rs::plugin_contract_meta_lands_in_registry` proves a plugin-contributed model lands in `models_for_plugin("plugin_name")` with no `.model::<T>()` line. Docs were lying — `plugins-are-apps.mdx` still said "for now register with `.model::<blog::Post>()`; auto-discovery lands when M7 ships." Fixed: the BlogPlugin example now overrides `Plugin::models()`, the `main.rs` snippet drops the redundant `.model::<Post>()`, and a new "One `.plugin(...)` line is enough" call-out explains when `.model::<T>()` is the right tool (models in main.rs with no owning plugin) vs `Plugin::models()` (plugin-owned models, the default).

```rust
let app = App::builder()
    .settings(settings)
    .database("default", pool)
    .model::<blog::Post>()                       // register the model
    .plugin(blog::BlogPlugin)                     // JUST REGISTER THE PLUGIN and all models in it get registered to the app, same with the resource if any there. So we need a way to decouple plugins ie REST (I know resources in this case are just for the REST plugin) in this case to avoid double registration of it ie plugin x might use the same etc. Maybe register resources somewhere within the plugin itself, I don't know. We just need to make it easy and direct.
    .plugin(
        umbra_rest::RestPlugin::default()
            .resource(blog::rest_resource()),    // REST customisation
    )
    .build()?;
```

Though we can still keep the current state as is, if it has no harm to the system in general but do the updates

14. [x] How do we use different models in different areas of the app ie BlogPlugin vs CategoryPlugin, ie we need to reuse categories in other plugins etc. This question goes hand in hand like, how do we do foreign keys, many-to-one, one-to-many, many-to-many etc. In django we also use foreign keys extensively for many-to-one, one-to-many, many-to-many relationships without much hassle and this automatically helps in validation and data integrity.
       — Shipped: `ForeignKey<T: Model>` field type. Stores `i64` referencing `T::TABLE`'s PK. The derive emits `BIGINT REFERENCES "<T::TABLE>"("id")` DDL (both SQLite and Postgres), a `ForeignKeyCol<Owner>` constant in the sibling module (`.eq`, `.ne`, `.in_`, `.asc`, `.desc`), and `FieldSpec.fk_target = Some(T::TABLE)`. At runtime: `.id() -> i64`, `.set(i64)`, `From<i64>`, and async `.resolve(&pool) -> Result<T, sqlx::Error>`. Serialises as a plain integer. Tests: `tests/foreign_keys.rs` (10 tests). Docs: `documentation/docs/v0.0.1/orm/relationships.mdx` + column-types.mdx FK row + models.mdx PK table update. Deferred: non-i64 FK targets, ON DELETE beyond RESTRICT, reverse accessors, M2M. See `docs/specs/relationships.md` for the full cut record.
15. [x] What have we done to make sure the system can handle many requests safely. Things like proper database access, proper file handling, proper static handling for css/js and static files through cache (this is sensitive, should have a shutdown config ie static cache off to allow better development). Like Django does lots of things right, if its scaling, I can add redis cache and cache decorators to views
       — Shipped: three cooperating features in `plugins/umbra-cache/` and `plugins/umbra-static/`.
       (1) **Redis backend** (`plugins/umbra-cache/src/lib.rs`, behind `--features redis`). `Cache::redis(url)` connects via `redis::aio::ConnectionManager` (auto-reconnect). `RedisBackend` implements the existing `CacheBackend` trait exactly. TTL stored natively via `SETEX` so expiry is server-side. `Cache::sqlite()` return type widened to `Result<Self, CacheError>` (breaking, but correct — SQLite constructor can fail). New `CacheError` enum with `Redis(redis::RedisError)`, `Sqlx(sqlx::Error)`, `Other(String)` variants. `Cache::redis(url)` constructor gated on `#[cfg(feature = "redis")]`. Existing in-memory and SQLite backends unchanged except the `SqliteBackend::new` / `sweep` return type is now `CacheError` instead of `sqlx::Error`.
       (2) **`cache_page` view middleware** (`plugins/umbra-cache/src/cache_page.rs`, new). `cache_page(Duration)` returns a `CachePageLayer` implementing `tower::Layer`. `CachePageService` wraps the inner service: on GET/HEAD it checks `AMBIENT_CACHE` for a hit, on miss calls through, collects the body, serialises response headers + body in a compact binary format, and stores. Bypass rules: non-GET/HEAD methods, non-200 status, `Cache-Control: no-store`, `Set-Cookie`. Cache key: `cache:page:<METHOD>:<URI>`. `.with_cache(cache)` method on `CachePageLayer` for test injection without initialising the ambient OnceLock. Ambient fallback: silently a no-op if `CachePlugin::init` was never called.
       (3) **Static cache headers** (`plugins/umbra-static/src/lib.rs`). `StaticPlugin::max_age(Duration)` builder method adds `Cache-Control: public, max-age=N` via `tower_http::set_header::SetResponseHeaderLayer`. Dev-mode opt-out: when `umbra::settings::get_opt()` returns `Environment::Dev`, the effective max-age is forced to 0 regardless of the configured value. Requires adding `get_opt() -> Option<&'static Settings>` to `umbra-core/src/settings.rs` and re-exporting from `umbra::settings`.
       Tests: `plugins/umbra-cache/tests/cache_page.rs` (7 tests: second GET served from cache, POST bypass, no-store bypass, Set-Cookie bypass, query-string isolation, 404 not cached, Content-Type preserved). `plugins/umbra-cache/tests/redis_backend.rs` (8 tests, all gated on `#[cfg(feature = "redis")]` + `REDIS_URL` env — graceful skip when absent). `plugins/umbra-static/tests/cache_headers.rs` (4 tests: no header without max_age, correct header with max_age, max-age=0 emitted explicitly, files served in both modes). All 22 new tests pass; all 16 pre-existing tests still pass. Docs: `documentation/docs/v0.0.1/plugins/cache.mdx` (new), `documentation/docs/v0.0.1/plugins/static.mdx` (new). Deferred: memcached backend, distributed cache invalidation, ETag/304 conditional caching in cache_page, Vary-header awareness.
16. [x] Minininja already supports context, can we have the same for us here ie when returning a page, we can return it with a context that contains the data needed for the page - This seems to be done already
17. [x] There is no `.get` method on the model to get a single record by id or by any other field. Also we need the rest of the methods like `.first` if not yet implemented.
       — Shipped: `QuerySet::get()` + `Manager::get(predicate)` + `_pg` variants. Returns `Result<T, GetError>` with three variants (`NotFound`, `MultipleObjectsReturned`, `Sqlx`) — Django's exactly-one shape. Internally `SELECT ... LIMIT 2` to distinguish "one" from "many" cheaply. `.first()` was already implemented. 3 new tests in `tests/queryset.rs`. Docs: new "Terminals" subsection in `documentation/docs/v0.0.1/orm/models.mdx`.
18. [x] Django admin is so extensive ie you get alot of things out of the box by extending it ie you can add custom views, filters, actions, forms, etc. Do we have the same level of extensibility in our admin interface?
       — Shipped: `AdminConfig` builder in `plugins/umbra-admin/src/config.rs` (new). Per-model registration via `AdminPlugin::default().register(...)`. Six customisation methods covering the high-leverage 80% of Django's ModelAdmin: `list_display(&[col, ...])` picks visible columns; `list_filter(&[col, ...])` produces filter facets in the right sidebar; `search_fields(&[col, ...])` enables an `?q=` search box that ANDs `LIKE %term%` across the listed columns; `ordering(&["-published_at", "title"])` sets default sort (minus prefix = DESC); `actions(vec![Action::new("publish", handler)])` for bulk actions on selected rows with a built-in `Action::delete_selected()`; `readonly_fields(&[col, ...])` renders form fields as read-only. The admin list template (`templates/list.html`) gained filter sidebar, search box, ordering controls, and an actions dropdown. 6 new tests in `plugins/umbra-admin/tests/extensibility.rs`. Docs: new "Customizing the admin" section in `documentation/docs/v0.0.1/plugins/admin.mdx`. Deferred: fieldsets (form-section grouping), inlines (related-model editing on the parent form), prepopulated_fields, raw_id_fields.
19. [x] Defer this but have it somehwere as a pending task - We shall use tailwind css for everything, we are not going to rebuild any css by us for the admin interface, the default pages like 404, 500, etc. will use tailwind css by default.
       — Shipped piecewise across three landings: gap 35 (default 404/500 templates use inline Tailwind utility classes with a `<style>` reset fallback for when Tailwind isn't loaded), gap 45 (`templates/wrapper.html` holds the Tailwind CDN + design-token block + theme persistence IIFE, both `base.html` and `login.html` extend it), gap 47 (`AdminPlugin::default().site_title(...).brand_color(...)` chainable builders feed Tailwind's `--primary` token at boot). No remaining piece — the admin and the framework's default error pages are both Tailwind-first. The "tailwind CDN is dev-only" callout in `documentation/docs/v0.0.1/plugins/admin.mdx` flags the prod-build follow-up that's not part of this gap.
20. [x] When I start a project, do I get a complete scaffold out of the box? ie all the basic setup and configuration already done for me?
       — Shipped: `scaffold_project` in `crates/umbra-cli/src/scaffold.rs` rewritten from a hello-world skeleton (~30-line `main.rs`, 3 templates) to a complete blog-style demo. `umbra startproject myapp` now produces: `Cargo.toml` with all built-in plugins listed (umbra-auth, umbra-sessions, umbra-admin, umbra-rest, umbra-openapi); `src/main.rs` (~120 lines) declaring a `Post` model with `ForeignKey<AuthUser>`, registering all 5 plugins including `RestPlugin` with `ResourceConfig::new("post").enable_filters()`, three routes (`/` public, `/api/posts` JSON, `/dashboard` gated with `login_required_html("/login")`), a `LoggedIn<AuthUser>` extractor in the dashboard handler, an `umbra::transaction` wrapping the ORM fetch, and `auto_migrate()` on boot; `.env` with working `DATABASE_URL`/`BIND_ADDR`/`SECRET_KEY`; `README.md` with a tour table and run instructions; `templates/base.html` with Tailwind CDN; `templates/home.html`; `templates/dashboard.html`; `templates/404.html`; `templates/500.html`. Total files: 12 (was 8). 4 new tests in `crates/umbra-cli/tests/scaffold.rs` (12 total): file set, Cargo.toml plugin refs, main.rs plugin markers, Tailwind CDN, name substitution. Docs: `documentation/docs/v0.0.1/cli/startproject.mdx` rewritten — "what you get" list replaced with a surface table, new "Tour of the scaffold" section walking through models/app-wiring/handlers/templates/.env. Also fixed pre-existing `AdminPlugin` struct/unit typo in `plugins/umbra-admin/tests/integration.rs` (`.plugin(AdminPlugin)` → `.plugin(AdminPlugin::default())`) that was blocking `cargo clippy --all-targets`.
21. [x] DRF views, ie ApiView, has a way of extending and adding custom actions. Can we be able to have the same ie in the same viewset, I can have a method decorated by `@action` that is a custom action that is not part of the standard CRUD operations?
       — Shipped: `ResourceConfig::action(name, method, scope, handler)` is the umbra-rest equivalent of DRF's `@action`. `ActionScope::Collection` mounts at `/api/<table>/<name>/`; `ActionScope::Detail` mounts at `/api/<table>/<id>/<name>/`. The handler is any `async Fn(ActionContext) -> Result<Value, ActionError>`. The `ActionContext` carries `table`, `name`, `pk` (detail-scope), `identity`, `body`, and `query`. `ActionError` variants (`BadInput`, `NotFound`, `Unauthenticated`, `Forbidden`, `Internal`) map to the existing JSON error envelope. The Permission gate runs with `Action::Custom(name)`, which `permission.rs` now models alongside the five CRUD variants; existing impls (`AllowAny`, `IsAuthenticated`, `IsStaff`, `ReadOnly`) handle Custom via the trait's `&Action` signature without further changes. The trait method changed from `check(&self, Action, ...)` to `check(&self, &Action, ...)` because `Custom(String)` makes `Action` no longer Copy — this is a public-API breaking change for downstream Permission impls but worth the trade. 6 new tests in `plugins/umbra-rest/tests/actions.rs` + 1 gated test in `actions_gated.rs`. Docs: new "Custom actions (`@action`)" section in `documentation/docs/v0.0.1/plugins/rest.mdx`.
22. [x] How do we do templates discovery? ie a template in app1 might be referenced in a view in app2 ie referencing base.html in different apps/plugins. But remember, we should not override templates in the same app. (http://localhost:5173/docs/v0.0.1/templates/rendering-html - There is a deffered section here for reference, after fix update the doc)
       — Shipped: new `Plugin::templates_dirs(&self) -> Vec<PathBuf>` hook (default empty). `App::build()` collects the app's own `templates_dir` plus every plugin's `templates_dirs()` into one ordered search list. The minijinja loader walks it in registration order with **first-match-wins**: a plugin can reference another plugin's `base.html` via `{% extends "base.html" %}` and resolve. Same-name collisions across plugins emit a `tracing::warn!` at boot and the first registration wins (matches Django's `APP_DIRS` loader). 2 new tests in `tests/template_discovery.rs`. Docs: deferred note removed from `documentation/docs/v0.0.1/templates/rendering-html.mdx`; new "Cross-plugin templates" section explains the search order and collision policy. Deferred: namespaced lookups (`admin:base.html`), hot reload via `minijinja-autoreload`, `Plugin::on_ready` custom filter registration.
23. [x] Update the docs landing page to have complete SEO as to why this is the best Rust web framework for developing complete web applications with APIs and some features. Remove the term "Django Shape" from the landing page. Replace it with something better that reflects the actual capabilities of the framework.
       — Shipped: `documentation/src/routes/+page.svelte` rewritten. "Django shape. Rust spine." replaced with "Ship the whole app. In Rust." — capability-led, not analogy-led. Hero subtitle now names the actual surface area inline: managed migrations, REST API, admin UI, auth + sessions, background tasks, email. Feature grid expanded from 3 cards (Plugins / Migrations / Safety) to 6 (ORM + migrations, REST + DRF-shape ResourceConfig + @action, Admin UI, Auth + sessions + security defaults, Tasks + email, Plugin architecture). New "Why Umbra" section frames the framework against the "here's a router" baseline most Rust frameworks stop at. SEO: `<title>` + `<meta description>` + `<meta keywords>` (Rust web framework, Rust ORM, Rust admin panel, etc.), OpenGraph + Twitter Card tags, `<link rel="canonical">`, and a JSON-LD `SoftwareApplication` structured-data blob. `specra.config.json` description also rewritten away from "Django-shape." svelte-check + vite SSR/browser bundles transform clean for the page; the build's mdx-cache failure is a pre-existing vendor issue in node_modules/specra unrelated to this edit.
24. [x] How do we handle cases where a user deleted some migrations but in the db there are migrations. We need a strong migration flow and logic
       — Shipped: four-state drift model in `crates/umbra-core/src/migrate.rs`. `detect_drift(plugin, applied_set, plugin_dir)` returns `Vec<MigrationEntry>` where each entry carries one of `Applied`, `Pending`, `AppliedButMissing` (critical: file gone, tracking row present), or `OutOfOrder` (file restored after a later migration was applied). `DriftReport` aggregates entries with `has_critical_drift()` / `missing_on_disk()` accessors. `run_checked_in(dir, allow_drift)` wraps the existing apply loop: errors with `MigrateError::DriftDetected { missing }` when drift is critical and `allow_drift = false`; logs per-missing-file warnings and proceeds when `allow_drift = true`. `show_in` upgraded to the four-state `[X]` / `[ ]` / `[!]` / `[?]` display. New CLI surfaces on `umbra migrate`: `--fake <plugin/name>` (mark one migration applied without SQL via `fake_apply_in`), `--fake-initial` (per-plugin: if 0001 tables exist, fake-apply — SQLite path checks `sqlite_master`, Postgres path checks `information_schema.tables`), `--allow-drift`. `showmigrations` now prints the pending count at the end. `umbra-admin` type-annotation fix bundled because the pre-existing `umbra-auth` generics refactor was already in the working tree and broke the `authenticate` call-site. 9 tests in `crates/umbra-core/tests/migrate_drift.rs`: 4 pure-function tests (no pool), 5 pool-needing tests serialized with a `tokio::sync::Mutex`. Docs: `documentation/docs/v0.0.1/migrations/migration-drift.mdx` (new page: three drift states, four flags, two worked examples); `documentation/docs/v0.0.1/cli/management-commands.mdx` updated with migrate flags table and four-state showmigrations reference.
25. [x] How do we handle primary keys? when they are ints and when they are uuids, strings? By default, in django, a user does not need to specify a primary key field, it is auto-generated. In our case, we need to be able to specify a primary key field by default too which should be a bigint and then make sure its autoincrementing. For UUIDs, they should also be auto-generated. So the user does not need to specify a primary key and if they do, we help them handle the autogeneration. This means for all ints, we need to enable auto-increment feature on them
       — Shipped: `id: i64` / `id: i32` → `INTEGER PRIMARY KEY AUTOINCREMENT` (SQLite) / `BIGSERIAL PRIMARY KEY` (Postgres). `id: uuid::Uuid` → `TEXT PRIMARY KEY` (SQLite) / `UUID PRIMARY KEY` (Postgres), no auto-default at v1. `id: String` → `TEXT PRIMARY KEY`, user-supplied. `Manager::create` omits the PK column when it equals the sentinel (0 for ints, `Uuid::nil()` for UUID, `""` for String) so the DB autoassigns. The sentinel logic was already in `orm/write.rs::is_default_pk`; this gap adds schema-emission tests and documents the rule. Tests: `tests/pk_autogen.rs` (14 tests). Docs: `documentation/docs/v0.0.1/orm/column-types.mdx` PK table + `models.mdx` PK auto-generation rule table.
26. [x] In django, for view, we had this decorator called `login_required` (It has something like redirect_to or auth_page) that required the user to be logged in to access the view. Do we have the same functionality in our framework?
       — Shipped: two complementary shapes in `plugins/umbra-auth/src/login_required.rs`. (1) `LoggedIn<U: UserModel>` axum extractor — drop in a handler signature; resolves the `umbra_session` cookie into the authenticated user of type `U`, or returns the configured rejection. Generic over any `UserModel` so `LoggedIn<TenantUser>` works without adapters. (2) `LoginRequiredLayer` tower middleware — wraps a Router subtree; gates every route before the inner handler is called; injects the `LoginRequired` config into request extensions so downstream `LoggedIn<U>` extractors read it automatically (no double check). (3) `LoginRequired` config struct with two shapes: `LoginRequired::API` (401 JSON + `WWW-Authenticate: Bearer`, default) and `LoginRequired::html("/login")` (302 to `/login?next=<uri>`). `.no_next()` drops the query parameter. Convenience constructors: `login_required()` (API shape) and `login_required_html(path)` (HTML shape), both returning `LoginRequiredLayer` which implements `tower::Layer`. The layer authenticates by querying only the `session` table (user_id IS NOT NULL, expires_at > now); the extractor does the full user hydration from `U::TABLE`. umbra-auth does not depend on umbra-sessions; the SHA-256 token hash is re-implemented locally (~5 lines) to keep the dep arrow pointing the right way. 7 new tests in `plugins/umbra-auth/tests/login_required.rs`. Docs: new "Guarding views" section in `documentation/docs/v0.0.1/plugins/auth.mdx` (above "Swapping the user model"). Deferred: `permission_required(perm)` and `staff_member_required` — depend on gap 33 (groups + content-type model) which does not exist yet.
27. [x] In docs, under backends, we have only shown postgresql but no sqlite! We need to add sqlite to the list of supported database backends. The `postgres` doc is also out of date!
       — Shipped: new `documentation/docs/v0.0.1/backends/sqlite.mdx` covers configuration (the three URL forms `:memory:` / `sqlite://path?mode=rwc` / `sqlite:`), when-to-reach-for-SQLite framing, migrations (`AUTOINCREMENT` PKs + table-recreation for nullability changes), querying with `.on(&sqlite_pool)`, the SQLite inspectdb path, the v1 limits (no native arrays / JSONB / FTS / UUID / network types / RLS), and the SqlType→column-type table. `postgres.mdx` rewritten to match current reality: removed the stale "What's not there yet" section (inspectdb-pg, array/network/JSONB, RLS plugin all shipped); added a Postgres-only field types table; documented the `_pg` QuerySet variants; added the type catalogue; pointed at `umbra-rls` and the new sqlite page from "See also". Both pages link to `orm/models.mdx`'s per-type sections (the source of truth on individual fields).
28. [x] Django has a way of doing `prefetch_related` and other helpers like `select_related`, `F`, `Q` to avoid N+1 queries. Do we have the same functionality in our framework?
       — Shipped: `F`, `FExpr`, `Q`, `select_related`. `F::col("col")` returns an `FExpr` for column references; arithmetic via `.add(n)` / `.sub(n)` / `.mul(n)` / `.div(n)` enables atomic updates (`update_expr("views", F::col("views").add(1))`). `FColExt` trait on `IntCol`, `ForeignKeyCol`, `StrCol` adds `.eq_f(FExpr)` / `.ne_f(FExpr)` for column-vs-column WHERE. `Q::and(a, b)`, `Q::or(a, b)`, `Q::not(p)` compose predicates explicitly; the existing `&`/`|` operators are unchanged. `select_related("field")` and `select_related_many(&[...])` on `QuerySet` eagerly load FK fields via a batch `SELECT ... WHERE id IN (...)` after the main query; `ForeignKey<T>.resolved()` returns `Some(&T)` after loading. `#[derive(Model)]` now emits `HydrateRelated` with `fk_id_for` and `hydrate_fk` methods used by the select_related machinery. `prefetch_related` (reverse FKs, M2M, nested traversal `"author__manager"`) is deferred — documented in relationships.mdx. Tests: `tests/f_expressions.rs` (8), `tests/q_objects.rs` (8), `tests/select_related.rs` (9). Docs: `documentation/docs/v0.0.1/orm/expressions.mdx` (new), `documentation/docs/v0.0.1/orm/relationships.mdx` (updated with select_related + prefetch_related deferred section).
29. [x] The rest framework is also extended by some packages like `django-filter`, `django-rest-swagger`, `django-rest-framework-simplejwt` that provide additional functionality. Do we have the same functionality in our framework? The target is the django-filter like plugin for our rest framework. So first question is, can a plugin be extended? Next question is, can a plugin be used next to the other? Can a plugin be used in a view with a single registration point? And then, how can we create our own `filter` plugin for complex filtering and data modeling?
       — Shipped: django-filter-style query-string filtering built into `umbra-rest` as a first-class feature. `ResourceConfig::enable_filters()` opts a resource in; disabled by default so existing resources see no behaviour change. New module `plugins/umbra-rest/src/filtering.rs` contains `parse_filters()` (parser entry point), `FilterClause` (WHERE fragment + ordered bindings), and `FilterValue` (per-type binding enum). Lookup grammar: `eq` (default), `ne`, `gte`, `lte`, `gt`, `lt` (numeric/date/datetime only), `in` (comma-separated, expands to `IN (?,?,?)`), `contains` (`LIKE %v%`), `icontains` (`UPPER(col) LIKE UPPER(%v%)`), `startswith` (`LIKE v%`), `isnull` (nullable columns only, value = "true"/"false"). All lookups are type-validated at parse time: range ops on text fields, string-pattern ops on non-text fields, and `isnull` on NOT NULL columns all return HTTP 400 with a clear "X is not available for field Y of type Z" message. Empty values (`?published=`) also 400. Filters are applied before pagination so the paginated `count` reflects the filtered result set. The list handler's `fetch_rows` and the new `count_rows_filtered` both accept a `&FilterClause` and bind the values via `bind_filter_values`. `RestPlugin` gains a `filters_enabled: HashSet<String>` and the `resource()` merge path propagates the flag. 9 new tests in `plugins/umbra-rest/tests/filtering.rs`. Docs: new "Filtering" section in `documentation/docs/v0.0.1/plugins/rest.mdx`. Deferred: granular per-field/per-lookup allowlist (`filter_fields`/`filter_lookups`), OR-composition via repeated keys, relation traversal (`author__name__icontains`), separate `umbra-filter` crate.
30. [x] Do we create tables based on plugin namespace ie `blogs_Post` `main_Category`? I ask this since it will be essential for the migrations to tell whether a table was moved to a different namespace and it just needs to be renamed instead of creating a new table and deleting the old table. This goes hand in hand with the cli makemigrations command in which we have to quee and show the user or ask the user some questions like did you rename/move the table? So the makemigrations command needs to be strong. So migrations can have enough data about the changes safely.
       — Shipped: two cooperating features, both opt-in and backward-compatible.
       (1) **Plugin-namespaced table names.** `#[umbra(plugin = "blog")]` on a model emits `const TABLE: &'static str = "blog_post"` instead of the bare `"post"`. Precedence: explicit `table = "..."` always wins over a `plugin = "..."` prefix, which wins over the bare snake_case default. Built-in plugins (auth, sessions, admin, tasks) are NOT changed — their table names are stable DB identifiers. `Model::NAME` (the Rust struct name) is unaffected; it remains the rename-detection anchor.
       (2) **Rename detection in `makemigrations`.** `diff()` now runs two passes before falling back to drop+create. First pass: if a model's `Model::NAME` is unchanged but its `Model::TABLE` changed (the common case when the `plugin` attribute is added to an existing model), emit `Operation::RenameTable { from, to }` — zero DropTable + CreateTable. Second pass: among still-unpaired drops and creates, if the column shapes are bit-identical (sorted (name, ty, nullable, fk_target) tuples), emit `RenameTable` plus a stderr warning naming both sides so the developer can verify. A stdout message is printed on every first-pass rename; the stderr warning fires only on second-pass heuristic matches. `RenameTable` DDL: `ALTER TABLE "from" RENAME TO "to"` on both SQLite and Postgres (rendered via sea-query's `Table::rename()`). `suffix_for` produces `rename_<from>_to_<to>` filenames. Tracking table rows are keyed by `(plugin, migration_name)` and are not affected by table renames inside the migration.
       Files touched: `crates/umbra-macros/src/lib.rs` (parse `plugin` key, compute namespaced table name), `crates/umbra-core/src/migrate.rs` (add `RenameTable` variant, two-pass `diff`, `suffix_for`, SQLite + Postgres renderers), `crates/umbra-core/src/inspect.rs` (exhaustive match arm), `crates/umbra-core/tests/migrate.rs` (exhaustive match arm), `documentation/docs/v0.0.1/orm/models.mdx` (new "Plugin-namespaced tables" subsection), `documentation/docs/v0.0.1/migrations/managed-migrations.mdx` (new "Rename detection" section). New test files: `crates/umbra-core/tests/namespacing.rs` (6 tests), `crates/umbra-core/tests/rename_detection.rs` (6 tests).
       Deferred: interactive CLI prompts asking the user to confirm ambiguous renames (the second-pass warning is the v1 substitute), per-plugin `table_prefix()` hook on the `Plugin` trait (the model-level attribute is simpler and doesn't force a shared prefix on every model in a plugin), cross-snapshot rename-tracking for squashed migrations.
31. [x] Custom User Model implementation and setting the custom user model in settings
       — Shipped: `UserModel` trait in `plugins/umbra-auth/src/lib.rs`. Required methods: `id() -> i64`, `username() -> &str`, `password_hash() -> &str`, `set_password_hash(String)`. Default-impl optional flags: `is_active()` (true), `is_staff()` (false), `is_superuser()` (false). `AuthUser` implements the trait unchanged. `AuthPlugin<U: UserModel = AuthUser>` is now generic with `PhantomData<U>`; `AuthPlugin::default()` and all existing code continue to work without annotation. `authenticate<U>` and `set_password<U>` are generic over `U: UserModel`; creation helpers (`create_user`, `create_user_with_flags`, `create_superuser`) remain `AuthUser`-specific because constructing an arbitrary `U` from a fixed column set is not possible without a user-supplied factory. `AuthPlugin.user_model_name: Option<String>` is an informational-only field for admin/OpenAPI nav; dispatch is entirely through the type parameter, not a string setting. `umbra-sessions` and `umbra-admin` reference `AuthUser` concretely - documented in `documentation/docs/v0.0.1/plugins/auth.mdx` under "What requires manual adjustment" rather than refactored, because making sessions/admin generic is a separate follow-on. 9 new tests in `plugins/umbra-auth/tests/custom_user.rs`; 11 existing tests in `tests/integration.rs` pass unchanged after trivial `AuthPlugin` -> `AuthPlugin::<AuthUser>::default()` fixes. All downstream plugin test files (`umbra-sessions`, `umbra-admin`, `umbra-openapi`) updated similarly. See `documentation/docs/v0.0.1/plugins/auth.mdx`.
32. [x] There is no dedicated docs page for template helpers like humanize etc
       — Shipped: new `documentation/docs/v0.0.1/templates/helpers.mdx`. Covers 40+ minijinja built-ins across six sections (strings, numbers, collections, default/type, global functions, loop helpers) with `{{ value | filter }}` examples. Calls out that umbra registers zero custom filters today, names django-style helpers absent at v1 (`naturalday`, `intcomma`, etc.) with the recommended workaround (compute in the handler, pass as context strings), and points at the `Plugin::on_ready` extension hook for future filter registration. Links the upstream minijinja filter docs for the full reference catalogue.
33. [x] We don't have proper role management, hence contenttypes and user groups. Django has groups which automatically handles actions. We need to fix this up, we can create groups too, we can have that model level roles and at the same time we need this on top of RLS. If a user wants to use RLS they can do that otherwise they can use groups and such roles. There should be a complete middleware for this for db access per logged in user etc. Related to #31. We will need to map roles to the active User model implementation whether default or custom User model. Also, we shall build a UI in the admin to do this and update docs on how to create roles per model for the User etc. This means, we don't control access via is_superuser perse fully but rather with proper RBAC (Proper permissions). We need a `hasPermission` method after this for the user
       — Shipped: new `plugins/umbra-permissions/` crate providing the Django-style RBAC data model + query layer. Five models in `models.rs`: `ContentType` (id, app_label, model — one row per registered Model), `Permission` (id, content_type_id FK, codename, name), `Group` (id, name), `GroupPermission` (id, group_id FK, permission_id FK), `UserGroup` (id, user_id, group_id FK), `UserPermission` (id, user_id, permission_id FK). The query layer in `perm.rs` exposes `has_perm(user_id, "<app>.<codename>")` and `user_perms(user_id) -> HashSet<String>` as free async functions taking `user_id: i64` rather than a generic `UserModel` instance — keeps the dependency arrow `umbra-permissions → umbra-auth` clean (no cycle). `has_perm` returns true when (a) the user is a superuser, OR (b) a direct `user_permission` row matches, OR (c) the user is in a group that has the permission — single JOIN, not three queries. Standard permissions (`add_<model>`, `change_<model>`, `delete_<model>`, `view_<model>`) auto-create at boot via `PermissionsPlugin::on_ready` for every registered model. 7 new tests in `tests/integration.rs`. Docs: `documentation/docs/v0.0.1/plugins/permissions.mdx` (new). Deferred: admin UI for managing groups/perms (depends on gap 19's Tailwind admin), `permission_required(perm)` layer from gap 26 (now unblocked by this gap — a follow-up), RLS predicate injection (umbra-rls's responsibility), ContentType auto-population is best-effort and may miss models registered late.
34. [x] Related to #32 - Add the third column in the tables for `usage`
       — Shipped: third "Usage" column added to all five filter-listing tables in `documentation/docs/v0.0.1/templates/helpers.mdx`: strings (13 rows), numbers (4 rows), collections (16 rows), default/type (5 rows), and global functions (4 rows) — 42 rows total. All snippets use canonical minijinja invocation form (`{{ value | filter }}` for pipe filters; `{% for i in range(5) %}` / `{% set ... %}` for global functions that are naturally used in tag context rather than expression context).
35. [x] We should catch internal server errors and allow the developer to use/display the error if they want to and also we shall create default 404 and 500 error pages using tailwindcss(Related to #19)
       — Shipped: three cooperating features, all additive on `App::builder()`.
       (1) **`on_server_error` hook.** `App::builder().on_server_error(|err, path| { ... })` registers a `Fn(&str, &str)` closure that fires before the 500 template is rendered. Used for Sentry, Datadog, custom logging. Cannot change the response.
       (2) **Panic catching is now always-on.** Previously the `CatchPanicLayer` was only installed when `.server_error_template(...)` was explicitly set. Now it installs whenever default pages are enabled (the default) OR a hook is registered. Panicking handlers produce a graceful 500 instead of aborting the connection even with no explicit configuration.
       (3) **Default Tailwind 404/500 templates.** Two HTML pages embedded via `include_str!` in `crates/umbra-core/src/templates/defaults/`. Both use inline Tailwind utility classes and include a `<style>` reset so they degrade to functional unstyled HTML when Tailwind isn't loaded. The 500 page renders an expandable dev-mode block (error message, cause chain, request path) when `settings.environment == Dev`; the block is empty in `Prod`/`Test`. Templates registered into minijinja at `__umbra__/default_404.html` and `__umbra__/default_500.html` in `templates::init` before any disk directories are scanned. Opt out via `App::builder().disable_default_error_pages()`.
       Cross-reference gap 19 (Tailwind, deferred): this gap uses inline Tailwind utility classes without requiring a Tailwind build pipeline — the classes work with any stylesheet that includes them (Tailwind CDN, pre-compiled CSS), and the fallback `<style>` reset makes pages usable without any stylesheet.
       Follow-up `4798f22`: handler-Err 500s also route through the template now (was panic-only at first ship). New response-rendering middleware (`render_500_middleware`) intercepts any non-HTML 500 response and re-renders through the configured 500 template, also firing `on_server_error`. HTML 500s (already rendered by `CatchPanicLayer` or by the handler itself) pass through unmodified.
       New files: `crates/umbra-core/src/templates/defaults/default_404.html`, `default_500.html`, `crates/umbra-core/tests/error_handling.rs` (16 tests). Modified: `src/errors.rs` (hook, defaults, dev context, `render_500_middleware`), `src/app.rs` (two new builder methods, panic + response middleware wiring), `src/templates.rs` (register defaults at init time), `tests/error_pages.rs` (updated for 2-arg `server_error_panic_handler`), `documentation/docs/v0.0.1/web/error-pages.mdx`.
36. [x] Related to #26, update the docs on how to use this structure for auth gating! And its like you forgot to do so for every feature implementation yet its in claude.md!
       — Shipped: new `documentation/docs/v0.0.1/web/auth-gating.mdx` (sidebar position 3 under Web) as the discoverable entry point for the `LoggedIn<U>` extractor + `LoginRequiredLayer` story. Cross-links into the full reference in `plugins/auth.mdx#guarding-views`. Fixed stale section in `documentation/docs/v0.0.1/auth/users-and-passwords.mdx` that still listed the `Auth<U>` / `#[login_required]` shape as deferred and never updated when gap 26 shipped — replaced with a "Guarding views" section pointing at the new web/ page, and rewrote the "What ships now vs deferred" list to reflect actual current state (custom user model shipped, extractor + layer shipped, login/logout/reset shipped via sessions + email, RBAC/groups still deferred → gap 33). The full reference under `plugins/auth.mdx` was already there from gap 26 (lines 116-235) — this gap closes the discoverability and stale-cross-reference issues. Companion commit `ea4dbe1` swept all 21 broken `/v0.0.1/...` MDX cross-references across 8 files to `/docs/v0.0.1/...` (SvelteKit only routes docs under `/docs/...`).
37. [x] In django, if you have a `Post` model, and it has `author` which is an fk to User model, when you get the post, in the templates, you can call `post.author.first_name` etc. In other words, it does prefetch the related and linked models on the fly deeply! Do we have that capability?
       — Shipped as a side-effect of gap 28's `select_related`. `ForeignKey<T>` gains a `resolved: Option<Box<T>>` slot; when populated by `select_related("author")`, `serde::Serialize` emits the full `T` object instead of the bare integer. Template rendering via minijinja therefore resolves `{{ post.author.username }}` correctly — the template engine reads `ctx["author"]["username"]` from the serialised JSON context. No template-engine changes were needed: the behaviour falls out of the serialisation contract. Backward compat is preserved: without `select_related`, `ForeignKey<T>` still serialises as a bare integer. See `tests/select_related.rs::select_related_serialises_as_full_object` for the round-trip verification and `documentation/docs/v0.0.1/orm/relationships.mdx` for the user-facing docs.
38. [x] How do we handle atomic txs here? We need this since its veryvery critical and nice to have
       — Shipped: three cooperating layers in the ORM/db module, all additive.
       (1) **`Transaction` wrapper type.** `umbra::db::Transaction` wraps either a `sqlx::Transaction<'static, Sqlite>` or a `sqlx::Transaction<'static, Postgres>` in a `TransactionInner` enum. Exposes `as_sqlite_mut()`, `as_pg_mut()`, `backend_name()`, `commit()`, and `rollback()`.
       (2) **`transaction` / `transaction_sqlite` / `transaction_pg` closure helpers.** Each takes a `for<'a> FnOnce(&'a mut Transaction) -> TxFuture<'a, T, E>` closure (a `Pin<Box<dyn Future>>`). Calls `begin`, runs the closure, commits on `Ok`, rolls back best-effort on `Err`, and returns the closure's result. `begin` / `begin_sqlite` / `begin_pg` are the manual-control variants. `TxFuture<'a, T, E>` is the named type alias for the HRTB-compatible pinned future that stable Rust requires for closures capturing `&mut Transaction`.
       (3) **ORM integration.** `QuerySet::on_tx(&mut Transaction) -> QuerySetTx` attaches a transaction to a query chain; all terminals on `QuerySetTx` (`fetch`, `first`, `count`, `exists`, `get`, `delete`, `update_values`, `create`) execute inside the open transaction. `Manager::create_in_tx(instance, &mut tx)` and `Manager::bulk_create_in_tx(instances, &mut tx)` are the Manager-level entry points. Existing `.on(&pool)` and ambient-pool calls are unchanged.
       Facade: `umbra::db::{Transaction, TxFuture, begin, begin_sqlite, begin_pg, transaction, transaction_sqlite, transaction_pg}` + top-level `umbra::transaction(...)` re-export. `umbra::orm::QuerySetTx` also re-exported. Tests: `crates/umbra-core/tests/transactions.rs` (8 tests). Docs: `documentation/docs/v0.0.1/orm/transactions.mdx`. Deferred: savepoints / nested transactions, per-transaction isolation-level control, two-phase commit.
39. [x] The startapp command should check if there is an existing app to avoid app confusions (Check if it is implemented that else implement)
       — Shipped: `scaffold_app` already rejected `plugins/<name>/` collisions (commit 95f0709's `AlreadyExists` branch) but had no check for the harder case — picking a name that collides with a built-in umbra plugin like `auth` or `sessions`. The user would scaffold an empty plugin and only discover the conflict at `App::build()` time when route mounts or migration tables collided. Now `scaffold_app` checks the requested name against the `RESERVED_PLUGIN_NAMES` const (`admin`, `app`, `auth`, `cache`, `email`, `openapi`, `permissions`, `rest`, `rls`, `security`, `sessions`, `signals`, `static`, `tasks`) before creating any files. Reserved-name attempts return `ScaffoldError::ReservedName(name)` with a message that lists the full reserved set so the user can pick again. Hyphen/underscore normalised (`umbra-auth` and `umbra_auth` both reject). The `AlreadyExists` message also reworded to say "an app already exists at ..." rather than the generic "target ..." so the diagnostic clearly names what went wrong. 4 new tests in `crates/umbra-cli/src/scaffold.rs::tests` (reserved-name rejection per name, error message lists the set, AlreadyExists wording). Docs: new "Names umbra refuses to scaffold" subsection in `documentation/docs/v0.0.1/cli/startproject.mdx`.
40. [x] I haven't seen any places for signals! Also this goes hand in hand with the Tasks plugin. We need to finalize that to have the #[task] macro. Remember for signals, I might want to run tasks behind the scenes ie after user signup etc. Another thing I think covered from before is the settings/env variables, I should be able to get those anywhere in the app easily. So document that well how to use env vars and settings. Update this doc too afterwards http://localhost:5173/docs/v0.0.1/plugins/tasks
       — Shipped: three cooperating pieces.
       (1) **`#[task]` attribute macro** in `crates/umbra-macros/src/lib.rs`. The user writes `#[task] async fn send_welcome(payload: WelcomeEmailPayload) -> Result<(), String>`; the macro preserves the original function and emits a sibling `pub fn register_send_welcome()` that wraps the body with `serde_json::from_str` + JSON-payload deserialisation and calls `umbra_tasks::register_handler` against the function's identifier. `#[task(name = "blog.send_welcome")]` overrides the registration name. Constraints enforced via `compile_error!`: must be `async fn`, exactly one parameter, return `Result<(), String>`. 9 new tests across `crates/umbra-macros/tests/task_macro.rs` (6: 2 inline + 4 trybuild rejection snapshots) and `plugins/umbra-tasks/tests/macro_integration.rs` (3: happy path, name override, bad-payload deserialise error). Re-exported as `umbra::task` through the facade. The macro references `::umbra_tasks::register_handler` and `::umbra_tasks::_serde_json` directly — `umbra` facade cannot depend on `umbra-tasks` (cycle), but every user crate that applies `#[umbra::task]` already has `umbra-tasks` in deps because `TasksPlugin` requires it.
       (2) **Signals docs page** at `documentation/docs/v0.0.1/plugins/signals.mdx` (sidebar position 12). Covers plugin registration, `subscribe` / `subscribe_async` / `emit` API, the durable-signal pattern (signal handler enqueues a task so the work survives the process crash), payload-shape conventions, the built-in signals shipped at v0.0.1 (`post_login`, `post_logout`, `post_signup`), and what's deferred (typed events via enum, cross-process broker, persistence). Cross-links to `tasks.mdx` and `getting-started/settings-and-env.mdx`.
       (3) **Settings + env-vars docs page** at `documentation/docs/v0.0.1/getting-started/settings-and-env.mdx` (sidebar position 2). Covers the three-source loading precedence (defaults → `umbra.toml` → `UMBRA_`-prefixed env vars), every known field with its env-var name and default, the canonical `umbra.toml`, `umbra::settings::get()` / `get_opt()` access from any context (handler, on_ready, `#[task]`, signal handler), the `extra` field for app-defined keys with `extra_str()` and nested-table access patterns, the explicit "umbra does not parse `.env` files" call-out with the recommended `set -a; source .env` workaround, and the `--addr`-flag precedence rule.
       (4) **`tasks.mdx` rewritten** to put `#[task]` as the recommended path; the stringly-typed `register_handler("name", closure)` form demoted to "advanced / dynamic registration" use cases. Added "Combining with signals" callout pointing at the durable-fire-and-forget pattern. The cross-link between signals → tasks → settings is now bidirectional.
41. [x] The commands are not well explained, even with basic usage. http://localhost:5173/docs/v0.0.1/cli/management-commands, ie there is serve command but I have no idea how to pass in the port or even `0.0.0.0:8080`
       — Shipped: `documentation/docs/v0.0.1/cli/management-commands.mdx` rewritten so every subcommand leads with copy-pasteable invocations before listing flags. `serve` section shows four common cases (default 127.0.0.1:3000, custom localhost port, 0.0.0.0:8080 for production / Docker / LAN, env-var equivalent) plus precedence rules (`--addr` > `UMBRA_BIND_ADDR` > `umbra.toml` > default). New dedicated sections for `makemigrations`, `dev`, `dumpdata` / `loaddata`, `createsuperuser`, and `tasks-worker` — each starts with a real command, then a flags table with defaults. `dev` covers the watch-path syntax explicitly: each path needs its own `-w` flag, comma-separated values are NOT supported (would be read as one path literally named "src/,templates/"). Cross-references the in-process template hot-reload so users know they don't need `dev` for `.html` edits. The old "Plugin-contributed subcommands" table collapsed to a pointer at the dedicated sections.
42. [x] No doc illustrating use of umbra.toml or .env anywhere. This is part of env and settings module
       — Shipped in commit `1cc47fc` as part of gap 40's `documentation/docs/v0.0.1/getting-started/settings-and-env.mdx`. The page has dedicated sections "umbra.toml" (with a copy-pasteable example covering database_url, bind_addr, secret_key, environment, log_level, allowed_hosts, and a `[databases]` named-pool block) and "A `.env` file is not loaded automatically" (with the explicit non-support callout and the recommended `set -a; source .env; set +a` workaround). Plus a precedence table showing umbra.toml is layered between built-in defaults and UMBRA_-prefixed env vars. The sibling section "Custom settings (the `extra` field)" shows both env-var and `umbra.toml` paths for app-defined keys (`UMBRA_OPENAI_API_KEY` vs `[external.openai]`). No new docs work needed; this gap was already closed by the time it was filed.
43. [ ] Can a plugin be extended ie extension of AdminPlugin with more features maybe, like more pages, components, etc
44. [x] I forgot - When creating models, is it possible we define display name and icon (ie "users" for a users table)? Or can the Admin Plugin allow for this where you can rename a given model (Like display name) and dashboard icon?
       — Shipped: `#[umbra(display = "Users", icon = "users")]` attributes on `#[derive(Model)]`. Two new associated consts on the `Model` trait: `DISPLAY` (defaults to `NAME`) and `ICON` (defaults to `"database"`). The derive macro picks up `display = "..."` and `icon = "..."` keys alongside the existing `table` and `plugin` keys and emits the two consts. `ModelMeta` gains `display: String` and `icon: String` fields populated from `Model::DISPLAY` / `Model::ICON`; `#[serde(default)]` on both fields keeps existing JSON snapshots forward-compatible. `AdminRegistry::apps()` rewired to synthesise a default `AdminRegistration` for every model in the global registry (auto-discovery — no explicit `.register(...)` call needed); explicit registrations overlay and win. `AdminModel::label()` / `AdminModel::icon()` on an explicit registration override the struct-level defaults. Ordering: named plugins alphabetically first, implicit `"app"` bucket last. 4 new tests in `crates/umbra-macros/tests/display_icon.rs` (explicit attrs, defaults, display-only, icon-only). 4 new tests appended to `plugins/umbra-admin/tests/phase1_shell.rs` (explicit label override, explicit icon, auto-discovery, theme toggle onclick). Updated `documentation/docs/v0.0.1/plugins/admin.mdx` with a "Model display name and sidebar icon" section.
45. [x] The admin templates, both base.html and login.html have the same general layout ie tailwind config, either choose one, create a helper js function in a js asset or simply create wrapper.html..
       — Shipped: new `templates/wrapper.html` holds `<!DOCTYPE>`, `<html>`, `<head>` with Tailwind config + Lucide + Inter font + design-token block + theme persistence IIFE (including `toggleTheme`, `applyTheme`, `sidebarFilter`, `toggleUserMenu` + Lucide `createIcons` on DOMContentLoaded). `base.html` extends `wrapper.html` via `{% extends "admin/wrapper.html" %}` and overrides `{% block body %}` with the sidebar + topbar + `{% block content %}` slot. `login.html` extends `wrapper.html` directly (no chrome) and overrides `{% block body %}` with only the centered login card. Theme toggle wiring also fixed (gap 1): `onclick="umbra.toggleTheme()"` was already on the button but the IIFE wasn't exposing `toggleTheme` on the `umbra` object — now it is, from `wrapper.html`, so both chrome pages and the login page honour the persisted preference on load. `wrapper.html` registered in the template engine's `ENGINE` OnceLock alongside the existing templates. 
46. [x] Django has a way of defining the string method of a column. Instead of displaying all the columns, we can display a value from such a method ie id by default and then the actions column instead of all columns in a given model as default. We can use another macro like `#[umbra(string = true, max_length=50)]. So behind the scenes we pick the first field with this from the model and limit characters to this. Only applicable to string col types only. But maybe you have implemented it, I don't know, if so, update the docs under models else do some implementation around it and update the docs
       — Shipped in commit `c533c99`. `parse_umbra_field_attr` in `umbra-macros` now accepts `string` (bareword or `= true`) and `max_length = N`. Two new fields on `FieldSpec` and `migrate::Column` (`is_string_repr: bool`, `max_length: u32`) carry the values through to the registry and snapshots. `umbra-admin/src/discovery.rs::default_list_display(meta)` returns `[pk, first string_repr field]` (falls back to `[pk, first Text column]`, then to all columns) when no `list_display` is configured. `rows::apply_max_length_truncation` walks each fetched row and truncates cells per their column's cap — UTF-8 char count, appends `…` so the cut is visible. Docs added in `documentation/docs/v0.0.1/orm/models.mdx` under "Display hints (`string`, `max_length`)".
47. [x] Complete Admin Rename ie site title, description, default colorscheme (primary color and theming)
       — Shipped in commit `efd3436`. Three chainable builders on `AdminPlugin`: `.site_title("...")`, `.site_description("...")`, `.brand_color("#xxx")`. New `plugins/umbra-admin/src/branding.rs` owns the struct + a `OnceLock<AdminBranding>` that `Plugin::routes()` seals once. The template engine reads the sealed value on first init and registers `site_title`, `site_description`, `brand_color` as Jinja globals. Templates touched: `wrapper.html` (title + meta + brand-color `<style>` that reassigns `--primary` across `:root` and `.dark`), `base.html` (sidebar header), and every per-page title block in `changelist/detail/form/dashboard/list/index/login`. Handlers need no per-render context changes — the values are always in scope.
48. [x] Plugin of `#umbra(plugin = "app")` needs further details ie I tried to change the plugin the `Article` model and I started encountering errors. Plugin `app` does not exist, but you realize the UI showed extra table called `Articles` under `APP` namespace
       — Shipped in commit `1475c43`. `"app"` is the implicit namespace for every user-binary model registered via `App::builder().model::<T>()`, so the macro now skips the table prefix when `plugin == "app"`. Previously, tagging `Article` with `#[umbra(plugin = "app")]` renamed the table from `article` to `app_article` and triggered a destructive rename migration against the existing database (the user had `0002_rename_article_to_app_article.json` and `0003_rename_app_article_to_article.json` artifacts as evidence). The model still groups under the `app` sidebar bucket — that's governed by the runtime registry, not the table name — so the user-visible label they wanted is unchanged.
49. [/] Static plugin and media plugins, we need this out of the box for files under `static` or `assets` or `media` folders. Also, we need this for `S3` or create a new `S3 compatible` plugin (We should use any existing libraries for this) including a library to handle images, generate metadata for them like thumbnails and store to db. Like we need a proper media/file handler a user can easily integrate as a plugin and get some things automagically like file upload and url back etc.
       — Partially shipped. `umbra-static` already existed (filesystem dir → URL prefix); `documentation/docs/v0.0.1/plugins/static.mdx` extended with a "Referencing files from templates" section showing how to wire CSS / JS / images / favicon plus a `static_url` template-context pattern. New `umbra-media` plugin (commit ⟨in-progress⟩) ships the user-upload counterpart: `MediaPlugin::new(mount, dir).max_size(N)` plus a `save(filename, content_type, bytes)` helper, with every upload tracked in the framework-tracked `MediaFile` model (`id, key, filename, content_type, size, uploaded_at`). Docs at `documentation/docs/v0.0.1/plugins/media.mdx`. S3-compatible backend, image library (thumbnails, EXIF strip, format probing), and signed URLs are explicitly deferred to v0.1/v0.2/v0.3 — design captured in `docs/decisions/2026-06-02-media-and-s3.md` with the `MediaBackend` trait extraction plan.
50. [x] http://localhost:5173/docs/v0.0.1/plugins/permissions - We need to finish this, the admin now is fully tailwind and update the docs
       — Shipped three pieces. (1) **`permission_required(perm)` axum layer** in `plugins/umbra-permissions/src/middleware.rs`. Resolves the `umbra_session` cookie to a user id via the new public `umbra_auth::current_session_user_id(&headers)` helper, then calls `has_perm`. `PermissionRequired::api(perm)` returns 401 JSON on unauthenticated, 403 JSON on lacks-perm; `PermissionRequired::html(perm, login_url)` returns 302 to `login_url?next=<uri>` on unauthenticated. Superuser bypass via a tolerant `SELECT is_superuser FROM auth_user WHERE id = ?` that returns false (no bypass) when the column is absent, so custom user models opt out by simply not having the column. (2) **umbra-permissions → umbra-auth dep escalation**: dev-dep promoted to runtime so `permission_required` can call into the session helper. This is the correct arrow direction (permissions sit above auth in the layering). Pulls `axum`, `axum-core`, `http`, `tower` as runtime deps; `tokio` gains `rt-multi-thread` to fix a `block_in_place` regression on the existing `on_ready`. (3) **Polish + admin discoverability**: all six permission models (`ContentType`, `Permission`, `Group`, `GroupPermission`, `UserGroup`, `UserPermission`) gained `display = "..."` and `icon = "..."` attributes so they appear in the admin sidebar with proper labels under the `permissions` plugin bucket — Plugin::models() auto-discovery (from gap 13/44) means no explicit admin registration is needed. 6 new tests in `plugins/umbra-permissions/tests/middleware.rs`. Docs: `documentation/docs/v0.0.1/plugins/permissions.mdx` rewritten — removed the "deferred admin UI" note, added a new "Gating routes with `permission_required`" section with API + HTML usage examples, superuser-bypass section, and composition story with `login_required`. The "deferred admin UI" callout is replaced by an honest "the six tables are visible as standard models; a Django-style permission-matrix widget lands in a follow-up." Deferred: `Meta.permissions = [...]` syntax, object-level / row-level permissions, RLS predicate injection (umbra-rls's job).
51. [x] The admin plugin has some unimplemented stuff like inline edit, it does not appear in the frontend, the password reset. Make sure everything in the backend is fully supported on the frontend
       — Backend audit found every feature in this gap is **already shipped end-to-end** — the appearance of incompleteness was a discoverability problem, not a code gap. The actual landings: (1) **Inline cell edit**: `plugins/umbra-admin/src/handlers/inline_edit.rs` exposes `GET /admin/{table}/{id}/cell/{field}/edit` + `POST /admin/{table}/{id}/cell/{field}`; `templates/_macros/data_table.html` wires `dblclick` triggers + `field_editor.html` renders the editor fragment. Opt-in via `AdminModel::inline_edit_fields(&[...])`. (2) **Password reset on edit sheet**: `change_password_handler` in `handlers/sheet.rs` at `POST /admin/{table}/{id}/change-password` (validates, argon2-hashes via `umbra_auth::hash_password`, writes a `password_field` column, logs to `admin_audit_log`, fires a success toast). The "Change password" button at `sheet.html:249` calls `umbra._openChangePasswordDialog` (defined at `wrapper.html:547`) which opens an HTMX-driven dialog. Opt-in via `AdminModel::password_field("password_hash")`. (3) **Bulk actions** + **FK async combobox** + **Choices `<select>`** + **MultiChoice chip checkboxes** — all auto-wired with no opt-in, fully visible on every model. The shipped work in this gap: derive-demo gained `.inline_edit_fields(&["title", "status"])` on Article and a new `.register("auth_user").password_field("password_hash")` so a fresh `cargo run` from the example surfaces both features in the admin UI. Docs: new "**Opt-in features (and what makes them visible)**" table at the top of `documentation/docs/v0.0.1/plugins/admin.mdx` enumerating every feature, whether it's automatic, and the exact registration line that unhides it. Per-field validation errors (single string → per-column error map) are deferred — that's a bigger handler refactor for a separate gap; today the form renders a top-level red banner with the error message.
52. [x] We have choices and now we need to close this off by having a multichoice macro, more like an M2M field
       — Shipped: `MultiChoice<E>` wrapper type in `crates/umbra-core/src/orm/multichoice.rs`. Storage = single TEXT column carrying a CSV of the underlying `ChoiceField` enum's variant strings. The macro detects the type directly (no `#[umbra(multichoice)]` marker needed — `MultiChoice<E>` is already unambiguous), pulls `<E as ChoiceField>::VALUES` / `LABELS` from the inner enum at derive time, and sets a new `is_multichoice: bool` flag on `FieldSpec` + `migrate::Column`. The DDL emitter deliberately **skips** the single-value `CHECK (col IN (...))` constraint when `is_multichoice` is true (it would reject every legal CSV); validation happens via sqlx's `Decode` path which fails fast on any unknown segment. `#[umbra(default = "tag1,tag2")]` is reused — CSV initial value used both as the SQL `DEFAULT` and as the value `ALTER TABLE ADD COLUMN` applies. The wrapper exposes the usual collection surface (`as_slice`, `into_vec`, `push`, `contains`, `IntoIterator`, `Deref<Target=[E]>`, `FromIterator`); serde serialises to a JSON array `["a","b"]` and deserialises from either an array or a CSV string (so HTML form posts round-trip). Admin: `view.rs::input_kind` returns `"multiselect"` when `is_multichoice` is true; `templates/_macros/field_editor.html` and `sheet.html` both render a checkbox-chip group bound to a hidden CSV input via the new `initMultiChoicePickers` JS in `templates/wrapper.html` (idempotent, runs on `htmx:afterSwap`). The macro emits a `StrCol` predicate constant for filter chains. Tests: `crates/umbra-core/src/orm/multichoice.rs` (8 unit tests covering CSV round-trip, whitespace, unknown-variant rejection, serde forms, Deref) + `crates/umbra-core/tests/multichoice.rs` (3 integration tests covering `FieldSpec`, `migrate::Column`, and end-to-end sqlx round-trip via in-memory SQLite). Mass-patched ~43 sites across test fixtures + `inspect.rs` + `plugin_contract.rs` to add `is_multichoice: false,` to `Column` / `FieldSpec` literals. Docs: new "**`MultiChoice<E>` - multi-valued choices**" section in `documentation/docs/v0.0.1/orm/column-types.mdx` with the worked Article + Tag example, a "what's different from Choices" comparison table, and the v1 limits (no `Option<MultiChoice<E>>`, no regex CHECK on Postgres, no element-level membership filter — the `StrCol::contains` predicate is the v1 substitute). Facade re-exports `MultiChoice` from `umbra::orm` and the prelude.
