/target
/shop.db*
.env
Cargo.lock

# Tailwind build artefacts (gaps2 #20). Committed bits:
#   styles/{input.css,tailwind.config.js,package.json}  — source
#   static/css/shop.css                                  — compiled output
#   static/css/files/*.woff2 / *.woff                    — Inter weights
# Below is regenerated by `cd styles && npm install && npm run build`.
/styles/node_modules
/styles/package-lock.json
