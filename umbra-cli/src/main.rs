//! The `manage.py` equivalent binary for umbra.
//!
//! Subcommands (migrate, makemigrations, worker, inspectdb, ...) are added
//! as their milestones land. See `docs/specs/06-migration-engine.md` for the
//! migrate / makemigrations shape and `docs/specs/07-inspectdb.md` for
//! inspectdb.

fn main() {
    println!("umbra-cli — scaffold only at M0; subcommands not implemented yet");
}
